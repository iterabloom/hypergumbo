Original commit body from b53c88dc222eb761f7eaf1314b4a8405d618ca90:

prepare-release merges release/vX into dev, then opens the release PR from
dev→main (source=dev). Two per-PR-model gates mis-fired on that PR's cumulative
623-commit diff:

- pytest: the release-PR skip keyed on `source == release/*`, which a dev→main
  PR never matches, so it ran the targeted-manifest model against a cumulative
  diff it can't cover. Now also skips when `target == main` (full-suite covers
  release PRs post-merge); the release/* source case is kept for direct pushes.
- dco: re-checked Signed-off-by across the whole cycle and failed on one
  pre-DCO-gate commit (54a041c171, admin-josh's ADR-0044 refactor) that can't be
  signed retroactively without rewriting all of dev's history. Grandfathered by
  SHA with a documented reason; the gate stays strict for every other commit.

Unblocks the v7.0.0 dev→main release PR (#39).

Signed-off-by: jgstern-agent <josh-agent@iterabloom.com>

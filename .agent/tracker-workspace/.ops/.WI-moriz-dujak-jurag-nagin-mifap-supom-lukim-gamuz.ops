- op: create  # 3b4d
  at: '2026-06-05T04:03:14Z'  # 3b4d
  by: agent  # 3b4d
  actor: jgstern_agent  # 3b4d
  clock: 1  # 3b4d
  nonce: 3b4d  # 3b4d
  data:  # 3b4d
    kind: work_item  # 3b4d
    title: "validation_report violations_by_class all=0 despite 6644+ enumerable cross_field defects"  # 3b4d
    status: todo_hard  # 3b4d
    priority: 1  # 3b4d
    parent: INV-luhur-sital-lonar-tagiv-kuzab-mokol-jikot-fosug  # 3b4d
    tags:  # 3b4d
    - dogfood_tranch_03  # 3b4d
    - dogfood  # 3b4d
    isbefore: []  # 3b4d
    duplicate_of: []  # 3b4d
    not_duplicate_of: []  # 3b4d
    pr_ref:  # 3b4d
    description: "**Mechanic**: The validation report exposes buckets `axis_conformance`, `cross_field`, `id_format`, `verdict_enum`, `writer_contract`, all reporting 0 defects. Direct substrate scan finds 492 dangling-pass-id violations (cross-field), 4,507 :unresolved-with-is_resolved=True violations (cross-field), 1,645 empty-origin violations (cross-field), and others. Either the validation rules are not wired to check these classes, or the report is generated against a frozen rule set that pre-dates the check categories.\n\n**Expectation**: `cross_field` and `id_format` counters reflect real violations in the substrate, or the report names which rules are wired (so a zero count signals \"no instances of the rules that exist\", not \"no instances of any cross-field defect\").\n\n**Scope**: All five `violations_by_class` counters report zero on the self-corpus; multiple cross-field defects in the same substrate are enumerable from a direct scan.\n\n**Exposure**: Any consumer reading `violations_by_class.cross_field=0` and concluding the substrate is clean on that dimension.\n\n**Consequence**: Consumers are led to believe whole defect classes are absent when they are merely unchecked; the validation report's apparent passing grade is misleading.\n\n**Source**: F51.A3 (INDEPENDENT, HIGH).\n\nFiled via tranch 03 dogfood (passes 41-60, substrate sha256 a32c4a31..., HEAD 9a18959071). Tranch state: `~/hypergumbo_lab_notebook/dogfood_tranch_03/tranch_state.json`."  # 3b4d
    fields: {}  # 3b4d
- op: discuss  # b183
  at: '2026-06-05T16:02:53Z'  # b183
  by: agent  # b183
  actor: jgstern_agent  # b183
  clock: 2  # b183
  nonce: b183  # b183
  message: "[+ tranch_04: WI-mujor — related (card 029/F74.A1): the authoritative 110533-edge endpoint-integrity table shows 23 src-dangling edges (0.02%) pass the spec_validator clean because it has NO dangling-endpoint check — the same validator-all-zero gap this row tracks (cross_field/id_format counters report 0 despite enumerable defects).]"  # b183
- op: discuss  # 5a6b
  at: '2026-06-06T10:35:53Z'  # 5a6b
  by: agent  # 5a6b
  actor: jgstern_agent  # 5a6b
  clock: 3  # 5a6b
  nonce: 5a6b  # 5a6b
  message: "New concrete facet on the id_format counter (blind-severity-derived priority signal: P0). The published validation_report.violations_by_class is all-zero, but 1670 nodes have an id kind-SEGMENT that disagrees with the kind FIELD while still passing the shape regex (e.g. a node with kind field 'function' whose id ends ':route') — every one is invisible to the id_format validator. Root cause: _check_id_format / _CANONICAL_ID_PATTERN checks only SHAPE (segment count, colon-free tail, valid lang prefix, span pattern, lowercase kind suffix); it never compares the id's kind-segment to node.kind. So the all-zero report gives false structural-consistency assurance: the violation-class names (id_format, cross_field) imply far broader coverage than the predicates deliver."  # 5a6b
- op: discuss  # 9196
  at: '2026-06-08T03:21:25Z'  # 9196
  by: agent  # 9196
  actor: jgstern_agent  # 9196
  clock: 4  # 9196
  nonce: '9196'  # 9196
  message: "Re-surfaced in dogfooding tranche 10 (U02, F181.A2, severity 4). validation_report all-zero is a false clean-bill: uncovered defect classes the validator never checks — dangling-src endpoints and kind-slot mismatch. Same defect+fix as this item (counters report 0 because rules aren't wired, not because the substrate is clean). Specific instances map to WI-mujor (dangling-endpoint check) and WI-bovoh (kind-slot/id_format registry-scope gap). Fix: add dangling-endpoint + kind-slot validation checks (or scope/relabel the report)."  # 9196

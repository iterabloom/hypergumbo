- op: create  # 5160
  at: '2026-06-05T22:45:31Z'  # 5160
  by: agent  # 5160
  actor: jgstern_agent  # 5160
  clock: 1  # 5160
  nonce: '5160'  # 5160
  data:  # 5160
    kind: work_item  # 5160
    title: "edge.quality.score = round(confidence,3) diverges from top-level confidence for 257 dispatches_to/type_hierarchy edges"  # 5160
    status: todo_hard  # 5160
    priority: 4  # 5160
    parent:  # 5160
    tags:  # 5160
    - dogfood  # 5160
    isbefore: []  # 5160
    duplicate_of: []  # 5160
    not_duplicate_of: []  # 5160
    pr_ref:  # 5160
    description: "tranche-05 chunk-1 (pass 82 / F82.A6; preserved per operator). edge.quality.score = round(confidence,3) at ir.py:827 (_derive_edge_quality), so quality.score != top-level confidence for the 257 edges whose confidence carries >3 decimals (entire dispatches_to/type_hierarchy family). Cosmetic/minor consumer confusion. Reproduced fresh. Evidence: agent_notes + dogfood_tranche_05/pass_82.md."  # 5160
    fields: {}  # 5160
- op: discuss  # 99ae
  at: '2026-06-06T04:08:37Z'  # 99ae
  by: agent  # 99ae
  actor: jgstern_agent  # 99ae
  clock: 2  # 99ae
  nonce: 99ae  # 99ae
  message: "Dogfood tranche 05 reproduction (F91.A2 / anon-card-002): TRUE-DUPLICATE confirmation on frozen substrate. For 110276/110533 edges quality.score == confidence exactly; the remaining 257 — ALL type=dispatches_to, evidence_type=type_hierarchy, quality.reason=low_confidence_fallback — have quality.score == round(confidence,3) != confidence (e.g. confidence=0.3470110468942836 vs quality.score=0.347). One producer (the type-hierarchy linker) rounds its score to 3 decimals while every other producer copies confidence verbatim. A consumer comparing edge.confidence to edge.quality.score for equality finds 257 false mismatches; a consumer trusting quality.score as authoritative confidence loses precision. (exact:110276 / rounded_match:257 / neither:0.) Blind severity 2/5, reproduced on frozen substrate."  # 99ae
- op: discuss  # c7c2
  at: '2026-06-06T10:36:50Z'  # c7c2
  by: agent  # c7c2
  actor: jgstern_agent  # c7c2
  clock: 3  # c7c2
  nonce: c7c2  # c7c2
  message: "Re-confirmed quality.score==confidence on a full behavior map (TRUE_DUPLICATE; blind-severity-derived priority signal: P2). quality.score == confidence verbatim on 110276/110533 edges; the remaining 257 differ only by round(confidence,3) and are all the type-hierarchy / type_hierarchy / low_confidence_fallback bucket. So quality.score carries no signal distinct from confidence — a consumer reading it expecting an independent quality metric gets the same number back, and combining the two double-counts one value."  # c7c2
- op: discuss  # f4c8
  at: '2026-06-11T04:25:55Z'  # f4c8
  by: agent  # f4c8
  actor: jgstern_agent  # f4c8
  clock: 4  # f4c8
  nonce: f4c8  # f4c8
  message: "2026-06-10 design-interview ruling (ADR-0039, merged in PR #4181): quality.score is deleted via a one-version deprecation, so the score-vs-confidence divergence on the 257 dispatches_to/type_hierarchy edges disappears with the field; confidence_source and rank_score absorb the legitimate roles. Waits on the deprecation implementation."  # f4c8
- op: discuss  # 5910
  at: '2026-07-13T02:30:35Z'  # 5910
  by: agent  # 5910
  actor: jgstern_agent  # 5910
  clock: 5  # 5910
  nonce: '5910'  # 5910
  message: "ADR-0039 ruling 4 DEPRECATION step landed (PR: deprecate Edge.quality; SCHEMA_VERSION 0.14.5->0.14.6). Edge.quality now carries deprecated:true in docs/schema.json + a spec deprecation note; still emitted this release. This item CLOSES when the field is removed next release (the one-version window's removal step)."  # 5910

- op: create  # 85d3
  at: '2026-04-08T01:42:35Z'  # 85d3
  by: agent  # 85d3
  actor: jgstern_agent  # 85d3
  clock: 1  # 85d3
  nonce: 85d3  # 85d3
  data:  # 85d3
    kind: work_item  # 85d3
    title: "Stop-hook REPLY-FIRST CYCLE branch (suppress drive prompt while reply debt exists)"  # 85d3
    status: todo_soft  # 85d3
    priority: 3  # 85d3
    parent:  # 85d3
    tags:  # 85d3
    - agent_governance  # 85d3
    - developer_experience  # 85d3
    before: []  # 85d3
    duplicate_of: []  # 85d3
    not_duplicate_of: []  # 85d3
    pr_ref:  # 85d3
    description: "Per ~/hypergumbo_lab_notebook/notebookjournal_04072026_1518.md 'Elaboration on item 6' — the user explicitly identified this as the clearest remedy because it is the only proposal that changes the agent's incentive structure rather than adding tools/knowledge.\n\n**Change:** in packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py:generate_guidance, branch when items_with_unread_blocking + unread_non_blocking > 0 and emit a *different* guidance file rather than the standard TODO/bakeoff one with an unread-message section appended. The new guidance:\n\n> REPLY-FIRST CYCLE — N unread human message(s)\n>\n> You are not in autonomous-drive mode this cycle. Do not start new code tasks, do not run bakeoffs, do not pick from tracker ready. Your only assignment is to clear reply debt. For each item:\n> 1. tracker show <ID> — load the full thread + description\n> 2. Classify: approval, directive, or question\n> 3. Questions: plan the investigation (Read/Grep, Explore subagent, WebSearch on allowlisted domains) BEFORE drafting\n> 4. tracker discuss <ID> — post the reply\n\n**Critical:** while reply debt exists, the hook does NOT emit 'AUTONOMOUS MODE: N TODOs block stopping'. The TODO list isn't shown at all. This is what gives the change teeth — the agent can't make progress on anything else without first clearing the reply.\n\n**Scope of suppression:** default to suppressing only when there is a *blocking* unread item (hard TODO, invariant violation, hard escalation). Treat non-blocking unread messages as a softer reminder. The notebook flags this as a judgment call worth thinking about.\n\n**Failure mode to design against:** the agent might learn that REPLY-FIRST CYCLE clears as soon as tracker discuss posts anything. The companion item ('--ack-thread enforcement') prevents this; without it, this change might just produce shorter drive-by replies.\n\n**Implementation cost:** ~50-80 lines plus tests asserting the autonomous-drive section is omitted when blocking unread exists. Behavior change is concentrated in one function.\n\n**Validation plan** (per notebook): run for a week and measure (a) reply length per by:human-prompted discuss, (b) tool calls per reply turn (Read/Grep/Explore/WebSearch), (c) replies citing specific code locations / ADRs / PRs, (d) whether the agent ever uses WebSearch for a tracker reply (currently 0 across ~6500 records).\n\nNOT a governance file — packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py is application code."  # 85d3
    fields: {}  # 85d3
- op: update  # 9529
  at: '2026-04-10T18:20:54Z'  # 9529
  by: agent  # 9529
  actor: jgstern_agent  # 9529
  clock: 2  # 9529
  nonce: '9529'  # 9529
  set:  # 9529
    status: needs_human_review  # 9529
- op: discuss  # eaf2
  at: '2026-04-10T18:20:54Z'  # eaf2
  by: agent  # eaf2
  actor: jgstern_agent  # eaf2
  clock: 3  # eaf2
  nonce: eaf2  # eaf2
  message: "Requires changes to .agent/hooks/ (governance files). WI-zufuj (--ack-thread gate) merged in PR #2896 — this is the companion item that suppresses the drive prompt while reply debt exists. Awaiting human approval to modify governance files."  # eaf2
- op: update  # 73b6
  at: '2026-04-14T04:51:41Z'  # 73b6
  by: agent  # 73b6
  actor: jgstern_agent  # 73b6
  clock: 4  # 73b6
  nonce: 73b6  # 73b6
  set:  # 73b6
    priority: 2  # 73b6
- op: discuss  # e5ea
  at: '2026-04-14T04:51:41Z'  # e5ea
  by: agent  # e5ea
  actor: jgstern_agent  # e5ea
  clock: 5  # e5ea
  nonce: e5ea  # e5ea
  message: "Promote P3→P2 to reflect the user's framing in notebookjournal_04072026_1518.md: 'the clearest remedy because it is the only proposal that changes the agent's incentive structure rather than adding tools/text.' Higher priority than the sibling stop-hook items (WI-sudoj, WI-tubuk, WI-fusak)."  # e5ea
- op: update  # 98aa
  at: '2026-04-14T19:25:25Z'  # 98aa
  by: agent  # 98aa
  actor: jgstern_agent  # 98aa
  clock: 6  # 98aa
  nonce: 98aa  # 98aa
  set:  # 98aa
    description: "Per ~/hypergumbo_lab_notebook/notebookjournal_04072026_1518.md 'Elaboration on item 6' — the user explicitly identified this as the clearest remedy because it is the only proposal that changes the agent's incentive structure rather than adding tools/knowledge.\n\n**Change:** in packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py:generate_guidance, branch when items_with_unread_blocking + unread_non_blocking > 0 and emit a *different* guidance file rather than the standard TODO/bakeoff one with an unread-message section appended. The new guidance:\n\n> REPLY-FIRST CYCLE — N unread human message(s)\n>\n> Take a breather from the \"forward-march vibe\" of autonomous mode. Do not start new code tasks, do not run bakeoffs, do not pick from tracker ready. Your only assignment is to clear reply debt. For each item:\n> 1. tracker show <ID> — load the full thread + description\n> 2. Classify: approval, directive, or question\n> 3. Questions: plan the investigation (Read/Grep, Explore subagent, WebSearch on allowlisted domains) BEFORE drafting\n> 4. tracker discuss <ID> — post the reply\n\n**Critical:** while reply debt exists, the hook does NOT emit 'AUTONOMOUS MODE: N TODOs block stopping'. The TODO list isn't shown at all. This is what gives the change teeth — the agent can't make progress on anything else without first clearing the reply.\n\n**Scope of suppression:** default to suppressing only when there is a *blocking* unread item (hard TODO, invariant violation, hard escalation). Treat non-blocking unread messages as a softer reminder. The notebook flags this as a judgment call worth thinking about.\n\n**Failure mode to design against:** the agent might learn that REPLY-FIRST CYCLE clears as soon as tracker discuss posts anything. The companion item ('--ack-thread enforcement') prevents this; without it, this change might just produce shorter drive-by replies.\n\n**Implementation cost:** ~50-80 lines plus tests asserting the autonomous-drive section is omitted when blocking unread exists. Behavior change is concentrated in one function.\n\n**Validation plan** (per notebook): run for a week and measure (a) reply length per by:human-prompted discuss, (b) tool calls per reply turn (Read/Grep/Explore/WebSearch), (c) replies citing specific code locations / ADRs / PRs, (d) whether the agent ever uses WebSearch for a tracker reply (currently 0 across ~6500 records).\n\nNOT a governance file — packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py is application code."  # 98aa
- op: update  # d4c2
  at: '2026-04-14T19:54:33Z'  # d4c2
  by: agent  # d4c2
  actor: jgstern_agent  # d4c2
  clock: 7  # d4c2
  nonce: d4c2  # d4c2
  set:  # d4c2
    description: "Per ~/hypergumbo_lab_notebook/notebookjournal_04072026_1518.md 'Elaboration on item 6' — the user explicitly identified this as the clearest remedy because it is the only proposal that changes the agent's incentive structure rather than adding tools/knowledge.\n\n**Change:** in packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py:generate_guidance, branch when items_with_unread_blocking + unread_non_blocking > 0 and emit a *different* guidance file rather than the standard TODO/bakeoff one with an unread-message section appended. The new guidance:\n\n> REPLY-FIRST CYCLE — N unread human message(s)\n>\n> Take a breather from the \"forward-march vibe\" of autonomous mode. Do not start new code tasks, do not run bakeoffs, do not pick from tracker ready. Your only assignment is to clear reply debt. For each item:\n> 1. tracker show <ID> — load the full thread + description\n> 2. Classify: approval, directive, or question\n> 3. Questions: plan the investigation (Read/Grep, Explore subagent, WebSearch on allowlisted domains) BEFORE drafting\n> 4. tracker discuss <ID> — post the reply\n\n**Critical:** while reply debt exists, the hook does NOT emit 'AUTONOMOUS MODE: N TODOs block stopping'. The TODO list isn't shown at all. This is what gives the change teeth — the agent can't make progress on anything else without first clearing the reply.\n\n**Scope of suppression:** Default to treating all unread items as equally important, with the exception of unread items whose first characters match \\[\\[.+\\]\\] -- for example, if the user prefaces their remarks with [[IMPORTANT]] or [[do-right-fucking-now]] or [[Mclovin]]. This special treatment should not apply if the double-bracketed text contains spaces or if the double-bracketed text is anywhere other than the absolute first characters of the unread item. To avoid overcomplicating the logic of message triage, the presence of a [[]] annotation should only ever trigger a single distinct codepath. In other words, if the user annotation is ambiguous (e.g. \"[[Mclovin]]\"), it will still activate the alternate path, but that path is always going to encounter an LLM-mediated gate to the effect of \"There are a total of 3 unread messages from the human. Does the human-provided preface tag '[[Mclovin]]' imply that this message takes precedence over the other unreads?\"\n\n**Failure mode to design against:** the agent might learn that REPLY-FIRST CYCLE clears as soon as tracker discuss posts anything. The companion item ('--ack-thread enforcement') prevents this; without it, this change might just produce shorter drive-by replies.\n\n**Implementation cost:** ~50-80 lines plus tests asserting the autonomous-drive section is omitted when blocking unread exists. Behavior change is concentrated in one function.\n\n**Validation plan** (per notebook): run for a week and measure (a) reply length per by:human-prompted discuss, (b) tool calls per reply turn (Read/Grep/Explore/WebSearch), (c) replies citing specific code locations / ADRs / PRs, (d) whether the agent ever uses WebSearch for a tracker reply (currently 0 across ~6500 records).\n\nNOT a governance file — packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py is application code."  # d4c2
- op: update  # 40cf
  at: '2026-04-14T20:01:59Z'  # 40cf
  by: agent  # 40cf
  actor: jgstern_agent  # 40cf
  clock: 8  # 40cf
  nonce: 40cf  # 40cf
  set:  # 40cf
    description: "Per ~/hypergumbo_lab_notebook/notebookjournal_04072026_1518.md 'Elaboration on item 6' — the user explicitly identified this as the clearest remedy because it is the only proposal that changes the agent's incentive structure rather than adding tools/knowledge.\n\n**Change:** in packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py:generate_guidance, branch when items_with_unread_blocking + unread_non_blocking > 0 and emit a *different* guidance file rather than the standard TODO/bakeoff one with an unread-message section appended. The new guidance:\n\n> REPLY-FIRST CYCLE — N unread human message(s)\n>\n> Take a breather from the \"forward-march vibe\" of autonomous mode. Do not start new code tasks, do not run bakeoffs, do not pick from tracker ready. Your only assignment is to clear reply debt. For each item:\n> 1. tracker show <ID> — load the full thread + description\n> 2. Classify: approval, directive, or question\n> 3. Questions: plan the investigation (Read/Grep, Explore subagent, WebSearch on allowlisted domains) BEFORE drafting\n> 4. tracker discuss <ID> — post the reply\n\n**Critical:** while reply debt exists, the hook does NOT emit 'AUTONOMOUS MODE: N TODOs block stopping'. The TODO list isn't shown at all. This is what gives the change teeth — the agent can't make progress on anything else without first clearing the reply.\n\n**Scope of suppression:** Default to treating all unread items as equally important, with the exception of unread items whose first characters match \\[\\[.+\\]\\] -- for example, if the user prefaces their remarks with [[IMPORTANT]] or [[do-right-fucking-now]] or [[Mclovin]]. This special treatment should not apply if the double-bracketed text contains spaces or if the double-bracketed text is anywhere other than the absolute first characters of the unread item. To avoid overcomplicating the logic of message triage, the presence of a [[]] annotation should only ever trigger a single distinct codepath. In other words, if the user annotation is ambiguous (e.g. \"[[Mclovin]]\"), it will still activate the alternate path, but that path is always going to encounter an LLM-mediated gate to the effect of \"There are a total of 3 unread messages from the human. Does the human-provided preface tag '[[Mclovin]]' imply that this message takes precedence over the other unreads?\"\n\n**Failure mode to design against:** the agent might learn that REPLY-FIRST CYCLE clears as soon as tracker discuss posts anything. The companion item ('--ack-thread enforcement') prevents this; without it, this change might just produce shorter drive-by replies.\n\n**Implementation cost:** a single-function behavior change in generate_guidance plus a preface detector and an LLM-gate call. Roughly:\n- ~40-60 lines for the default REPLY-FIRST CYCLE branch (swallow the AUTONOMOUS MODE TODO section, emit the \"forward-march vibe\" reminder, list unreads).\n- ~30-50 lines for the \\[\\[...\\]\\] preface detector (anchored at character 0, no internal whitespace, single codepath regardless of whether the tag is recognisable English — the gate decides).\n- ~40-60 lines to invoke the LLM-mediated precedence gate and thread its answer back into which unread the agent is steered toward first.\n- Tests: (a) the AUTONOMOUS MODE TODO section is omitted whenever any unread human message is present; (b) preface detection accepts `[[IMPORTANT]]`, `[[do-right-fucking-now]]`, `[[Mclovin]]` at position 0 and rejects `[[has space]]`, `leading text [[IMPORTANT]]`, and `[[ space before close]]`; (c) an ambiguous preface still enters the alternate path and the LLM gate receives a well-formed prompt naming every other unread; (d) when the gate declines to elevate, guidance falls back to the default equal-priority listing.\n\n**Validation plan** (per notebook): run for a week and measure (a) reply length per by:human-prompted discuss, (b) tool calls per reply turn (Read/Grep/Explore/WebSearch), (c) replies citing specific code locations / ADRs / PRs, (d) whether the agent ever uses WebSearch for a tracker reply (currently 0 across ~6500 records), (e) frequency of \\[\\[...\\]\\] prefaces in human discuss entries and how often the LLM gate elevates them, (f) whether an elevated item actually gets answered before the other unreads in the same session (the behavioural point of the alternate path).\n\nNOT a governance file — packages/hypergumbo-tracker/src/hypergumbo_tracker/stop_hook.py is application code."  # 40cf
- op: update  # e298
  at: '2026-04-14T20:03:26Z'  # e298
  by: human  # e298
  actor: jgstern  # e298
  clock: 9  # e298
  nonce: e298  # e298
  set:  # e298
    status: todo_soft  # e298
- op: discuss  # e500
  at: '2026-04-17T07:24:39Z'  # e500
  by: agent  # e500
  actor: jgstern_agent  # e500
  clock: 10  # e500
  nonce: e500  # e500
  message: "Phase 1 merged: PR #3110 → commit 98ecb22ca on dev. generate_guidance now branches to a REPLY-FIRST CYCLE document when any unread human messages exist (blocking or non-blocking): TODO/violated/soft sections suppressed, replaced with a 4-step protocol + per-item unread list. Companion change in .agent/hooks/*/stop.sh (claude-code, cursor, codex-cli, gemini-cli) reads a new UNREAD_COUNT export from stop_logic.sh and swaps the one-line reason text to 'REPLY-FIRST CYCLE: N unread human message(s)…' hiding the TODO count entirely while reply debt exists.\n\nTests (all passing, 100% coverage in package isolation): branch-firing on blocking/non-blocking/both, suppression of Hard/Invariant/Soft sections, priority-sorted listing, and the default path when no unreads exist.\n\nPhase 2 ([[...]] preface detector + LLM-mediated precedence gate) filed as WI-mofaz-dinip-falil-hugus-fupoj-kinij-jusiz-lakop.\n\nLeaving THIS item open until Phase 2 lands — the six validation metrics from the item description still apply to Phase 1 alone (reply length, tool calls per reply, code/ADR/PR citations, WebSearch usage).\n\nNB: I posted an earlier version of this update before the PR merged. The auto-pr rebase/restore cycle (backed up .ops to /tmp/ops-backup- then restored after rebase) silently overwrote that earlier discuss entry. Data-loss bug in auto-pr's .ops-restore logic — filing a tracker item."  # e500
- op: update  # c1d5
  at: '2026-04-17T08:45:54Z'  # c1d5
  by: agent  # c1d5
  actor: jgstern_agent  # c1d5
  clock: 11  # c1d5
  nonce: c1d5  # c1d5
  set:  # c1d5
    status: done  # c1d5
    pr_ref: '3110'  # c1d5
- op: discuss  # 4e1a
  at: '2026-04-17T08:45:54Z'  # 4e1a
  by: agent  # 4e1a
  actor: jgstern_agent  # 4e1a
  clock: 12  # 4e1a
  nonce: 4e1a  # 4e1a
  message: "Closing as done: Phase 1 shipped in PR #3110 (commit 98ecb22ca on dev). That covers the core behavioural change described in this item — REPLY-FIRST CYCLE guidance document + hook reason-text swap, hiding TODO counts while reply debt exists. Phase 2 ([[...]] preface + LLM-mediated precedence gate) is a distinct refinement and is tracked separately as WI-mofaz-dinip-falil-hugus-fupoj-kinij-jusiz-lakop. Splitting is cleaner than keeping this open indefinitely — the item's validation plan (reply length / tool calls per reply / WebSearch usage) applies to the Phase 1 shape alone and can be measured independently of Phase 2."  # 4e1a

- op: create  # a305
  at: '2026-04-23T08:30:52Z'  # a305
  by: agent  # a305
  actor: jgstern_agent  # a305
  clock: 1  # a305
  nonce: a305  # a305
  data:  # a305
    kind: work_item  # a305
    title: "Level 2: NameMatcher utility + matcher-site retrofit + lint rule + polyglot coverage fixture"  # a305
    status: todo_hard  # a305
    priority: 2  # a305
    parent:  # a305
    tags:  # a305
    - analysis_quality  # a305
    isbefore: []  # a305
    duplicate_of: []  # a305
    not_duplicate_of: []  # a305
    pr_ref:  # a305
    description: "Containment strategy for the 'name-form normalization at matcher boundaries' bug class (WI-zigah parent class). See lab notebook notebookjournal_04232026_0343.md §'Three-Level Response'.\n\nScope:\n1. Build a shared NameMatcher utility that takes a canonical-form spec (language, optional module_path, name) and matches against any valid source-form spelling for that language. Handles the dotted-submodule / bare / aliased / namespace-as import variants.\n2. Retrofit confirmed-vulnerable matcher sites to use NameMatcher:\n   - packages/hypergumbo-core/src/hypergumbo_core/framework_patterns.py:287-310 (decorator / base_class / annotation matching — CONFIRMED vulnerable on 2026-04-23 audit)\n   - packages/hypergumbo-core/src/hypergumbo_core/linkers/di_resolution.py:641 (interface-name lookup — probable)\n   - Verify packages/hypergumbo-core/src/hypergumbo_core/io_boundary.py lookup_with_module (line 833) — may already canonicalize; audit needed.\n3. Add a lint/test rule that flags raw re.match / string comparison against symbol-meta strings (decorators, base_classes, annotations) in matcher code paths, with a whitelist for legitimate uses.\n4. Polyglot coverage fixture: one fixture file per language (start with Python, JS/TS, Go, Rust, Java) enumerating the top ~5 import×call constructs. Property test asserts the count of syntactic call sites in the fixture matches the count of outgoing call edges from the analyzer.\n\nSibling: Level 1 (Python analyzer-layer) = WI-zigah-fanob-jitik-lanit-kurar-hujij-pugun-padan.\nParent of (structural-level goal): Level 3 canonical-form IR refactor (separate item, P4 needs_human_review).\n\nSuccess criteria: after retrofit, the framework_patterns.py audit test shows identical enrichment outputs for class Foo(BaseModel) imported three ways (from pydantic import BaseModel / import pydantic / from pydantic.main import BaseModel). After fixture, adding a new import construct to the fixture auto-fails CI if the analyzer misses it."  # a305
    fields: {}  # a305
- op: discuss  # 231b
  at: '2026-04-23T08:33:02Z'  # 231b
  by: agent  # 231b
  actor: jgstern_agent  # 231b
  clock: 2  # 231b
  nonce: 231b  # 231b
  message: "Scope revision (2026-04-23): Level 1's localized framework_patterns.py bug fix belongs to WI-zigah, not here. Level 2's role on framework_patterns.py is REFACTOR-TO-SHARED-UTILITY, not bug fix. Revised scope:\n1. Build NameMatcher utility (canonical-form tuples, source-spelling variants).\n2. REFACTOR framework_patterns.py to call NameMatcher instead of its local normalization from WI-zigah.\n3. Retrofit di_resolution.py (line 641 interface-name lookup).\n4. Audit io_boundary.py lookup_with_module (line 833) — may already canonicalize; if so, document and skip.\n5. Lint/test rule flagging raw regex/string matching against symbol-meta in matcher code paths.\n6. Polyglot coverage fixture (one file per language × top import/call constructs) + property test."  # 231b
- op: update  # 2242
  at: '2026-04-23T09:43:43Z'  # 2242
  by: agent  # 2242
  actor: jgstern_agent  # 2242
  clock: 3  # 2242
  nonce: '2242'  # 2242
  set:  # 2242
    status: todo_soft  # 2242
- op: discuss  # 5b45
  at: '2026-04-23T09:43:43Z'  # 5b45
  by: agent  # 5b45
  actor: jgstern_agent  # 5b45
  clock: 4  # 5b45
  nonce: 5b45  # 5b45
  message: "Phase 1 shipped in PR #3313 (merged 2026-04-23): NameMatcher utility + framework_patterns.py base_class/parent_base_class retrofit. Remaining Level 2 scope downgraded to todo_soft and moved to follow-up: decorator/annotation retrofit via NameMatcher.match(), di_resolution.py interface-name lookup retrofit, io_boundary.py lookup_with_module audit, polyglot coverage fixture, lint rule flagging raw re.match on symbol-meta strings. The first-party payoff site (base_class matching) is live; remaining sites are hardening."  # 5b45

- op: create  # a46e
  at: '2026-05-28T22:13:51Z'  # a46e
  by: agent  # a46e
  actor: jgstern_agent  # a46e
  clock: 1  # a46e
  nonce: a46e  # a46e
  data:  # a46e
    kind: invariant  # a46e
    title: "PPPPP: yaml analyzer emits 0 nodes for 174 files / 17027 LOC; only yaml_ansible runs (1 file); generic yaml content is invisible to behavior map"  # a46e
    status: violated  # a46e
    priority: 3  # a46e
    parent:  # a46e
    tags:  # a46e
    - dogfooding  # a46e
    - pass16  # a46e
    - yaml  # a46e
    - analyzer_coverage  # a46e
    isbefore: []  # a46e
    duplicate_of: []  # a46e
    not_duplicate_of: []  # a46e
    pr_ref:  # a46e
    description: ""  # a46e
    fields:  # a46e
      statement: "yaml analyzer produces 0 path-bearing nodes despite 174 yaml files / 17027 LOC (per profile.languages.yaml). No 'yaml' pass appears in analysis_runs at all — only 'yaml_ansible' exists (1 file, 6ms, 1 node for .yamllint.yaml). 173 of 174 yaml files are effectively unanalyzed: zero file/section/key nodes in the behavior map"  # a46e
      root_cause: "Either (a) the yaml analyzer is registered but doesn't emit any IR (broken extraction), or (b) there's no general yaml analyzer at all (only yaml_ansible covering ansible-specific YAML). profile.languages.yaml counts the files but no pass extracts symbols from them. yaml files include docs/hypergumbo-self-catalog/*.yaml (the highest-centrality entries via UUUU) and packages/*/frameworks/*.yaml — content is invisible to all downstream analysis"  # a46e

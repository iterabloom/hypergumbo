- op: create  # b4d4
  at: '2026-08-03T12:08:55Z'  # b4d4
  by: agent  # b4d4
  actor: jgstern_agent  # b4d4
  clock: 1  # b4d4
  nonce: b4d4  # b4d4
  data:  # b4d4
    kind: work_item  # b4d4
    title: "TypeScript DDG spec covers only function_declaration; class methods excluded to avoid duplicating the analyzer kind-slot decision"  # b4d4
    status: todo_hard  # b4d4
    priority: 3  # b4d4
    parent:  # b4d4
    tags:  # b4d4
    - adr_0017  # b4d4
    - taint  # b4d4
    - analysis_quality  # b4d4
    isbefore: []  # b4d4
    duplicate_of: []  # b4d4
    not_duplicate_of: []  # b4d4
    pr_ref:  # b4d4
    description: "DISCLOSED GAP from PR #186 (WI-tohuk), filed because an undocumented\n\"tracked separately\" is a fresh L50 instance the day it ships.\n\nThe TypeScript LanguageDdgSpec registers function_node_types =\n{function_declaration}. method_definition is deliberately excluded: js_ts.py\ndecides that node kind slot INLINE -- method, or getter/setter when a get/set\nchild is present -- and re-deriving that classification inside the DDG spec\nwould put a second copy of a production judgement in a consumer. That is the\nshape the L53 sharpening names as the bug itself, and it has produced wrong\nnumbers in this subsystem before. A DDG symbol id that disagrees with the\nanalyzer id for the same function attributes coverage to a symbol nothing else\nrefers to, which reads downstream as \"this function has no DDG data\".\n\nFIX SHAPE: extract the kind-slot decision from js_ts.py into a shared helper\nand have the spec call it via kind_for, the way go_def_use imports\n_extract_receiver_type_from_node from go.py. Then widen function_node_types.\n\nPRIORITY IS LOW FOR A REASON: TypeScript has 0 catalogued taint sinks, so no\namount of TypeScript DDG coverage can move a taint verdict today. This\nbecomes worth doing when TS sinks land, or immediately if the javascript\nwiring item reuses the same spec."  # b4d4
    fields: {}  # b4d4
- op: discuss  # 3f8f
  at: '2026-09-21T10:07:41Z'  # 3f8f
  by: agent  # 3f8f
  actor: jgstern_agent  # 3f8f
  clock: 2  # 3f8f
  nonce: 3f8f  # 3f8f
  message: "THE SAME DEFECT EXISTS IN PYTHON, AND THERE IT IS LOAD-BEARING. Filed 2026-09-21 as WI-ripas.\n\nThis row prices itself P3 on a stated reason: \"TypeScript has 0 catalogued taint sinks, so no amount of TypeScript DDG coverage can move a taint verdict today.\" That reasoning is sound for TS and inverts exactly for python, which carries the richest sink catalogue in the tree -- and python's LanguageDdgSpec supplies neither `name_for` nor `kind_for` either.\n\nMEASURED on hypergumbo itself: 0 of 24,915 python methods match a ddg_symbols key, while 21,257 of them (85.3%) HAVE their DDG data sitting in that set under the bare-name/`:function` key nothing asks for. 68.5% of python callables can therefore never be walked, and every taint finding rooted in one is `structural` by construction.\n\nNothing about THIS row changes -- the TS gap is still real and still cheap to leave -- but the fix shape this row already wrote down (\"have the spec call it via kind_for, the way go_def_use imports _extract_receiver_type_from_node from go.py\") is the one WI-ripas will implement for python. If the shared kind-slot helper this row asks for gets extracted, both languages should take it."  # 3f8f
- op: update  # 693c
  at: '2026-09-23T00:09:47Z'  # 693c
  by: agent  # 693c
  actor: jgstern_agent  # 693c
  clock: 3  # 693c
  nonce: 693c  # 693c
  set:  # 693c
    status: done  # 693c
- op: discuss  # 6fc0
  at: '2026-09-23T00:09:47Z'  # 6fc0
  by: agent  # 6fc0
  actor: jgstern_agent  # 6fc0
  clock: 4  # 6fc0
  nonce: 6fc0  # 6fc0
  message: "DONE by the same change as WI-sakir (2a92c2a650), using this row's own prescribed fix shape: extract the analyzer's kind-slot decision into a shared helper (jsts_method_kind, plus jsts_method_name), have the TypeScript spec call it via kind_for / name_for, then widen function_node_types to method_definition. apollo-server (TS), baseline dev in a worktree: methods 0 -> 314 of 355 walked under the analyzer's key. The 41 left are 39 bodiless interface method signatures (nothing to walk) and 2 methods in .mts/.cts files the *.ts glob does not claim, the TypeScript twin of WI-fovus. The row's P3 price still holds for verdicts: TypeScript carries 0 taint sinks."  # 6fc0

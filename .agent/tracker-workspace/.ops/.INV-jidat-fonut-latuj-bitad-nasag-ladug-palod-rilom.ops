- op: create  # 5a68
  at: '2026-05-20T00:06:54Z'  # 5a68
  by: agent  # 5a68
  actor: jgstern_agent  # 5a68
  clock: 1  # 5a68
  nonce: 5a68  # 5a68
  data:  # 5a68
    kind: invariant  # 5a68
    title: "Symbol.origin / Edge.origin should be list[str] — multi-source attribution; current scalar erases multi-pass contributions"  # 5a68
    status: violated  # 5a68
    priority: 3  # 5a68
    parent: INV-dubam-rusok-nizot-zibor-dotuh-gasip-hahin-gujug  # 5a68
    tags:  # 5a68
    - schema  # 5a68
    - analysis_quality  # 5a68
    - explainability  # 5a68
    - breaking_change  # 5a68
    isbefore: []  # 5a68
    duplicate_of: []  # 5a68
    not_duplicate_of: []  # 5a68
    pr_ref:  # 5a68
    description: "## Empirical observation\n\n`Symbol.origin` and `Edge.origin` are declared as `str` in `ir.py` and `docs/schema.json`. When evidence for an Edge is contributed by multiple passes — e.g., the JS/TS analyzer detects a call-site, the http-linker resolves the call's target, the route-handler-linker tags the target as a route — only one pass receives credit in `origin`. The current convention is \"the pass that called Edge.create() wins\", which biases credit toward late-stage linkers and erases contributions from earlier evidence sources.\n\n## What's needed\n\nChange `Symbol.origin` and `Edge.origin` from `str` to `list[str]`. Order matters: list reads chronologically — the first contributor first, then each subsequent pass that refined or confirmed.\n\nSchema:\n\n```json\n\"origin\": {\n  \"type\": \"array\",\n  \"items\": { \"type\": \"string\" },\n  \"minItems\": 1,\n  \"description\": \"Ordered list of pass IDs that contributed to this Edge/Symbol's existence. First entry is the originating pass; subsequent entries are passes that refined or extended.\"\n}\n```\n\n## Migration\n\nThis is a schema-breaking change. Behavior map JSON files predating this fix carry a scalar `origin`. Migration steps:\n\n1. Spec version bump: 0.8.x → 0.9.0 with a deprecation note.\n2. `Edge.from_dict()` and `Symbol.from_dict()` handle both forms (scalar and list) for backward compatibility during the migration window.\n3. Producer code switches to emitting lists. Single-element lists are valid for passes that don't share construction with others.\n4. After the migration window, `from_dict()` stops accepting scalars; behavior maps from old hypergumbo versions need to be regenerated.\n\n## Scope\n\n- Schema bump\n- IR: `Symbol`, `Edge` dataclasses\n- Producers: ~67 Edge construction sites + ~130 Symbol construction sites\n- Consumers: anywhere `edge.origin` or `symbol.origin` is read as a string (the audit task is finding these)\n- Tests: ~40 test files with `assert sym.origin == \"...\"` style assertions\n\n## Tests\n\n- Schema validation: round-trip a behavior map with multi-contributor origins through `from_dict() -> to_dict()`.\n- Property test: every Edge's `origin` list is non-empty.\n- Migration test: load an old-format behavior map (scalar origin) and assert it deserializes correctly.\n\n## Related\n\nParent META: INV-dubam. Sibling items: derivation graph (Edge.derived_from), pass dependency declaration (Pass.requires)."  # 5a68
    fields:  # 5a68
      statement: "When evidence for a Symbol or Edge is contributed by multiple passes, all contributing passes should be credited in origin. Currently origin is scalar — only the pass that called Edge.create()/Symbol(...) gets credit."  # 5a68
      root_cause: "The current model treats origin as a single-author field. When the analyzer detects a call-site and a linker resolves the target, only the linker is credited. Multi-pass evidence combination forces a lossy attribution choice."  # 5a68
- op: update  # a5df
  at: '2026-05-27T06:55:45Z'  # a5df
  by: agent  # a5df
  actor: jgstern_agent  # a5df
  clock: 2  # a5df
  nonce: a5df  # a5df
  set:  # a5df
    status: pending_validation  # a5df
  add:  # a5df
    tags: [awaits_bakeoff_validation]  # a5df
- op: discuss  # 54b8
  at: '2026-05-27T06:55:45Z'  # 54b8
  by: agent  # 54b8
  actor: jgstern_agent  # 54b8
  clock: 3  # 54b8
  nonce: 54b8  # 54b8
  message: "Fixed in PR #3927 (merged). Symbol.origin and Edge.origin changed from str to list[str]. Schema 0.10.0. Auto-normalization preserves backward compat. Needs BROAD bakeoff to validate multi-element origins appear correctly."  # 54b8
- op: update  # bee9
  at: '2026-05-30T09:36:52Z'  # bee9
  by: agent  # bee9
  actor: jgstern_agent  # bee9
  clock: 4  # bee9
  nonce: bee9  # bee9
  set:  # bee9
    status: pending_validation  # bee9
- op: discuss  # eb43
  at: '2026-05-30T09:36:52Z'  # eb43
  by: agent  # eb43
  actor: jgstern_agent  # eb43
  clock: 5  # eb43
  nonce: eb43  # eb43
  message: "Cross-ref backfill (2026-05-30, linked-pile L4 investigation): item LANDED. bm.json (/tmp/hg-dogfood-s11/bm.json) confirms 108649/108649 edges carry list-typed origin (Symbol.origin / Edge.origin migrated to list[str]). Moving to pending_validation. Awaits bakeoff confirmation that no consumer-side regression; once that lands, eligible for satisfied."  # eb43
- op: update  # 852e
  at: '2026-06-06T10:35:36Z'  # 852e
  by: agent  # 852e
  actor: jgstern_agent  # 852e
  clock: 6  # 852e
  nonce: 852e  # 852e
  set:  # 852e
    status: violated  # 852e
- op: discuss  # f665
  at: '2026-06-06T10:35:36Z'  # f665
  by: agent  # f665
  actor: jgstern_agent  # f665
  clock: 7  # f665
  nonce: f665  # f665
  message: "validation failed: the list[str] migration shipped but multi-pass attribution is still vacuous — 0 of 34396 nodes and 0 of 110533 edges carry a multi-element origin list (100% singletons). Every origin is single-pass, so the list type buys nothing over a scalar; no producer ever appends a second pass id. (Blind-severity-derived priority signal: P3.)"  # f665
- op: update  # c18b
  at: '2026-06-22T02:52:56Z'  # c18b
  by: agent  # c18b
  actor: jgstern_agent  # c18b
  clock: 8  # c18b
  nonce: c18b  # c18b
  set: {}  # c18b
  remove:  # c18b
    tags: [awaits_bakeoff_validation]  # c18b
- op: discuss  # d966
  at: '2026-06-22T02:52:56Z'  # d966
  by: agent  # d966
  actor: jgstern_agent  # d966
  clock: 9  # d966
  nonce: d966  # d966
  message: "Drain audit (deep-20260621-215349), human-authorized strip. no_move/violated: 2026-06-06 bakeoff found 0/110533 edges carry multi-element origin (100% singletons) after the PR #3927 schema change — the producer-side goal (passes appending multiple pass IDs) never materialized. Deterministic structural gap, NOT cohort-re-runnable. Stripping awaits_bakeoff_validation; violated status remains the live signal. Re-tag only on a future fix with a fresh quantitative claim."  # d966
- op: update  # bfe5
  at: '2026-07-21T21:20:44Z'  # bfe5
  by: agent  # bfe5
  actor: jgstern_agent  # bfe5
  clock: 10  # bfe5
  nonce: bfe5  # bfe5
  set:  # bfe5
    title: "Symbol/Edge.origin multi-source ATTRIBUTION gap — every producer emits single [PASS_ID] (0/110533 multi-element); the list[str] TYPE goal is DONE"  # bfe5
    fields:  # bfe5
      statement: "SCOPE (2026-07-21 audit): the TYPE sub-goal is DONE and the 'current scalar' framing is STALE -- origin is now 'str | List[str]' (ir.py:569/813), __post_init__ normalizes scalar->list, to_dict serializes a list, and a bakeoff confirmed 108649/108649 edges carry list-typed origin (since PR #3927). The LIVE invariant is the multi-source ATTRIBUTION gap: when evidence for a Symbol/Edge is contributed by multiple passes, all contributing passes should be credited in origin, but every producer still emits a single-element [PASS_ID] and no merge/finalize step appends a second (edge-dedup unions referring_paths, not origins; 0/110533 edges multi-element). Re-scoped to the producer-side attribution gap."  # bfe5
- op: discuss  # 405b
  at: '2026-07-21T21:20:44Z'  # 405b
  by: agent  # 405b
  actor: jgstern_agent  # 405b
  clock: 11  # 405b
  nonce: 405b  # 405b
  message: "Re-scoped statement/title off the stale 'should be list[str] / current scalar' framing: the type widening landed long ago (origin is str|List[str], ir.py:569/813; 108649/108649 list-typed). The substantive invariant -- multi-source attribution -- is genuinely unmet (single-element [PASS_ID] everywhere; 0/110533 multi-element). Root_cause ('single-author field / lossy attribution choice') is still accurate, left as-is."  # 405b

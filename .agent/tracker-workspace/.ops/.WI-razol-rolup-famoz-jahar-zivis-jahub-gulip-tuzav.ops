- op: create  # 8abe
  at: '2026-06-05T04:01:51Z'  # 8abe
  by: agent  # 8abe
  actor: jgstern_agent  # 8abe
  clock: 1  # 8abe
  nonce: 8abe  # 8abe
  data:  # 8abe
    kind: work_item  # 8abe
    title: "verify-claims INV-maluk FP cascade: 5541 violations on self-claims; taint.py ignores ambiguous_names"  # 8abe
    status: todo_hard  # 8abe
    priority: 0  # 8abe
    parent: INV-somut-lapar-furob-tovul-juhor-puhat-zujim-lobir  # 8abe
    tags:  # 8abe
    - dogfood_tranch_03  # 8abe
    - dogfood  # 8abe
    isbefore: []  # 8abe
    duplicate_of: []  # 8abe
    not_duplicate_of: []  # 8abe
    pr_ref:  # 8abe
    description: "**SEVERE - tranch 03's single Severe card.**\n\n**Mechanic**: The project's primitive catalog lists `replace`, `write`, `run` as ambiguous names — short names that collide with `str.replace`, `dict.replace`, `pandas.DataFrame.replace`, etc. The catalog's `classify_with_module_hint` returns None for ambiguous names without a module hint. The taint-sink matcher, however, indexes sinks purely by `callee_name` and blindly returns the first sink when no module hint is available. When an edge has `dst=\"external:0-0:replace:unresolved\"` (no module hint — the common case for unresolved Python method calls), the matcher returns the first registered \"replace\" sink (the filesystem-write `Path.replace`), so every `str.replace` in the call graph becomes a host-filesystem-write evidence row. Two consumers of the same YAML diverge on the `ambiguous_names` semantics.\n\n**Expectation**: Both consumers of the primitive catalog honor `ambiguous_names`; in particular, the taint-sink matcher returns None (not the first sink) when the callee name is in `ambiguous_names` and no disambiguating module hint is present. Reported violations distinguish true positives from ambiguous-name false-positive cascades.\n\n**Scope**: 5,541 false-positive flow violations across 4 claims on the project's own canonical claims-doc. The same defect affects any consumer running verify-claims against any codebase whose call graph includes Python `replace`/`write`/`run` calls on non-filesystem types.\n\n**Exposure**: Any consumer following the spec's documented contract for the 5-verdict vocabulary and trusting \"violated\" as authoritative.\n\n**Consequence**: A consumer concludes the runtime CLI subcommands violate a \"do not write to arbitrary filesystem paths\" security property when they do not. The user-facing exit code (1) and message (\"4 VIOLATED\") give no signal to distinguish a true violation from an ambiguous-name cascade. This is the precise decision-corruption Tier 5 defines — a security property the contract claims to verify is silently misreported.\n\n**Source**: F46.A1 (INDEPENDENT, HIGH). Pass 46 Severe candidate confirmed.\n\nFiled via tranch 03 dogfood (passes 41-60, substrate sha256 a32c4a31..., HEAD 9a18959071). Tranch state: `~/hypergumbo_lab_notebook/dogfood_tranch_03/tranch_state.json`."  # 8abe
    fields: {}  # 8abe
- op: discuss  # 95bf
  at: '2026-06-05T06:01:46Z'  # 95bf
  by: agent  # 95bf
  actor: jgstern_agent  # 95bf
  clock: 2  # 95bf
  nonce: 95bf  # 95bf
  message: "[related to INV-maluk (satisfied for io-boundaries chain-matcher) and WI-fabup (call-graph producer consumer) — this row covers the third consumer (taint.py sink-matcher) of the same ambiguous_names YAML]"  # 95bf
- op: discuss  # ebf3
  at: '2026-06-07T06:42:51Z'  # ebf3
  by: agent  # ebf3
  actor: jgstern_agent  # ebf3
  clock: 3  # ebf3
  nonce: ebf3  # ebf3
  message: "Dogfood enrichment (member F156.A1) — JUSTIFIED-BUNDLE, one fix; reproduces on the frozen substrate. taint.py match_sink DROPS the module qualifier the io_primitives catalog provides, so console writes (sys.stdout/sys.stderr.write) are mis-routed into net_send. The catalog already encodes the correct answer via the MODULE qualifier — python.yaml:180-188 puts sys.stdout/stderr under logging (separate from the net_send asyncio.StreamWriter.write entry). A SIBLING consumer of the same catalog, io-boundaries, HONORS the module qualifier: io-boundaries puts all stderr/stdout chains in the logging bucket and its net_send bucket is exactly genuine urllib.request.urlopen/urlretrieve/Request chains with zero stderr writes. So the defect is not merely that match_sink ignores ambiguous_names (this row's framing) — it is that match_sink discards the module qualifier the catalog provides (logging != net_send by module) while io-boundaries keeps it. One catalog, two consumers, only taint mis-routes console writes. Same fix as this row (wire the module qualifier into match_sink), hence bundled here rather than filed new. Substrate evidence: probe_2_logging.log / probe_2_vc_sinknames.log."  # ebf3
- op: update  # 5e48
  at: '2026-06-08T18:30:29Z'  # 5e48
  by: agent  # 5e48
  actor: jgstern_agent  # 5e48
  clock: 4  # 5e48
  nonce: '5e48'  # 5e48
  set: {}  # 5e48
  add:  # 5e48
    tags: [dogfood_tranche_03]  # 5e48
  remove:  # 5e48
    tags: [dogfood_tranch_03]  # 5e48

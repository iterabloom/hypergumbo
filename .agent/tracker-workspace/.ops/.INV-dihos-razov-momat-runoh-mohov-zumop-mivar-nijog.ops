- op: create  # 4f72
  at: '2026-04-07T07:45:38Z'  # 4f72
  by: agent  # 4f72
  actor: jgstern_agent  # 4f72
  clock: 1  # 4f72
  nonce: 4f72  # 4f72
  data:  # 4f72
    kind: invariant  # 4f72
    title: "Go receiver-type inference must follow method-call return types"  # 4f72
    status: violated  # 4f72
    priority: 1  # 4f72
    parent:  # 4f72
    tags:  # 4f72
    - analysis_quality  # 4f72
    - cross_language_linkers  # 4f72
    before: []  # 4f72
    duplicate_of: []  # 4f72
    not_duplicate_of: []  # 4f72
    pr_ref:  # 4f72
    description: "Source: deep-20260407-030917 prometheus reflect agent. Three observed misresolutions in API.query outgoing edges: qry.Close -> ResourceClient.Close (wrong), warnings.Add -> unsafeLabelAdder.Add (wrong), r.Context -> LazyLoader.Context (wrong, should be http.Request.Context). Receiver type tracking from WI-hikug catches direct type assignments and parameters but not types that flow through return values of method calls or struct field accesses. Affects every Go repo with the .Close()/.Add()/.Set()/.Context() pattern (essentially every Go service). Likely fix: extend _extract_go_var_types to record return types from typed method calls when the caller's type is known. Same issue contaminates symbol_ranking globally because every .Close() call in the codebase pollutes a single ResourceClient.Close with bogus in-degree."  # 4f72
    fields:  # 4f72
      statement: "Go method calls on local variables that hold the return value of another method call (or struct field access) must resolve via the receiver's typed return type, not via global short-name lookup."  # 4f72
      root_cause: "WI-hikug closed with receiver type tracking that handles short var declarations, var declarations, function parameters, and method receivers — but it does not propagate types through method-call return values. Example from prometheus deep-20260407-030917 web/api/v1/api.go:502-569 API.query: qry, err := ng.NewInstantQuery(...) should give qry the type promql.Query (return type of QueryEngine.NewInstantQuery). Then qry.Close() should resolve to promql.Query.Close. Instead it resolves to ResourceClient.Close from a different package. Same pattern for warnings.Add and r.Context."  # 4f72
- op: discuss  # b24a
  at: '2026-04-07T07:53:57Z'  # b24a
  by: agent  # b24a
  actor: jgstern_agent  # b24a
  clock: 2  # b24a
  nonce: b24a  # b24a
  message: "Verified by reading go.py:1248-1557. The receiver type tracker has 4 patterns: (1) short_var_declaration via _type_from_rhs which only handles literals (Server{}, &Server{}) and constructor naming (NewFoo() -> Foo); (2) var_spec; (3) parameter_declaration; (4) method_declaration. The misresolutions in API.query are caused by gaps: (a) qry := ng.NewInstantQuery(...) — _type_from_constructor_call infers 'InstantQuery' (strip New + capital match), but the actual return type is Query (interface). qry.Close then can't find InstantQuery.Close and falls back to global lookup -> ResourceClient.Close. (b) warnings := q.Warnings() — not a constructor naming pattern, so no type inferred at all. (c) r.Context() — r is a parameter with type *http.Request, parameter_declaration handler stores it as 'http.Request', but http.Request methods are not in source; resolver falls back globally. The structural fix requires a pre-pass that records each function/method declaration's return type into a registry, then when processing qry := bar.NewInstantQuery() looks up NewInstantQuery's actual return type instead of guessing from the name. This is a substantial architectural addition (cross-file pre-pass + new registry + new resolution path), not a small extension to _extract_go_var_types. Recommending P2 not P1 since the false targets are largely cosmetic (centrality pollution, slice noise) rather than a correctness regression vs prior sessions."  # b24a
- op: update  # 8477
  at: '2026-04-07T07:54:25Z'  # 8477
  by: agent  # 8477
  actor: jgstern_agent  # 8477
  clock: 3  # 8477
  nonce: '8477'  # 8477
  set:  # 8477
    status: needs_human_review  # 8477
    priority: 2  # 8477
- op: discuss  # 7483
  at: '2026-04-07T07:54:25Z'  # 7483
  by: agent  # 7483
  actor: jgstern_agent  # 7483
  clock: 4  # 7483
  nonce: '7483'  # 7483
  message: "Setting to needs_human_review pending architectural decision: the fix requires either (a) a new pre-pass that builds a function-return-type registry from method/function declarations across all Go source files, then resolves call expression types via this registry, or (b) accepting that name-based heuristic resolution will continue to misroute common methods like Close/Add/Context/Set when the receiver type isn't directly inferable. Option (a) is a substantial addition; option (b) leaves global name collisions in place. Recommend human triage before committing to a direction. Not a regression — this gap has been present and was only surfaced because INV-kunam fix made the prometheus slice now reach into web/api/v1/api.go where the misresolutions are visible."  # 7483
- op: discuss  # c394
  at: '2026-04-07T08:05:09Z'  # c394
  by: human  # c394
  actor: jgstern  # c394
  clock: 5  # c394
  nonce: c394  # c394
  message: "might analogous situations arise for other languages besides golang?"  # c394
- op: update  # 7b38
  at: '2026-04-07T09:19:06Z'  # 7b38
  by: human  # 7b38
  actor: jgstern  # 7b38
  clock: 6  # 7b38
  nonce: 7b38  # 7b38
  set:  # 7b38
    status: violated  # 7b38
- op: discuss  # 950d
  at: '2026-04-07T09:19:23Z'  # 950d
  by: human  # 950d
  actor: jgstern  # 950d
  clock: 7  # 950d
  nonce: 950d  # 950d
  message: "bumping to violated so you answer my above message"  # 950d
- op: discuss  # d378
  at: '2026-04-07T10:08:52Z'  # d378
  by: agent  # d378
  actor: jgstern_agent  # d378
  clock: 8  # d378
  nonce: d378  # d378
  message: "Investigated cross-language analogs via Explore agent. Verdict: this gap affects at least 8 statically-typed languages in structurally similar ways. The fix should NOT be Go-specific.\n\nLANGUAGES BY CATEGORY:\n\n**FULLY AFFECTED (same gap as Go)** — return-type-through-method-call resolution missing:\n- Java\n- Kotlin (return type tracking exists but doesn't propagate through method-call returns)\n\n**PARTIALLY AFFECTED** — some related infrastructure exists but return-value chaining missing:\n- C++ (explicit type declarations exist, but tree-sitter-cpp limits AST reconstruction; STL guard prevents false positives by enumeration, not by tracking)\n- C# (docstring mentions 'chained field type resolution' but implementation delegates without explicit return-value tracking)\n- Rust (struct field types enable self.field.method() resolution, but not return-value chaining for local variables)\n- PHP (optional type annotations exist but analyzer doesn't build return-type registries)\n- Scala (statically typed but no return-type tracking in analyzer)\n\n**NOT APPLICABLE** (dynamic typing makes the question incoherent without annotations):\n- Python, Ruby, JavaScript, Lua — receiver type is fundamentally unknowable at parse time without explicit annotations. These should be assessed separately: support restricted to annotated code, or gap is expected.\n\n**INSUFFICIENT TYPE TRACKING** (no tracking infrastructure to extend):\n- Swift, Kotlin (no full type tracking), Groovy, Dart\n\nCROSS-LINKER CHECK: type_hierarchy.py builds inheritance maps from extends/implements edges but operates post-analysis. It does not address the return-value type registry need.\n\nDESIGN IMPLICATION: a Go-specific fix to _extract_go_var_types would solve only 1 of ~8 affected languages and waste the architectural opportunity. The right approach is a shared pre-pass in the core analyzer framework:\n\n1. **Function-return-type registry pre-pass**: walk all symbols once, extract return types from declarations (signatures for typed languages, type hints for dynamic). Store as registry: dict[symbol_id] → return_type.\n2. **Per-language signature extractors**: each statically-typed analyzer contributes a small extractor that pulls return types from its language's syntax. ~30-50 lines per language.\n3. **Per-language consumers**: each analyzer's var_types inference gets one new branch — when processing 'x := obj.method()', look up method's return type in registry instead of guessing from name.\n\nThis becomes ADR-worthy because it's a cross-cutting architectural addition. Suggest scoping it as:\n- Phase 1: Java + Go (highest-value statically typed languages with most repos in the bakeoff pool, both already have receiver type tracking infrastructure)\n- Phase 2: Kotlin + C# + Rust (existing infrastructure; smaller delta)\n- Phase 3: C++ + Scala + PHP (no existing infrastructure; larger delta)\n- Phase 4: type-annotated Python (existing PEP 484 type hints make this tractable)\n\nANSWER TO YOUR DIRECT QUESTION ('might analogous situations arise for other languages besides golang?'): **Yes, at least 7 other languages have the same structural gap**. The fix should be cross-language. INV-dihos should be either (a) re-scoped as a meta-invariant covering all statically-typed languages, or (b) kept Go-specific and have sibling items created for each affected language. I recommend (a) for cleaner tracker organization."  # d378
- op: discuss  # e379
  at: '2026-04-07T16:20:08Z'  # e379
  by: human  # e379
  actor: jgstern  # e379
  clock: 9  # e379
  nonce: e379  # e379
  message: "ok that sounds good. should the ADR be brand new or augment an existing ADR? for \"Per-language signature extractors\" and \"Per-language consumers\", might there be a more convenient yaml config-based approach?"  # e379

- op: create  # 4c09
  at: '2026-07-18T03:36:42Z'  # 4c09
  by: agent  # 4c09
  actor: jgstern_agent  # 4c09
  clock: 1  # 4c09
  nonce: 4c09  # 4c09
  data:  # 4c09
    kind: work_item  # 4c09
    title: "INV-fahub D facet: gate unresolvable-receiver calls (exists/startsWith/toString @0.85) to unresolved; needs var-type tracking (or defer)"  # 4c09
    status: todo_hard  # 4c09
    priority: 2  # 4c09
    parent: INV-fahub-himis-zuzug-zijus-sahof-kabol-nolad-kakit  # 4c09
    tags:  # 4c09
    - cross_language_parity  # 4c09
    - wave5  # 4c09
    - analysis_quality  # 4c09
    isbefore: []  # 4c09
    duplicate_of: []  # 4c09
    not_duplicate_of: []  # 4c09
    pr_ref:  # 4c09
    description: "INV-fahub D facet. d_lang.py misbinds an unresolvable-receiver call (e.g. thing.exists()) to an arbitrary same-named internal def: exists(function) @0.85 (= 0.85 x 1.0 exact) for a top-level func. Misbind site: _resolve_and_emit_call_edge (d_lang.py:536-584), specifically the resolver.lookup at 563-567 emitting calls @ 0.85*conf at 568-577. Root cause is the WORST of the three: D does ZERO receiver/var-type tracking; the receiver is consulted only as an import-alias key (path_hint=import_aliases.get(receiver), 554-556), and — worse — `hints = imported_modules if not receiver else None` (562) DISABLES import-scope disambiguation the moment a receiver is present, so lookup does an unscoped global bare-name match. register_symbol (667-691) registers every func/method under its bare short name (691), seeding the exact-match.\n\nFIX (atomic, INV-nilud-conform): when a receiver is present but resolves to neither an import alias nor a tracked type, withhold the confident bare-name match and emit py.py's unresolved shape (calls, dst=d:external:0-0:{name}:unresolved, is_resolved=False, evidence_type=ast_call -> 0.40, meta={call_construct:method, +receiver_type_hint when typeable}). RECALL RECOVERY IS NOT OPTIONAL HERE: D must first grow at least minimal var->type tracking (from param types + `auto x = new T()` / typed decls) so it can attach a hint — otherwise ~100% of the funnel goes external with zero recovery (maximal dead-code FP spike). ADD 'd' to the inherited_calls Site-2 lang set for Step-1 direct.\n\nDISPOSITION DECISION (owner): if D var-type tracking is too large for one atomic PR, DEFER D entirely (out of the near-term INV-fahub build) rather than ship naked D suppression. Sequence LAST. Tests: linker template (lang=d) + d_lang.py producer test. Reproduces on current code @0.85 (verified 2026-07-17). Recall-regression severity: WORST."  # 4c09
    fields: {}  # 4c09
- op: discuss  # b859
  at: '2026-07-18T06:25:09Z'  # b859
  by: agent  # b859
  actor: jgstern_agent  # b859
  clock: 2  # b859
  nonce: b859  # b859
  message: "DEFERRED (owner decision 2026-07-18) after a read-only scope assessment. The standard atomic pattern (gate + type-threading + shared inherited_calls linker recovery) that closed the Scala (WI-bihit) and Swift (WI-votar) facets is INSUFFICIENT for D, for a D-specific reason: UFCS (Uniform Function Call Syntax). In D, 'thing.exists()' can mean EITHER a method on thing's type OR 'exists(thing)' — a free-function call. D's own model treats the receiver as a module prefix (_get_call_target_name_d: 'thing.exists()' -> (exists, 'thing')). The GATE is easy (a variable receiver that is not a known module -> suppress the misbind), but the shared linker's Site-2 recovery only resolves METHOD-on-type, NOT UFCS free-function resolution — so a legitimate 'thing.exists()' that is really 'exists(thing)' would be suppressed and CANNOT be recovered by the linker. Combined with (a) no existing var-type tracking (build from scratch) and (b) D's heavy 'auto' usage (many untyped-inference receivers -> no hint), naked D suppression would ship a large, UNRECOVERABLE recall regression on D's dominant call idiom (Agent-3's 'defer rather than ship naked' case). Doing D RIGHT requires UFCS-aware resolution — verify a free function's first-parameter type matches the receiver before binding — a D-specific resolver effort substantially larger than the Scala/Swift type-threading, arguably its own work item (candidate: a UFCS-aware Site in the inherited_calls linker, or an analyzer-side UFCS resolver). INV-fahub keeps its two clean facets (Scala + Swift, both merged); the D funnel (exists/startsWith/toString @0.85) stays open here, out of the near-term atomic build. Status kept todo_hard (deferred backlog, not near-term); re-scope to the UFCS-aware approach when picked up. Misbind sites unchanged: d_lang.py:536-584 (_resolve_and_emit_call_edge, resolver.lookup @0.85; 'hints=imported_modules if not receiver else None' at :562 disables import-scope disambiguation when a receiver is present)."  # b859
- op: update  # 4d28
  at: '2026-07-18T07:18:59Z'  # 4d28
  by: agent  # 4d28
  actor: jgstern_agent  # 4d28
  clock: 3  # 4d28
  nonce: 4d28  # 4d28
  set:  # 4d28
    description: "INV-fahub D facet — RE-SCOPED 2026-07-18 to route recovery through the shared receiver-type-index resolver (the family-2 substrate WI), rather than an analyzer-side UFCS resolver. D is the HARDEST/LAST member of that family.\n\nMISBIND (unchanged): d_lang.py confidently misbinds an unresolvable-receiver call (e.g. `thing.exists()`) to an arbitrary same-named internal def: `exists(function)` @0.85. Site: `_resolve_and_emit_call_edge` (d_lang.py:536-584); `resolver.lookup` @563-577; `hints = imported_modules if not receiver else None` @562 DISABLES import-scope disambiguation when a receiver is present; `register_symbol` @667-691 registers every func/method under its bare short name @691, seeding the exact-match.\n\nWHY D IS HARDEST (three compounding D-specifics):\n  1. UFCS — `thing.exists()` may legitimately mean `exists(thing)` (a FREE FUNCTION taking the receiver as first arg), NOT a method on thing's type. A pure suppression gate would kill these; recovery needs first-parameter-type matching, which only the family-2 resolver provides (NOT the inherited_calls MRO walker — that resolves method-on-type only).\n  2. Templates — `arr.map!(fn)` is a template call; `_get_ufcs_template_name` already exists (d_lang.py:518) but first-arg-type matching under generic constraints is harder than plain extension lookup.\n  3. Zero var-type tracking + heavy `auto` — D tracks no receiver types today, and `auto`-inferred receivers give no hint. Needs minimal var-type tracking (param types + typed decls + `auto x = new T()`) to attach a `receiver_type_hint`.\n\nPLAN (after the substrate + the Kotlin validation member land):\n  (a) GATE: a variable receiver that is neither a known module nor a tracked type -> withhold the confident bare-name match; emit py.py's unresolved shape (calls, dst=d:external:0-0:{name}:unresolved, is_resolved=False, evidence_type=ast_call -> 0.40, meta={call_construct:method, +receiver_type_hint when typeable}).\n  (b) VAR-TYPE TRACKING: build minimal receiver typing so the gate can attach a hint (else ~100% of the funnel goes external with zero recovery — the maximal dead-code FP spike; this is why naked D suppression was rejected 2026-07-18).\n  (c) RECOVERY via the family-2 resolver's UFCS branch: match a free function whose FIRST-PARAMETER TYPE == the receiver type before binding; emit `calls` is_resolved=True evidence_type=ast_call_ufcs. Also add `d` to inherited_calls Site-2 for the true method-on-type case (Step-1 direct).\n\nDEPENDS ON: the family-2 substrate WI + the Kotlin validation member (do the clean, template-free member first). INV-fahub D funnel: exists/startsWith/toString @0.85 (verified on current code 2026-07-17). Recall-regression severity if shipped naked: WORST. Tests: family-2 linker template (lang=d, UFCS branch) + d_lang.py producer test (gate + var-type + template)."  # 4d28
- op: discuss  # 0040
  at: '2026-07-18T07:18:59Z'  # 0040
  by: agent  # 0040
  actor: jgstern_agent  # 0040
  clock: 4  # 0040
  nonce: '0040'  # 0040
  message: "Re-scoped 2026-07-18: D recovery routes through the shared receiver-type-index resolver (INV-vigaf family-2 substrate WI-panah), NOT an analyzer-side UFCS resolver. D is the hardest/last member (UFCS + templates + no var-types + heavy auto). Now depends on WI-panah (substrate) + WI-lodij (Kotlin validation member). The UFCS finding from the 2026-07-18 3-lens investigation reframed D from an isolated one-off to the hardest point of a 15-language non-hierarchy-resolution family."  # 0040

- op: create  # 1937
  at: '2026-08-06T17:21:25Z'  # 1937
  by: agent  # 1937
  actor: jgstern_agent  # 1937
  clock: 1  # 1937
  nonce: '1937'  # 1937
  data:  # 1937
    kind: work_item  # 1937
    title: "python: route the tracked receiver type into the module slot so #231's edges can reach callees_at (INV-linub instance)"  # 1937
    status: todo_hard  # 1937
    priority: 2  # 1937
    parent:  # 1937
    tags:  # 1937
    - taint  # 1937
    - adr_0017  # 1937
    - analysis_quality  # 1937
    - io_boundary  # 1937
    isbefore: []  # 1937
    duplicate_of: []  # 1937
    not_duplicate_of: []  # 1937
    pr_ref:  # 1937
    description: "INSTANCE OF THE INV-linub CLASS, for python, on the SECOND consumer of L3\n(receiver evidence) that the class statement does not name.\n\nWHAT IS WRONG. PR #231 taught py.py to emit a call edge for an attribute chain\nrooted at a local -- voucher_list.append(x), p.save(...), payment.confirm().\nIt hardcodes the dst as python:external:0-0:NAME:unresolved, and its own\ncomment explains why: \"the receiver's type is genuinely unknown here\", and\ninventing a module would assert something false. Correct at that branch.\n\nBut the \"external\" placeholder in the module slot makes the edge USELESS\ndownstream. _module_from_symbol_path returns \"\" for the placeholder BY\nDOCUMENTED DESIGN, so _qualified_callee yields no key, so\n_catalogue_key_for_edge yields None, so the (function, line) never enters\ncallees_at, so _use_site_terminates cannot decide, so the section 3a walk\nrecords an ESCAPE. The edge is emitted and then discarded.\n\nMEASURED (2026-08-06, scripts/measure-call-escape-cause.py, positive control\npassed -- pretix: 443,296 edges offered, 191,264 accepted, 69 summary entries,\n31,913 callees_at entries): on pretix, 13 of 17 call-node escape sites (76.5%)\nare exactly this, and ZERO lack an edge. save, confirm, delete, get,\nrefresh_from_db, create_external_refund, log_action, append, create.\n\nWHY THIS IS THE JAVA SHAPE. PR #227 fixed the identical defect for java: the\ninferred receiver type \"was already in var_types, routed only to meta\", and the\nfix routed it into the module slot instead. py.py carries var_types AND\nexternal_var_types. The question this item must answer FIRST is whether either\nis reachable at the #231 emit site for the chain root -- if it is not, this is a\nlarger change than #227 was and should be re-scoped before being attempted.\n\n*** DIRECTION, AND IT IS THE DANGEROUS ONE. *** Giving these calls a qualified\nkey lets _use_site_terminates return True more often, which produces MORE False\nverdicts, and since PR #214 a False earns sanitized and DROPS the flow from the\nviolation set. This is a SUPPRESSIVE change. Two measured counterweights, both\nof which must be re-checked before landing rather than taken from this note:\n  1. Of the three pretix sites that DID get a key, two hit \"no summary\" and one\n     hit \"propagates\". None closed. So the empirical suppression on this corpus\n     is small.\n  2. There are ZERO barrier-arm walks on all four repos that produce walks\n     (hypergumbo 36, pretix 35, prometheus 7, alertmanager 3 -- every\n     invocation on the 3a arm, where False and None collapse). So today this\n     change cannot move a finding in EITHER direction.\n\nCONSEQUENCE FOR PRIORITY: this is a capability fix with a measured-zero blast\nradius, which makes it cheap and safe NOW and dangerous LATER -- the hazard\narms when a sanitizer catalogue exists. It is filed at P2 rather than P1\nbecause it moves no finding today; it is filed rather than skipped because the\nwindow in which it is safe closes.\n\nACCEPTANCE, and it must be measured through the consumer, not at the analyzer:\nre-run measure-call-escape-cause.py on pretix and show stage 3a shrinking, AND\nreport what the sites moved TO (4a catalogue gap / 4b genuine propagating\nescape), AND report the finding-level delta in both directions. An emission-only\nor key-only win measured at the edge is exactly the capability-is-not-recall\nfailure this class was filed to prevent."  # 1937
    fields: {}  # 1937
- op: update  # 8871
  at: '2026-08-06T18:31:06Z'  # 8871
  by: agent  # 8871
  actor: jgstern_agent  # 8871
  clock: 2  # 8871
  nonce: '8871'  # 8871
  set:  # 8871
    priority: 1  # 8871
- op: discuss  # 9a98
  at: '2026-08-06T18:31:06Z'  # 9a98
  by: agent  # 9a98
  actor: jgstern_agent  # 9a98
  clock: 3  # 9a98
  nonce: 9a98  # 9a98
  message: "RAISED P2 -> P1 and pulled to NEXT, 2026-08-06, on evidence gathered after\nfiling. When filed this looked like a capability fix with a measured-zero blast\nradius. It is now the gate on the whole refutation question.\n\nWHAT CHANGED. Two purpose-built fixtures established that the ADR-0017\nSANITIZER BARRIER ARM cannot run at all, and the cause is this item:\n  v1  f = Fernet(key); plain = f.decrypt(tok); cipher = f.encrypt(plain)\n      -> emits python:external:0-0:decrypt:external_symbol. Receiver type\n         never inferred, so neither the plaintext SOURCE nor the Fernet.encrypt\n         SANITIZER can match. Zero walks.\n  v2  the same shape module-qualified -> source and sanitizer resolve, but the\n      SINK pathlib.Path(...).write_text(x) emits NO call edge (a chain rooted\n      at a CONSTRUCTOR RESULT -- recorded on INV-mumov as a distinct open\n      sub-shape). Zero walks again.\n\nEVERY sanitizer in the shipped catalogue is an INSTANCE METHOD --\nFernet.encrypt, AESGCM.encrypt, AEAD.Seal, Cipher.doFinal. So registering any\nbarrier at all requires exactly the receiver evidence this item supplies.\n\nCONSEQUENCE: this item now gates three things, not one -- the section 4 summary\nlookup inside the 3a walk (its filing), the sanitizer barrier arm, and through\nthat arm every proposal in this area that depends on a False verdict. Until it\nlands, every escape/refutation measurement in the taint area is confounded, and\nre-measuring INV-busis first would re-measure the confound.\n\nSCOPE NOTE, because v2 says one half is not enough: a barrier walk needs BOTH\nreceiver typing for the source/sanitizer AND an emitted edge for a\nconstructor-result sink. Landing only this item will move the fixture from\n\"zero walks\" to \"still zero walks\" unless INV-mumov's constructor-result\nsub-shape lands with it. Sequence them together or the acceptance test cannot\npass.\n\nACCEPTANCE, unchanged in spirit and now concrete: the deliverable is a WORKING\nBARRIER-ARM POSITIVE CONTROL -- a fixture that produces at least one barrier\nwalk -- plus the finding-level delta in BOTH directions on the cohort. This is\nthe first change in this area that can make an unearned False suppress a real\nfinding, so WI-joluk's gate gets its first genuine A/B here."  # 9a98
- op: discuss  # 44db
  at: '2026-08-06T22:05:04Z'  # 44db
  by: agent  # 44db
  actor: jgstern_agent  # 44db
  clock: 4  # 44db
  nonce: 44db  # 44db
  message: "BARRIER-ARM MECHANISM CORRECTED A SECOND TIME, 2026-08-06 (dev 8b1a20cc1b,\nfiled as INV-funit). This thread carries a correction from earlier the same\nday saying \"receiver typing arms the suppression hazard, not catalogue\ngrowth\". THAT IS ALSO WRONG and is retracted; it was the second uncontrolled\naccount of the same zero.\n\nESTABLISHED WITH A POSITIVE CONTROL, which neither prior account had: the\nbarrier arm was blocked by _register_sanitizer_callers reading the NAME slot\nof the dst for receiver evidence that production analyzers put in the MODULE\nslot. The comparison was \"cryptography.fernet.Fernet.encrypt\" == \"encrypt\" --\nfalse by construction for every method-shaped sanitizer, i.e. the entire\nshipped catalogue -- so the INV-finoh call_construct==\"method\" refusal was\nunconditional in production.\n\nTwo probes differing ONLY in receiver form emit a BYTE-IDENTICAL dst\n(python:cryptography.fernet.Fernet:0-0:encrypt:external_symbol) and differ\nonly in meta.call_construct; that alone flipped sanitizer_lines from\n{(handler,'plaintext'): [7,7,7,7]} to {}. The idiomatic form reported\n\"violated\" at \"precise\" confidence about code that visibly encrypts, and now\nreports \"confirmed\" with the arm firing.\n\nWHAT SURVIVES FROM BOTH RETRACTED ACCOUNTS: WI-joluk's coverage gate landing\nbefore removal authority is still right. The section 3a arm still tests\n`is True`, so escape-reclassification and section 7a field sensitivity are\nstill structurally INERT for finding counts. INV-karud clause (b)'s zero is\nstill not independent corroboration.\n\nWHAT IS NEW AND DECISION-RELEVANT: the corpus holds ZERO plaintext sanitizer\ncall sites (pretix's single gate-reaching edge is a bare `encrypt`, correctly\nrefused; everywhere else is absence), and the module slot reaches taint only\nfor python and java. The sanitizer/barrier machinery has no live surface on\ntoday's corpus -- everything here is prevention whose value scales with a\ncatalogue that is not growing. That belongs in the exit-condition package."  # 44db
- op: update  # e32c
  at: '2026-08-07T20:21:39Z'  # e32c
  by: agent  # e32c
  actor: jgstern_agent  # e32c
  clock: 5  # e32c
  nonce: e32c  # e32c
  set:  # e32c
    status: done  # e32c
    pr_ref: '246'  # e32c
- op: discuss  # d959
  at: '2026-08-07T20:21:39Z'  # d959
  by: agent  # d959
  actor: jgstern_agent  # d959
  clock: 6  # d959
  nonce: d959  # d959
  message: "DONE by PR #246 (dev 5373b056c4), on behavioral evidence.\n\nShapes re-run on the merged tree:\n  def h(p: pathlib.Path, x): p.write_text(x)   external -> pathlib.Path,  tagged 0 -> 1\n  from pathlib import Path; def h(p: Path, x)  external -> pathlib.Path,  tagged 0 -> 1\n  import pathlib as pl; def h(p: pl.Path, x)   alias expands to pathlib.Path\n  def h(s: socket.socket, b): s.send(b)        external -> socket.socket, tagged 0 -> 1\n  AnnAssign and return-annotated factory       stay external, tagged 0 (deliberate)\n\nCORPUS: poetry 38 -> 52 reported boundaries (+14, +36.8%), sqlalchemy 43 -> 47\n(+9.3%). Additions are pathlib.Path.{exists,is_dir,is_file,stat,glob,iterdir,\nmkdir} on annotated parameters. Three adjudicated against source, all TRUE\nPOSITIVES (e.g. poetry \ncalling path.exists() and path.mkdir(parents=True, exist_ok=True)).\n\nSCOPE WIDER THAN THIS ITEM WAS FILED FOR: the filing named the section 4 summary\nlookup (callees_at) as the blocked consumer. The io-boundary consumer was\nblocked too, by the same missing module slot. Both are unblocked.\n\nSCOPED TO PARAMETERS on measurement: of 95,245 external-placeholder method\nedges, 4,160 have a receiver with a resolvable annotation and 63 reach the\ncatalogue; 96% of that payload is the annotated-parameter shape. AnnAssign,\nreturn-annotated factories, attribute reads and generics contribute exactly\nzero and are pinned as excluded by tests that say why.\n\nDIRECTION: this ADDS classifications -- the first change in this area that does.\nSafety is the binding check, not the gate: a minted hint bypasses\ngate_named_entry and ambiguous_names by design (gating the hinted path costs\n61.5-87.2% of all boundaries, see INV-kipor). Raw annotation text was measured\nto produce CONFIRMED false boundaries (conn: Connection -> sqlite3.Connection.\nexecute plus a database-zone taint sink; 174 such annotations in the corpus).\nResolution reuses _import_binding_for from INV-kipor / PR #244.\n\nGO: nothing built, and that is measured. Go has no equivalent gap (96.7% of\npackage-qualified receivers already reach a usable module slot). Its residual\ninitializer-inference gap and the reason the obvious fix is net-harmful (17.1%\nwrong-symbol hijack rate for +0.61% gain) are filed as WI-kamin.\n\nDOES NOT CLOSE INV-linub. This is one instance (python, annotated parameters).\nStill open under that class: unannotated receivers, self.<field>.<method>()\nwhich emits NO call edge at all, kotlin (WI-nasuf), and the FP channels\nINV-fokik / INV-fazim.\n\nCOLD START, disclosed: 17 of the 18 catalogue entries this activates fire zero\ntimes on the corpus today, so their boundary labels have never been validated\nagainst real output. Path.{exists,is_dir,is_file,stat} are catalogued fs_read\nthough they stat rather than read contents -- a catalogue-labelling question,\nuntouched here. Relative imports, importlib, conditional and TYPE_CHECKING\nimports are unprobed. The 63-site population is a property of the six-repo bag:\nadding one repo moved it +20.6%."  # d959
- op: discuss  # ca9c
  at: '2026-08-07T20:22:17Z'  # ca9c
  by: agent  # ca9c
  actor: jgstern_agent  # ca9c
  clock: 7  # ca9c
  nonce: ca9c  # ca9c
  message: "Correction to the previous entry: one line was truncated by shell quoting. The\nadjudicated example reads in full --\n\n  poetry src/poetry/utils/helpers.py:273-285\n\n      def is_dir_writable(path: Path, create: bool = False) -> bool:\n          try:\n              if not path.exists():\n                  if not create:\n                      return False\n                  path.mkdir(parents=True, exist_ok=True)\n\n  path.exists() -> pathlib.Path.exists / fs_read\n  path.mkdir()  -> pathlib.Path.mkdir  / fs_write\n\nBoth are real filesystem operations on an annotated parameter, invisible before\nPR #246. The other two adjudicated sites:\n\n  src/poetry/utils/cache.py:334-340\n      def _get_cached_archives(self, cache_dir: Path) -> list[Path]:\n          paths += cache_dir.glob(...)              -> Path.glob / fs_read\n\n  src/poetry/inspection/info.py:472-483\n      def from_sdist(cls, path: Path) -> PackageInfo:\n          if path.is_file():                        -> Path.is_file / fs_read\n\n3 of 3 true positives on direct source reading."  # ca9c
- op: discuss  # 5cea
  at: '2026-08-08T19:24:34Z'  # 5cea
  by: agent  # 5cea
  actor: jgstern_agent  # 5cea
  clock: 8  # 5cea
  nonce: 5cea  # 5cea
  message: "FOLLOW-ON SHIPPED: PR #247 (dev e54a20c07e) — a derivation from a typed receiver\nkeeps the type, for an allowlisted member only.\n\n#246 typed the ROOT (annotated parameter). #247 keeps that type across a\nderivation, which is what makes the root typing pay:\n\n    p = d / \"f.txt\"        -> pathlib.Path   (was external)\n    p = d.joinpath(\"f\")    -> pathlib.Path   (was external)\n    tests = path / \"tests\"; tests / \"__init__.py\"   (recurses)\n\nMEASURED against a post-#246 baseline so the delta is attributable to #247\nalone: poetry 52 -> 57 (+5, all five TRUE POSITIVES read against source);\nsqlalchemy 47 -> 47 (+0, a real zero — a DB library barely derives paths).\nmitmproxy moved 248 -> 254 but its baseline PREDATES #246, so that +6 spans both\nchanges and is NOT credited to #247. Its sites are direct annotated-parameter\nuses (load_dhparam(path: Path) calling path.exists()/path.write_bytes()), i.e.\n#246's work. Recorded because the mislabel nearly reached a commit message.\n\nDEFAULT-DENY ALLOWLIST, measured: propagating by default is 25.9% precise (7/27\nadded boundaries) and mints 16 false taint sinks incl. 2 database + 2 network;\n85.1% of corpus hop>=1 propagations are provably wrong or unverifiable, 20 of\nthe 21 provably-wrong ones being a Path->str transition. Allowlist = 85.7%,\ncosts 3 boundaries of 427 (0.7%).\n\nNOT A NEW CATALOGUE. FileAnalysis.method_return_types (INV-dihos / WI-kuroj) is\nthe language-neutral return-type registry go.py and rust.py already populate;\npython populated it ZERO times. A proposed 9th YAML catalogue was refuted 3/3 on\nexactly this point. TYPE_PRESERVING_MEMBERS is the stdlib complement, same shape.\n\n=== RESIDUAL GAPS IN THIS FAMILY (none are regressions; all pre-existing) ===\n\n1. INLINE CHAIN ROOT emits NO CALL EDGE AT ALL:\n       (d / \"a\" / \"b.txt\").write_text(x)     -> zero edges, before AND after #247\n   The BOUND form works (p = d / \"a\" / \"b.txt\"; p.write_text(x)). This is the\n   attribute-chain-rooted-at-an-expression gap, not a typing gap — an EMISSION\n   defect upstream of everything here, and the same family as INV-mumov and the\n   `self.<field>.<method>()` hole. Sized: unmeasured.\n2. `.parent` / `.parents[n]` are ATTRIBUTE READS, a different AST shape; not\n   routed. Smaller population than the call/operator shapes.\n3. Path.cwd() / Path.home() — classmethods on the TYPE, not derivations.\n4. Interprocedural propagation (a derived Path returned from a helper): the\n   probe produced 0 edges; FP risk unmeasured.\n5. `for p in d.iterdir(): p.unlink()` loop targets: 0 boundaries in every mode.\n6. self.attr receivers: production does not type them either.\n\n=== INSTRUMENT FAILURES, recorded so the zeros are not read as absence ===\n\nKotlin and Swift populations for this family are NOT MEASURED — both failed\ntheir POSITIVE CONTROL, so their zeros are instrument nulls:\n  - kotlin emits NO unresolved method-call edge for a method call on a receiver\n    at all (only bare callees), so all 181 method-kind kotlin catalogue entries\n    are currently unreachable. That is WI-nasuf's territory, independently\n    corroborated here.\n  - swift puts the receiver VARIABLE NAME in the module slot: a probe produced\n    `swift:fm:0-0:fileExists` for a `fm` local. A variable name in a module slot\n    is a defect in its own right — filed separately."  # 5cea
- op: discuss  # e928
  at: '2026-08-08T23:53:13Z'  # e928
  by: agent  # e928
  actor: jgstern_agent  # e928
  clock: 9  # e928
  nonce: e928  # e928
  message: "PYTHON RECEIVER TYPING COMPLETE on the shapes that carry payload. PR #250\n(dev d71f0c0b0d) closes the tail.\n\nFOUR PRs: #244 bare ctor names binding-checked; #246 annotated PARAMETER is\nreceiver evidence; #247 a DERIVATION keeps the type via an allowlist; #250\nINLINE EXPRESSION receivers + ANNOTATED ASSIGNMENTS.\n\npoetry 38 -> 59 reported io-boundaries. sqlalchemy 43 -> 47. Every addition\nadjudicated TRUE POSITIVE by reading source. #250's two poetry additions are\n`path.joinpath(\"direct_url.json\").exists()` and `.read_text(...)` in\nInstalledRepository -- an annotated param, a derivation, then an INLINE receiver.\n\nINDEPENDENT CONFIRMATION from hypergumbo's self-analysis (ARCHITECTURE.md regen):\ncalls edges 66,211 -> 72,266 (+6,055), total edges 131,586 -> 137,972,\n`write_text` centrality 3,310 -> 6,142.\n\n=== TWO CORRECTIONS TO THIS THREAD'S OWN RECORD ===\n\n1. `self.<field>.<method>()` does NOT \"emit no call edge at all\", as recorded\n   here earlier. 81-83% of those sites DO emit, carrying no receiver evidence --\n   a TYPING gap, not an emission one. The false claim came from generalising a\n   single fixture that used an OWN field, which is the ~17% minority that\n   genuinely emits nothing. Corrected on INV-mumov too.\n2. The AnnAssign zero-payload exclusion (#246) EXPIRED when #247 shipped: once\n   derivations kept their type, an annotated assignment began seeding a chain.\n   The test pinning that exclusion FAILED as designed and forced the re-pricing\n   rather than allowing a silent widening. Retired in place with the reason.\n\n=== STILL OPEN, each a recorded decision with a pinning test ===\n - return-annotated factories (`def mk() -> Path`): needs external-type\n   resolution of a FUNCTION's return annotation, a different mechanism.\n - `.parent` / `.parents[n]`: attribute reads. MEASURED at 16 sites = 0.7% of\n   chain-on-Name misses. Explicitly DO NOT PRIORITISE.\n - loop targets (`for p in d.iterdir()`), interprocedural propagation,\n   self.attr receivers: unbuilt.\n - INLINE CHAIN ROOT still emits NO edge for the *unbound* nested form; the\n   bound form works. Pre-existing emission gap (INV-mumov family), not typing.\n\n=== WHAT THIS DOES NOT CLOSE ===\nINV-linub stays violated. Catalogue reach is 16.08% (23.0% excluding\nsqlalchemy, whose 16,725 sites have ZERO catalogue entries -- an L1 gap\ninflating every blocker sizing ~2.9x). The largest remaining instance is the\nUNTYPED bare-Name receiver, 7,282 of 16,818 non-reaching sites (43.3%). Typing\nannotated and derived receivers was never going to close a class that quantifies\nover every receiver syntax and every language."  # e928
- op: discuss  # 7ec7
  at: '2026-08-09T01:53:25Z'  # 7ec7
  by: agent  # 7ec7
  actor: jgstern_agent  # 7ec7
  clock: 10  # 7ec7
  nonce: 7ec7  # 7ec7
  message: "CORRECTION to the 2026-08-08 note on this thread, which is replicated verbatim on\nINV-linub, INV-mumov and WI-zilag. Adjudicated by four independent measurement lenses\nplus three adversarial refuters each; the numbers below are only those that survived\nrefutation, and the refuted ones are named at the bottom so they are not re-cited.\n\n1. \"32.9% of the measured population (sqlalchemy, 16,725 sites)\" IS A COHORT ARTIFACT,\n   NOT A CATALOGUE GAP. The sqlalchemy REPO was one of the 7 repos measured (pretix,\n   sqlalchemy, mitmproxy, poetry, pyright, meson, kserve). All 16,994 rows in the\n   sqlalchemy artifact carry file paths under ALL_REPOS/curriculum_repos/sqlalchemy/,\n   and the instrument's own REPOS constant names it. It was never call sites INTO\n   sqlalchemy-as-a-dependency. Confirmed independently by three lenses; that 7-repo\n   cohort contains ZERO sqlalchemy consumers, so it could not have tested the other\n   reading even in principle.\n\n2. \"it inflates the blocker ~2.9x\" IS WRONG FOR REACH. The true factor is 1.47x\n   (50,772 population) / 1.48x (49,811). The genuine 2.9x belongs to a DIFFERENT\n   family -- CALL_RESULT_ROOT no-edge, 8,962 total of which sqlalchemy is 5,826 =\n   2.86x, which is INV-mumov's territory. The factor was computed on one family and\n   applied to another.\n\n3. \"ZERO catalogue entries -- unreachable at L1\" IS DIAGNOSTICALLY WRONG. sqlalchemy\n   has 88-93 REACHING sites (builtins.open 20, os.path.exists 11, os.remove 11). Its\n   denominator is dominated by short-name COLLISIONS with catalogue entries -- execute\n   3,228, all 1,721, order_by 1,362, filter, get, commit -- whose only catalogue homes\n   are django.db.models / sqlite3. Those sites MUST NOT reach. 52.7% of them are in\n   ambiguous_names. The remedy for a collision is not a catalogue entry.\n\n4. TWO POPULATIONS, ONE PROBE -- name one and stay in it. 50,772 (plain\n   (file,line,name) dedup) and 49,811 (agg2.py's uniq()) come from the same row set\n   under two aggregation rules, reproduced 7/7 per repo in both directions. PREFER\n   50,772 AND SAY SO: uniq() DELETES BOTH MEMBERS of a colliding site, dropping 961\n   distinct sites that are reach-depleted (8.1% vs 16.06%), so 49,811 is the truncated\n   population and mildly INFLATES every reach rate.\n\n5. NEW, AND IT UNDERCUTS THE L4 FIGURES ON THIS THREAD: django.db.models-stamped sites\n   reach at 4,623/4,623 = 100.00% BY CONSTRUCTION -- py.py:527 hard-codes the module\n   string, python.yaml holds the same 29 names, and _module_matches on identical\n   strings is unconditionally True. That family is 57.3% of the reach numerator. Strip\n   it and L4-admit-given-evidence falls 78.69% -> 61.22%. The 87.0% D3 figure is\n   pretix at 99.34% against 54.84% for the other six repos. Do not quote 78.6%/87.0%\n   as a general result.\n\n6. INV-mumov's \"15.2%\" reproduces from NO candidate denominator any lens tried. Treat\n   it as unsourced.\n\nREFUTED 3/3, DO NOT RE-CITE: a \"cross-instrument splice\" between two different probes\n(there is one probe, two aggregation rules); and the whole \"no long tail / 2.53%\nthird-party / bounded curation\" family -- its census admits a site only if the method\nname is ALREADY in python.yaml, which excludes by construction every name curation\nwould add, and adding two out-of-cohort repos moved distinct third-party packages\n52 -> 88 with no saturation.\n\nARTIFACT INTEGRITY: the run that produced the 32.9%/23.0% anchor (inv8/denom-inference)\ncarries an ENOSPC traceback in its own log. The per-repo rows are self-consistent and\nreproduce, but that artifact was written under the WI-kovoj disk trap.\n\nWHAT THIS MEANS FOR THE WORK: there is no L1 catalogue fix to make here. The real\nuser-facing defect found while adjudicating this paragraph is filed separately as\nINV-fibis (verify-claims returns `confirmed` for a boundary reachable through modules\nthe catalogue cannot adjudicate -- reproduced on poetry and full-stack-fastapi-template\nwith a positive control firing in the same run)."  # 7ec7
- op: discuss  # bb85
  at: '2026-08-09T10:49:05Z'  # bb85
  by: agent  # bb85
  actor: jgstern_agent  # bb85
  clock: 11  # bb85
  nonce: bb85  # bb85
  message: "Mechanism C landed as PR #253 (pathlib.Path constructor typing, both table keys, plus the unbound-name tightening). See the full A/B and the remaining-mechanism ranking on INV-linub's thread. This item's own scope (annotated receivers) was already done; recording the follow-on so the chain is traceable."  # bb85

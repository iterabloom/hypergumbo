- op: create  # 7bed
  at: '2026-05-28T23:15:55Z'  # 7bed
  by: agent  # 7bed
  actor: jgstern_agent  # 7bed
  clock: 1  # 7bed
  nonce: 7bed  # 7bed
  data:  # 7bed
    kind: invariant  # 7bed
    title: "slice feature.edge_ids leaks file-level imports when entry is a function"  # 7bed
    status: violated  # 7bed
    priority: 2  # 7bed
    parent:  # 7bed
    tags:  # 7bed
    - pass19  # 7bed
    - dogfooding  # 7bed
    isbefore: []  # 7bed
    duplicate_of: []  # 7bed
    not_duplicate_of: []  # 7bed
    pr_ref:  # 7bed
    description: ""  # 7bed
    fields:  # 7bed
      severity: "high"  # 7bed
      statement: "hypergumbo slice --entry='<function>' --max-hops 0 (and 1) returns feature.edge_ids containing edges of the *containing file*, not the function. Concretely: entry=_hash (function in fingerprint.py:242-244) with --max-hops 0 returns 12 edge_ids — ALL are 'imports' edges with src=fingerprint.py:1-1:file:file (file-level imports). The actual edges involving _hash (4 real call-graph edges) are NOT in feature.edge_ids. With --max-hops 1: 14 total edges, only 2 touch the entry. The slice subcommand conflates function-entry scope with containing-file scope."  # 7bed
      root_cause: "slice gathers edges by walking from the entry's containing FILE, not from the entry node itself, then bounds by hops. The reduction step doesn't filter file-level imports that don't reach the function. Affects packages/hypergumbo-core/src/hypergumbo_core/slice.py or analogous."  # 7bed
- op: update  # a4f9
  at: '2026-05-30T05:32:27Z'  # a4f9
  by: agent  # a4f9
  actor: jgstern_agent  # a4f9
  clock: 2  # a4f9
  nonce: a4f9  # a4f9
  set:  # a4f9
    priority: 1  # a4f9
    fields:  # a4f9
      dogfood_anon_id: "ITEM-8da5aa"  # a4f9
  add:  # a4f9
    tags: [dogfood_corpus_20260528, dogfood_singleton, dogfood_severity_significant]  # a4f9
- op: discuss  # 0cc5
  at: '2026-05-30T09:06:43Z'  # 0cc5
  by: agent  # 0cc5
  actor: jgstern_agent  # 0cc5
  clock: 3  # 0cc5
  nonce: 0cc5  # 0cc5
  message: "Cross-ref backfill (2026-05-30, deeper-issue investigation): Confirmed mechanism. slice.py:550-551 seeds add_file_imports(entry.path) during entry-node initialization regardless of whether the entry is a function or a file node. With --max-hops 0, the BFS loop at slice.py:588 immediately continues without traversing anything, leaving file-level imports as the sole contents of visited_edges. Two structural fixes: (i) only seed file-imports when the entry kind is a container/file kind; (ii) post-filter visited_edges to drop import edges whose src file-node was not itself walked as an in-scope node. Twin to INV-titid — both describe the same architectural choice (feature scope not re-derived from emitted graph): INV-tarol leaks above-scope content INTO feature.edge_ids; INV-titid leaks below-scope refs OUT OF features[].edge_ids when the referenced edges are pruned."  # 0cc5
- op: discuss  # cb58
  at: '2026-06-05T16:03:19Z'  # cb58
  by: agent  # cb58
  actor: jgstern_agent  # cb58
  clock: 4  # cb58
  nonce: cb58  # cb58
  message: "[+ tranch_04: F66.A1 (EXTENSION) — re-confirmed on substrate HEAD eb5355917e: every FORWARD slice's edge_ids include file-import edges with BOTH endpoints absent from node_ids (subgraph not closed; 33 dangling dsts exist as in-repo nodes); F66.A2 adds 1526/25504=6.0% of functions are zero-out-degree where file imports are substituted for callees, and --exclude-imports exposes the honest empty slice. F74.A1 proved this is ORTHOGONAL to the src-dangling/tier-4 hole (slice projection, globally closed).]"  # cb58

- op: create  # c4b8
  at: '2026-06-10T17:35:33Z'  # c4b8
  by: agent  # c4b8
  actor: jgstern_agent  # c4b8
  clock: 1  # c4b8
  nonce: c4b8  # c4b8
  data:  # c4b8
    kind: work_item  # c4b8
    title: "Re-check the two no-fix pip-audit ignores in release-check; drop when upstream fixes land"  # c4b8
    status: todo_hard  # c4b8
    priority: 2  # c4b8
    parent:  # c4b8
    tags:  # c4b8
    - release  # c4b8
    - security  # c4b8
    isbefore: []  # c4b8
    duplicate_of: []  # c4b8
    not_duplicate_of: []  # c4b8
    pr_ref:  # c4b8
    description: "scripts/release-check carries two --ignore-vuln flags added 2026-06-10 to unblock prepare-release 6.0.0, both for advisories with NO released fix at the time (verified fix_versions: [] via pip-audit -f json): (1) PYSEC-2026-139 — torch 2.10.0, transitive dep of the optional sentence-transformers embeddings extra; this one matters most because end users who install the embeddings extra get torch too. (2) CVE-2025-69872 — diskcache 5.6.3, pulled in only by llama_cpp_python (local LM experimentation, not a hypergumbo dependency in any extra). ACTION at each release prep (or when this surfaces in tracker ready): run pip-audit -f json (or check the advisories directly), and for each of the two: if a fixed version now exists, upgrade the local package, remove the corresponding --ignore-vuln flag + its comment block from scripts/release-check, and verify release-check passes. Close this item only when BOTH ignores are removed. Context thread: the 6.0.0 release-prep session 2026-06-10; the gate edit merged as 0b24486ac9."  # c4b8
    fields: {}  # c4b8
- op: discuss  # 8c43
  at: '2026-06-11T17:21:40Z'  # 8c43
  by: agent  # 8c43
  actor: jgstern_agent  # 8c43
  clock: 2  # 8c43
  nonce: 8c43  # 8c43
  message: "[Wave-0 hygiene re-verify] Rescope WI-totak to track THREE no-fix ignores (not two): CVE-2025-69872 (diskcache via llama_cpp_python), PYSEC-2026-139 (torch), and CVE-2025-3000 (torch advisory family variant, added 2026-06-10). Distinguish from two permanent CI ignores: CVE-2025-71176 (pytest dev-only) and CVE-2026-3219 (pip AV:L). Action: at each release or when this item surfaces in `tracker ready`, run pip-audit -f json and for each of the three, if a fixed version exists, upgrade the package and remove the corresponding --ignore-vuln flag + comment block from scripts/release-check:212-227. Close only when all three no-fix ignores are removed. Reference: scripts/release-check:202-227."  # 8c43
- op: discuss  # 789d
  at: '2026-08-18T06:23:20Z'  # 789d
  by: agent  # 789d
  actor: jgstern_agent  # 789d
  clock: 3  # 789d
  nonce: 789d  # 789d
  message: "RE-CHECKED BOTH IGNORES 2026-08-18 WITH LIVE RUNS, not by re-reading the list.\nThis is what this item asked for; one of the two is now discharged.\n\nPYSEC-2026-597 (nltk) -- DROPPED in PR #407. It was carried as \"no fix\navailable\" and that had stopped being true: nltk 3.10 fixes it along with\nPYSEC-2026-3582/3583/3584. Evidence: with the ignore removed and nltk upgraded,\npip-audit reports \"No known vulnerabilities found, 6 ignored\". nltk is now\nPINNED in the local audit-env sync instead, per the script's own policy that\nfixable deps are pinned rather than ignored. A dead ignore in a security gate is\nworse than the advisory it names -- it is a false statement about what was\nchecked, and it suppresses the NEXT advisory for that package.\n\nCVE-2025-69872 (diskcache) -- STAYS, and this was tested rather than assumed.\nDropping it does not produce a clean audit: it surfaces PYSEC-2026-2447, which\nis the same advisory under its PYSEC id (pip-audit resolves the alias, which is\nwhy only one of the two ids ever appears), and it STILL has no fix version.\ndiskcache reaches this box via CairoSVG/safety and is not a dependency of any\nshipped hypergumbo package.\n\nTHE ITEM STAYS OPEN because its trigger is still live for the remaining\ncarve-out: when diskcache ships a fix, the ignore should be dropped and the dep\npinned. The comment block in release-check now records both results inline, so a\nfuture reader does not have to re-derive them."  # 789d
- op: discuss  # fbcb
  at: '2026-09-25T21:38:01Z'  # fbcb
  by: agent  # fbcb
  actor: jgstern_agent  # fbcb
  clock: 4  # fbcb
  nonce: fbcb  # fbcb
  message: "RE-AUDITED 2026-09-25 for the 8.1.0 cut, with pip-audit -f json and NO ignores (PR #1211).\n\nBefore: the gate would have failed on anyio 4.12.1 (CVE-2026-63374/-64847, fix 4.14.2), transformers 5.0.0 (PYSEC-2026-3929, fix 5.10.0), and nltk 3.10.3 (PYSEC-2026-3740, NO fix). torch CVE-2025-3000 / PYSEC-2025-194 had gained a fix (2.13.0).\n\nDone: upgraded to torch 2.14.0, transformers 5.17.0, anyio 4.15.1 (sentence-transformers 5.2.2 allows transformers <6, torch >=1.11, and imports on the new stack). release-check's sync step now upgrades those three when installed. nltk PYSEC-2026-3740 added as a local-only no-fix carve-out beside diskcache (nltk arrives via safety; no hypergumbo package depends on it). Result: an audit with no ignores reports ONLY diskcache PYSEC-2026-2447 and nltk PYSEC-2026-3740; the gate's exact command exits 0.\n\nSTILL OPEN, and now the item's main residue: on torch 2.14 / transformers 5.17 the whole ML-stack carve-out is DEAD -- PYSEC-2025-189..197, -210, -211..218, PYSEC-2026-139, PYSEC-2026-2289/2290, CVE-2025-3000, CVE-2026-4372, and joblib PYSEC-2024-277 no longer fire. They sit in four lockstep lists (scripts/release-check, .github/workflows/ci.yml, release.yml, .woodpecker/woodpecker.yml). No CI audit env installs torch/transformers, so they are inert in CI, but in release-check they would hide the NEXT torch/transformers advisory. Retire them in all four lists, with ci.yml's rationale block. Also: ci.yml's comment says transformers 5.3.0 is \"transitively capped by sentence-transformers~=5.2.2\" -- false; 5.2.2 requires transformers<6.0.0,>=4.41.0 (verified via importlib.metadata). Close when the carve-out is retired and diskcache + nltk have fixes or are otherwise gone."  # fbcb

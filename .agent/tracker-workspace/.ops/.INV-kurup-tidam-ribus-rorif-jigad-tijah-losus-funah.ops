- op: create  # d09f
  at: '2026-05-30T05:41:07Z'  # d09f
  by: agent  # d09f
  actor: jgstern_agent  # d09f
  clock: 1  # d09f
  nonce: d09f  # d09f
  data:  # d09f
    kind: invariant  # d09f
    title: "META: Identifier-bearing fields emit non-canonical formats from several emission paths"  # d09f
    status: violated  # d09f
    priority: 2  # d09f
    parent:  # d09f
    tags:  # d09f
    - dogfood_corpus_20260528  # d09f
    - dogfood_umbrella  # d09f
    - dogfood_severity_moderate  # d09f
    isbefore: []  # d09f
    duplicate_of: []  # d09f
    not_duplicate_of: []  # d09f
    pr_ref:  # d09f
    description: "**Subject:** Identifier-bearing fields emit non-canonical formats from several emission paths\n\n**Mechanic:** Several identifier-bearing fields declare schemes (a fingerprint scheme prefix, a stable-id hash prefix, a structured 4-colon shape for node IDs) but multiple emitter paths bypass the canonical factories that enforce those schemes. Result: the same field carries values in multiple incompatible formats simultaneously — some with the declared prefix, some without; some matching the structured-ID shape, some emitting raw hex, bare names, or extra-colon variants. Affected paths include language-specific analyzer paths for certain ecosystems, linker-emitted call-site nodes, and dependency-declaration emitters. The non-canonical fraction is small relative to total record count, but the pattern is consistent: emission boundaries do not validate against the declared scheme.\n\n**Expectation:** Every identifier value should conform to the field's declared scheme, with format enforcement at the emission boundary. A field declared as `<scheme>:<hash>` should always carry the prefix; a field declared as `<lang>:<path>:<span>:<name>:<kind>` should always have that colon count, with internal colons in any component escaped.\n\n**Scope:** Affects multiple identifier-bearing fields. Each individual field's non-canonical fraction is a small minority — well under one percent of records — but the non-canonical records are not random; they concentrate in linker-emitted nodes, externals from dependency manifests, and a handful of language-specific paths.\n\n**Exposure:** Behavior-map node-level identifier fields, queried by any consumer joining records by identity.\n\n**Consequence:** Consumers pattern-matching on a scheme prefix miss the non-canonical instances; joins across runs or analyses drop or misalign the affected records; schema validation cannot rely on declared schemes being honored, so downstream tooling must do format-detection at every join.\n\nSee child member items for per-instance technical detail and per-symptom repro / fix scope."  # d09f
    fields:  # d09f
      dogfood_anon_id: "ITEM-803289"  # d09f
      statement: "Every identifier value should conform to the field's declared scheme, with format enforcement at the emission boundary. A field declared as `<scheme>:<hash>` should always carry the prefix; a field declared as `<lang>:<path>:<span>:<name>:<kind>` should always have that colon count, with internal colons in any component escaped."  # d09f
      root_cause: "Several identifier-bearing fields declare schemes (a fingerprint scheme prefix, a stable-id hash prefix, a structured 4-colon shape for node IDs) but multiple emitter paths bypass the canonical factories that enforce those schemes. Result: the same field carries values in multiple incompatible formats simultaneously — some with the declared prefix, some without; some matching the structured-ID shape, some emitting raw hex, bare names, or extra-colon variants. Affected paths include language-specific analyzer paths for certain ecosystems, linker-emitted call-site nodes, and dependency-declaration emitters. The non-canonical fraction is small relative to total record count, but the pattern is consistent: emission boundaries do not validate against the declared scheme."  # d09f
- op: discuss  # bd61
  at: '2026-05-30T09:06:42Z'  # bd61
  by: agent  # bd61
  actor: jgstern_agent  # bd61
  clock: 2  # bd61
  nonce: bd61  # bd61
  message: "Cross-ref backfill (2026-05-30, deeper-issue investigation): umbrella enrolled 5 members; three further active items address the same emission-format mechanic and should at minimum be cross-referenced (folded as members where the format-drift framing fits): INV-bazij (stable_id 60% collision, P0 — peer of member INV-hunup), INV-zudob (structurally-identical cross-module residue, pending_validation), INV-lidul (config_fingerprint constant across 84 runs — identifier-emitter-bypass). Deeper hypothesis: INV-kurup + INV-numat share a missing-architectural-layer root — \"no schema-vs-emission validator gate\" — and may merit a super-META once one of them sees a structural fix."  # bd61
- op: update  # f789
  at: '2026-05-30T09:50:48Z'  # f789
  by: agent  # f789
  actor: jgstern_agent  # f789
  clock: 3  # f789
  nonce: f789  # f789
  set: {}  # f789
  add:  # f789
    tags: [super_meta_no_validator_stage]  # f789
- op: discuss  # 2fdb
  at: '2026-06-02T06:13:52Z'  # 2fdb
  by: agent  # 2fdb
  actor: jgstern_agent  # 2fdb
  clock: 4  # 2fdb
  nonce: 2fdb  # 2fdb
  message: "Pass-27 INV-kurup new face — six sibling hashed-identifier fields use four distinct prefix conventions plus one absent-field convention.\n\nPass-27 surveyed every top-level identifier-bearing field that carries a content-addressed (hash-based) value. Results:\n\n| Field | Format | Sample |\n|---|---|---|\n| node.stable_id | `sha256:<16hex>` (1 colon) | `sha256:fc4f52d59df5d3a5` |\n| edge.id | `edge:sha256:<16hex>` (2 colons) | `edge:sha256:17ae7e43bd3ca98c` |\n| edge.edge_key | `edgekey:sha256:<16hex>` (2 colons) | `edgekey:sha256:4ca7645b4389a13d` |\n| feature.id | `sha256:<16hex>` (1 colon) | `sha256:71d09c5dcdf3a4a8` |\n| usage_context.id | `usage:sha256:<16hex>` (2 colons) | `usage:sha256:20d859135eb74337` |\n| entrypoint.id | **(field absent)** | — |\n\nSix sibling \"stable hashed identifier of this record\" fields, three concrete prefix conventions (`sha256:`, `<recordkind>:sha256:`, `usage:sha256:`) plus the entrypoint exception of having no id field at all. Consumers writing dedup/cross-ref code must learn which surface uses which convention.\n\nQuantitative:\n- 109915 edges all use `edge:sha256:<16hex>` for id (100%)\n- 109915 edges all use `edgekey:sha256:<16hex>` for edge_key (100%)\n- 34117 nodes use `sha256:<16hex>` for stable_id (99.6% — 136 None per pass-26 F26.10 / F27.10 below)\n- 16 features all use bare `sha256:<16hex>` for id\n- 8 usage_contexts all use `usage:sha256:<16hex>`\n- 104 entrypoints have no id field at all (0/104)\n\nINV-kurup's existing statement covers \"raw hex, bare names, or extra-colon variants\" abstractly; this is a clean per-field tabulation showing the canonical-hashed-id family alone has four conventions. The fix path could be either schema-level (canonicalize all to `<record>:sha256:<16hex>`) or document-only (declare each surface's convention in spec). Note: edge.edge_key and edge.id are TWO hashed-ids on the same edge record — pass-22's earlier WI-niboh observation (edge.id vs edge.edge_key naming-convention divergence from Symbol's id/stable_id pair) is now refined: not just naming-axis-uniformity but also prefix-convention divergence (edge.id uses `edge:` while edge.edge_key uses `edgekey:`).\n\nProbe artifact: /tmp/pass27_probe2.py + /tmp/pass27_probe5.py / outputs at /tmp/pass27_output[2,5].log.\n\nNo status change; pass-27 just enriches the quantitative face."  # 2fdb
- op: discuss  # 4c0e
  at: '2026-06-02T17:32:57Z'  # 4c0e
  by: agent  # 4c0e
  actor: jgstern_agent  # 4c0e
  clock: 5  # 4c0e
  nonce: 4c0e  # 4c0e
  message: "Pass-35 F35.A1 — doc/config analyzer id-format violations: 3 distinct sub-mechanisms (4321 total when --include-docs activates these writers).\n\nINV-kurup scope is \"META: Identifier-bearing fields emit non-canonical formats from several emission sites.\" Pass-35 finds doc/config analyzers (markdown + gitignore) constitute a NEW emission-site category at significant scale — gated behind `--include-docs` so they don't fire in default self-analysis, but when activated produce 4321 Symbol.id + Symbol.stable_id format violations.\n\nSubstrate `/tmp/p35_docs.json` (hypergumbo run --include-docs) validation_report breakdown:\n\n**Mechanism 1 — gitignore-pattern reuses Symbol.id as Symbol.stable_id (2667 cases).** Symbol.stable_id violations on records like:\n```\ngitignore:.ci/.gitignore:2-2:full-suite.log:pattern\ngitignore:.ci/.gitignore:5-5:.smart-test.lock:pattern\ngitignore:.ci/.gitignore:6-6:.smart-test.lock.pid:pattern\n```\nValidator message: `composite_no_sha_prefix (colon_count=4). Use a make_*_stable_id factory from analyze/base.py (or _short_sha256 directly) rather than constructing stable_ids with f-strings or raw hashlib.sha256(...).hexdigest().` The gitignore analyzer is writing the literal Symbol.id string AS the stable_id, not the sha256-hash form. ADR-0034's id-construction discipline applies; the gitignore-pattern emitter at the analyzer level bypasses `make_typed_stable_id`.\n\n**Mechanism 2 — markdown section ID includes section-title literal colons (825 + 825 cases).** Symbol.id and Symbol.stable_id violations on records like:\n```\nmarkdown:AGENTS.md:62-63:Priority Queues::section\nmarkdown:AGENTS.md:65-66:BROAD Mode Priority Queue::section\nmarkdown:AGENTS.md:68-69:DEEP Mode Priority Queue::section\n```\nValidator message: `double_colon_separator (INV-sadiv). Use make_symbol_id(...) from analyze/base.py rather than constructing IDs with f-strings.` The section title contains a `:` which gets concatenated literally → 5 colons total. The markdown analyzer needs name-segment sanitization.\n\n**Mechanism 3 — markdown code_block uses 'code' literal as span segment (584 cases).** Symbol.id violations on records where span position has the literal word `code` instead of `start-end` line range:\nValidator message: `malformed_span_segment ('code'). Use make_symbol_id(...) from analyze/base.py...`. The code_block emitter constructs IDs as `markdown:file.md:code:NAME:code_block` instead of `markdown:file.md:start-end:NAME:code_block`.\n\n**Cross-reference with closure campaign.** The INV-sugat closure campaign (Phase 5) shipped ADR-0034 + the `make_symbol_id` / `make_typed_stable_id` factories AND migrated 10 mainstream code analyzers. Doc/config analyzers (markdown, gitignore, plus likely toml/yaml/json analyzers that ship config-kind nodes — pass-35 didn't sweep beyond markdown + gitignore) were NOT migrated. The mainstream-code-analyzer Phase 6 sweep closed Symbol.id violations on the default substrate but doc/config code-paths use their own pre-factory ID-construction.\n\n**Fix-path candidate.** Mirror Phase 6's mainstream-analyzer sweep on the doc/config analyzer set:\n- markdown analyzer: switch to `make_symbol_id` for all 3 sub-mechanisms; sanitize name segment to escape colons; replace 'code' literal with `start-end` line span\n- gitignore analyzer: switch to `make_*_stable_id` factory call instead of reusing Symbol.id\n- audit toml/yaml/json/markdown-link analyzers for parallel violations (pass-35 didn't sweep these)\n\nDirection is concretely actionable — 3 sub-mechanisms × 2 analyzers = 6 targeted code changes likely close all 4321 violations.\n\n**Probe artifacts:** `/tmp/p35_docs.json` (substrate); `/tmp/p35_probeA.py` + `/tmp/p35_outputA.log` (violation classification). No status change."  # 4c0e
- op: discuss  # e9d8
  at: '2026-06-02T20:45:09Z'  # e9d8
  by: agent  # e9d8
  actor: jgstern_agent  # e9d8
  clock: 6  # e9d8
  nonce: e9d8  # e9d8
  message: "Pass 36 (continuing 06022026_1622) cross-codebase reach + decomposition of pass-35 F35.A1.\n\nF36.C2 (gitignore writer activation is grammar-files-present-conditional): pass-35 F35.A1's 4321 violations (markdown 4243 + gitignore 78) decompose differently across codebases. On tracker-pkg with --include-docs, 102/102 violations are markdown id_format only. The 78 gitignore violations from root are ABSENT — tracker-pkg has no .gitignore files in its scope, so gitignore writer doesn't fire. Markdown-violation ratio 102/4243 = 2.4% vs substrate-size ratio 5055/37816 = 13.4% suggests markdown writer fires more file-count-proportionally than gitignore writer. F35.A1's pass-35 fix-path ('mirror INV-sugat Phase 6 mainstream-analyzer sweep on doc/config analyzer set') is reaffirmed cross-codebase: same 3 sub-mechanisms (gitignore id-as-stable-id reuse, markdown section unsanitized colons, markdown code_block literal 'code' span segment) — markdown sweep is universal coverage, gitignore sweep affects smaller subset of codebases."  # e9d8
- op: discuss  # fac8
  at: '2026-06-11T04:25:50Z'  # fac8
  by: agent  # fac8
  actor: jgstern_agent  # fac8
  clock: 7  # fac8
  nonce: fac8  # fac8
  message: "2026-06-10 design-interview ruling (ADR-0036, merged in PR #4181): a single canonical node.id grammar now exists — anchored 5-slot, colon-free lang/span/name/kind, colon-tolerant path, parse from both ends, enumerated sentinels, kind slot == node.kind always. The non-canonical emission paths this META tracks are now measurable against one contract. Stays violated until the per-emitter conformance fixes land; re-validate then."  # fac8
- op: discuss  # 6172
  at: '2026-06-18T07:08:38Z'  # 6172
  by: agent  # 6172
  actor: jgstern_agent  # 6172
  clock: 8  # 6172
  nonce: '6172'  # 6172
  message: "Dominant fix WI-jopil (:unresolved->:external_symbol id-slot fold, ~458 nodes = bulk of this umbrella's id_format count) DEFERRED to Wave 3 item 10 (2026-06-18). Correction: ADR-0036 is Accepted — NOT a blocker (earlier 'blocked on ADR-0036' framing was wrong). INV-kurup stays violated; closes in Wave 5 (§Wave 5 item 2 + vocab item 6 zero-axis-violation cohort) after WI-jopil lands in Wave 3."  # 6172
- op: discuss  # 2990
  at: '2026-07-02T19:51:02Z'  # 2990
  by: agent  # 2990
  actor: jgstern_agent  # 2990
  clock: 9  # 2990
  nonce: '2990'  # 2990
  message: "ADR-0036 node.id member — sequencing set 2026-07-02 (hold lifted, standalone, patch bump, Option B). Order: Phase 0 prep (register meta.reference_syntax, fix slot-order doc comments, freeze baseline) -> Phase 1 core atomic train (WI-pubiv + WI-jopil, one PR) -> fast-follows (WI-kugaj, WI-zugob, npm_package/tsconfig) -> enforcement (WI-bovoh regex-scope gap, WI-popak advisory->error + ratchet baseline to 0, WI-dipim Ruling-3 sentinel, WI-vodin AST linter) -> closure (INV-dulah, then this umbrella's node.id member; other identifier-field members e.g. Edge.id stay scoped-open). isbefore edges wired on the fold items."  # 2990
- op: update  # 9397
  at: '2026-09-06T10:20:34Z'  # 9397
  by: agent  # 9397
  actor: jgstern_agent  # 9397
  clock: 10  # 9397
  nonce: '9397'  # 9397
  set: {}  # 9397
  add:  # 9397
    isbefore: [INV-linub-nokij-gibim-rovok-moros-gural-domip-rimof]  # 9397

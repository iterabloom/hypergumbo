- op: create  # 8696
  at: '2026-06-01T23:07:21Z'  # 8696
  by: agent  # 8696
  actor: jgstern_agent  # 8696
  clock: 1  # 8696
  nonce: '8696'  # 8696
  data:  # 8696
    kind: work_item  # 8696
    title: "INV-lidul/gizik/nihug tail: wire pass_version + config_fingerprint + toolchain on override-analyze() analyzers"  # 8696
    status: todo_hard  # 8696
    priority: 2  # 8696
    parent:  # 8696
    tags:  # 8696
    - analysis_runs  # 8696
    - analysis_quality  # 8696
    - analyzer  # 8696
    - writer_contract  # 8696
    isbefore: []  # 8696
    duplicate_of: []  # 8696
    not_duplicate_of: []  # 8696
    pr_ref:  # 8696
    description: "Phase 6 PR2 (commit 98533c0fff) closed INV-lidul / INV-gizik / INV-nihug for the analyzers that inherit `TreeSitterAnalyzer._analyze_body` — that base-class method now auto-stamps `pass_version`, derives a per-class `config_fingerprint`, and extends `toolchain` with grammar metadata.\n\nBut ~14 analyzers override `analyze()` entirely (go, csharp, java, python, html, javascript, xml, yaml_ansible, json, toml, make, manifest_targets, play-routes, plus enclosure-linker variants — see commit 98533c0fff's deferred-items section). They bypass `_analyze_body` and therefore don't pick up the auto-stamping. Self-analysis still shows their `AnalysisRun` records with empty `pass_version` and the default `config_fingerprint` (`sha256:44136fa355b3678a` from `_default_config_fingerprint()`).\n\nThe fix is per-analyzer: at each override-`analyze()` site, either (a) call the base class's auto-stamping helpers explicitly before constructing the AnalysisRun, or (b) refactor the override to delegate to `_analyze_body` for the metadata-stamping portion.\n\nThis is a mechanical sweep across 14 files. The validator's writer-contract check is currently silent on these because the gap exists pre-validate_ir (the runs reach validate_ir already filled in with default values, not None), but the broken-cache-key concern (every analyzer using the same `config_fingerprint` means cache keys collapse across configurations) is real.\n\nCloses the tail of INV-lidul / INV-gizik / INV-nihug."  # 8696
    fields: {}  # 8696
- op: update  # 2436
  at: '2026-06-02T00:58:13Z'  # 2436
  by: agent  # 2436
  actor: jgstern_agent  # 2436
  clock: 2  # 2436
  nonce: '2436'  # 2436
  set:  # 2436
    parent: INV-luhur-sital-lonar-tagiv-kuzab-mokol-jikot-fosug  # 2436
- op: discuss  # 5f60
  at: '2026-06-02T00:58:13Z'  # 5f60
  by: agent  # 5f60
  actor: jgstern_agent  # 5f60
  clock: 3  # 5f60
  nonce: 5f60  # 5f60
  message: "Investigated 2026-06-01 for cluster context. Parent set to INV-luhur (P1 violated, writer-contract META) because it spans three direct children: INV-lidul (config_fingerprint, still violated), INV-gizik (per-pass timer, still violated), INV-nihug (skipped_passes mirror, now pending_validation). Phase 6 PR2 (98533c0fff) closed these for analyzers inheriting TreeSitterAnalyzer._analyze_body via auto-stamping; ~14 analyzers that override analyze() entirely (go, csharp, java, python, html, javascript, xml, yaml_ansible, json, toml, make, manifest_targets, play-routes, plus enclosure-linker variants) bypass _analyze_body and remain affected. Doesn't duplicate any preexisting row — it's the override-path tail of three sibling INV-luhur children."  # 5f60
- op: discuss  # 94e5
  at: '2026-06-02T04:38:34Z'  # 94e5
  by: agent  # 94e5
  actor: jgstern_agent  # 94e5
  clock: 4  # 94e5
  nonce: '94e5'  # 94e5
  message: "**Pass-23 confirmation observation.** Pass 23 explicitly enumerated AR `pass_version=None`. Result: exactly 14/84 ARs (16.7%) have pass_version=None, and they are the exact override-analyze() set listed in this WI's description:\n\n```\n['html', 'go', 'csharp', 'javascript', 'play-routes', 'yaml_ansible',\n 'xml', 'json', 'java', 'manifest_targets', 'toml', 'make', 'python',\n 'enclosure-linker']\n```\n\nWI-mipul's framing verified empirically. No scope change.\n\n**Adjacent observation (filed as separate INV-luhur discussion, F23.2):** `AnalysisRun.toolchain.name='python'` for ALL 84 ARs, including grammar-based passes like rust (which DO have `grammar_module='tree_sitter_rust'` populated). The toolchain dict IS populated on every AR (so it's not a missing-extension gap that WI-mipul covers); but the `name` field carries the runtime name, not the analyzer language. Different concern; recorded on INV-luhur to keep WI-mipul scope tight.\n\n**Lab notebook entry:** `~/hypergumbo_lab_notebook/notebookjournal_06022026_0010_pass23.md`."  # 94e5
- op: discuss  # 4179
  at: '2026-06-05T06:00:12Z'  # 4179
  by: agent  # 4179
  actor: jgstern_agent  # 4179
  clock: 5  # 4179
  nonce: '4179'  # 4179
  message: "[+ tranch_03: anon-card-tranch03-001 F41.A1 — pass_version empty 14/84 incl python (folded as duplicate; blind severity 4; new row WI-mifok wont_do)]"  # 4179

- op: create  # f8d2
  at: '2026-06-28T02:37:35Z'  # f8d2
  by: agent  # f8d2
  actor: jgstern_agent  # f8d2
  clock: 1  # f8d2
  nonce: f8d2  # f8d2
  data:  # f8d2
    kind: work_item  # f8d2
    title: "confidence:F1 continuation: wire derive_confidence (multimodal is_resolved + Edge.create default flip + producer migration + range validation)"  # f8d2
    status: todo_hard  # f8d2
    priority: 1  # f8d2
    parent:  # f8d2
    tags:  # f8d2
    - analysis_quality  # f8d2
    isbefore: []  # f8d2
    duplicate_of: []  # f8d2
    not_duplicate_of: []  # f8d2
    pr_ref:  # f8d2
    description: "Continues confidence:F1 (ADR-0039) after the first slice (derive_confidence reader + base_confidence seed for 8 single-valued pathways; merged selfh/dev a4e5117918). REMAINING, high-blast-radius, DECISION-GATED steps: (1) Multimodal pathways conditioned on is_resolved -- ast_call_direct (resolved 0.85 / unresolved 0.50) and ast_call (resolved 0.85 / unresolved 0.40); ADR-0039 fork: two base fields vs base+penalty (recommend two explicit fields -- empirical penalties differ per type). (2) Flip the Edge.create flat ~0.85 default to route through derive_confidence when no explicit confidence passed (~44k edges). (3) Add Edge.confidence_source discriminator (bounded-enum evidence_derived|emitter_constant|composite, ADR-0024 lightweight axis) + the >100-edge-constant property guard, enabling INCREMENTAL producer migration (no flag-day). (4) Range validation (each edge within its evidence-type band) into the ADR-0033 validator + ADR-0043 finalize sub-step 10 (advisory for composite until F2). OPEN FORKS (need human ruling, per scope wf_84ea50b4-043): (a) DOMINANT -- proceed before the full vocab:F1 axis-gating wave (strategy names it the 'true prerequisite') or wait; the first slice was fold-safe but the default-flip/migration are where vocab:F1 incompleteness could re-key edges. (b) per-type base/range NUMBERS (hybrid: modal where in-band, principled bands for multimodal). (c) bump confidence_model hypergumbo-evidence-v1 -> v2 on the default flip. F2 (ranking/detection separation) owns type_hierarchy dampener + entrypoint boosts + the composite-vs-pure decision; naming_convention=1.0 ceiling-breach is a containment.py producer fix. Advances INV-suvil."  # f8d2
    fields: {}  # f8d2
- op: discuss  # 386e
  at: '2026-06-28T04:39:35Z'  # 386e
  by: agent  # 386e
  actor: jgstern_agent  # 386e
  clock: 2  # 386e
  nonce: 386e  # 386e
  message: "confidence:F1 multimodal table DONE (merged selfh/dev 51a4a2fc2a). Step 1 of this WI complete: added base_confidence_unresolved + is_resolved conditioning for the 2 high-volume call types (ast_call_direct 0.85/0.50, ast_call 0.85/0.40), verified bimodal on the live is_resolved x confidence cross-tab. The derivation TABLE now covers the highest-volume edge types (single-valued + multimodal) -- still not wired, no published value changes. REMAINING: (2) Edge.create default flip (change the flat 0.85 confidence default to a sentinel so Edge.create derives when no explicit confidence is passed); (3) confidence_source discriminator + per-producer migration; (4) range validation. Re-scoping note: the seeded types' producers currently pass EXPLICIT values that already match the registry, so the flip+migration is mostly de-duplication with minor-cohort normalization (e.g. ast_call_direct's 292 edges at 0.7 -> 0.85), NOT a 44k-edge value overhaul. confidence_model v1->v2 bump rides the flip."  # 386e
- op: discuss  # ea52
  at: '2026-06-28T07:06:40Z'  # ea52
  by: agent  # ea52
  actor: jgstern_agent  # ea52
  clock: 3  # ea52
  nonce: ea52  # ea52
  message: "confidence:F1 default-flip DONE (merged selfh/dev c596e8ed3c). Edge.create's flat 0.85 confidence default is now a sentinel: when a producer omits confidence, create() derives via derive_confidence(evidence_type, is_resolved=...) + falls back to 0.85 for unseeded pathways. Near-no-op confirmed (broad suite 15488 passed, zero fallout; the dominant omit case ast_call_direct-resolved derives 0.85 = old default). Steps 1-2 of this WI complete: the derivation TABLE (single-valued + multimodal) + the Edge.create WIRING both exist. REMAINING: (3) per-producer migration -- drop explicit confidence= so ~575 sites derive (this is the INV-suvil-closing grind, multi-PR, mostly no-op since seeded values match producers' explicit ones); (4) range-validation gate; + confidence_model v1->v2 bump (rides the first value-changing migration). (Process note: the flush's first auto-merge was a WI-bunag false positive on the commit-body prose 'migration that closes INV-suvil'; amended to 'closes the invariant' and re-flushed cleanly.)"  # ea52

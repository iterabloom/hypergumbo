- op: create  # bfba
  at: '2026-04-24T08:28:32Z'  # bfba
  by: agent  # bfba
  actor: jgstern_agent  # bfba
  clock: 1  # bfba
  nonce: bfba  # bfba
  data:  # bfba
    kind: invariant  # bfba
    title: "Every catalog with status=complete MUST declare stdlib_provenance whose source_url hostname suffix-matches ALLOWED_PROVENANCE_HOSTNAME_SUFFIXES"  # bfba
    status: pending_validation  # bfba
    priority: 1  # bfba
    parent:  # bfba
    tags:  # bfba
    - analysis_quality  # bfba
    isbefore: []  # bfba
    duplicate_of: []  # bfba
    not_duplicate_of: []  # bfba
    pr_ref:  # bfba
    description: "Plan C, PR B governance invariant."  # bfba
    fields:  # bfba
      statement: "For every io_primitives/<lang>.yaml whose top-level status is 'complete' (explicit OR defaulted when the field is absent), the file MUST declare stdlib_provenance.source_url; the URL MUST use the https scheme; and its hostname MUST suffix-match ALLOWED_PROVENANCE_HOSTNAME_SUFFIXES (defined as a frozenset in io_boundary.py). Validated by _validate_catalog_dict() at load time — violations raise ValueError before any edge matching runs."  # bfba
      root_cause: "The catalog principle (membership = stdlib, absence = probably-third-party-not-certain) only holds if completeness claims are auditable. Without provenance + allowlist, a typo or unofficial source could mislead consumers about what 'complete' means; without hard-error validation, the rule degrades to an honor-code comment that erodes over time. Allowlist additions go through PR review with justification (same governance shape as ALLOWED_WEBSITES.md)."  # bfba
- op: discuss  # 664a
  at: '2026-04-25T10:53:53Z'  # 664a
  by: agent  # 664a
  actor: jgstern_agent  # 664a
  clock: 2  # 664a
  nonce: 664a  # 664a
  message: "iter-10 DEEP bakeoff cycle (cohort-001 alertmanager+kafka+prometheus, post-WI-ramuv merge) ran clean and produced 3 GOOD verdicts with 0 concerns. No regression surfaced at the diagnose-threshold level. Per-claim reflect-aggregate validation is pending (requires LLM agent invocations per-repo)."  # 664a
- op: update  # 8817
  at: '2026-05-05T07:37:05Z'  # 8817
  by: agent  # 8817
  actor: jgstern_agent  # 8817
  clock: 3  # 8817
  nonce: '8817'  # 8817
  set:  # 8817
    priority: 2  # 8817
- op: discuss  # 1dfb
  at: '2026-05-30T09:51:52Z'  # 1dfb
  by: agent  # 1dfb
  actor: jgstern_agent  # 1dfb
  clock: 4  # 1dfb
  nonce: 1dfb  # 1dfb
  message: "Cross-ref backfill (2026-05-30, linked-pile investigation): 3 of 14 catalogs currently `status: complete` (python, rust, erlang); all 3 declare `stdlib_provenance.source_url` pointing at allowlisted hostnames. 11 remain `in_progress` and are correctly exempt from the invariant. The `pending_validation` status has been static since 2026-04-25; consider promoting to `satisfied` if the `_validate_catalog_dict()` enforcement path has been load-tested against the 3 complete catalogs in CI."  # 1dfb
- op: update  # 9a48
  at: '2026-06-11T17:21:39Z'  # 9a48
  by: agent  # 9a48
  actor: jgstern_agent  # 9a48
  clock: 5  # 9a48
  nonce: 9a48  # 9a48
  set:  # 9a48
    status: satisfied  # 9a48
- op: discuss  # c019
  at: '2026-06-11T17:21:39Z'  # c019
  by: agent  # c019
  actor: jgstern_agent  # c019
  clock: 6  # c019
  nonce: c019  # c019
  message: "[Wave-0 hygiene re-verify] Fixed and verified in current code. Hard-error enforcement implemented in io_boundary.py:156-202 (_validate_catalog_dict function) which raises ValueError for: (1) missing stdlib_provenance when status=complete (line 177-182), (2) non-HTTPS URLs (line 185-189), (3) hostnames not suffix-matching ALLOWED_PROVENANCE_HOSTNAME_SUFFIXES frozenset (line 191-202). Enforcement called at load time from IoBoundaryCatalog._from_dict at line 610. Status defaults to \"complete\" at line 603, so all catalogs are validated unless explicitly set to \"in_progress\". Verified all 3 complete catalogs (python, rust, erlang) have valid HTTPS URLs matching allowlist. Load-tested by passing CI test suite (293 tests including direct catalog loads of python and rust). No remaining violations in current tree."  # c019

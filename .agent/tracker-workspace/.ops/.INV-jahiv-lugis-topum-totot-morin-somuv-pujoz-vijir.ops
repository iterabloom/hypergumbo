- op: create  # 40ad
  at: '2026-05-30T09:48:39Z'  # 40ad
  by: agent  # 40ad
  actor: jgstern_agent  # 40ad
  clock: 1  # 40ad
  nonce: 40ad  # 40ad
  data:  # 40ad
    kind: invariant  # 40ad
    title: "META: Analyzer parity — declared Symbol/Edge fields populated unevenly across languages"  # 40ad
    status: violated  # 40ad
    priority: 1  # 40ad
    parent:  # 40ad
    tags:  # 40ad
    - dogfood_corpus_20260528  # 40ad
    - dogfood_umbrella  # 40ad
    - dogfood_severity_significant  # 40ad
    - analyzer_parity  # 40ad
    isbefore: []  # 40ad
    duplicate_of: []  # 40ad
    not_duplicate_of: []  # 40ad
    pr_ref:  # 40ad
    description: "**Origin.** Filed 2026-05-30 from the linked-pile L7 investigation (notebookjournal_05302026_0522_linked_L7_analyzer_framework_dcm.md sub-pile A). Standalone META, peer of INV-luhur (writer contract — bm.json meta-layer).\n\n**The shape.** Three active items describe the same anti-pattern: a dataclass field on `Symbol` / `Edge` is declared in the schema but populated unevenly across language analyzers. Python's analyzer populates the field; non-Python analyzers don't, or populate with wrong-shape values. The schema reads as a uniform contract; the data delivers a Python-only or sparse-coverage reality.\n\n**Distinct from INV-luhur** (writer contract for bm.json meta-layer fields like AnalysisRun, metrics, limits, config_fingerprint). This META is about per-analyzer Symbol/Edge field-population coverage parity, not meta-layer writers.\n\n**Members (3).** INV-golap (JavaScript analyzer populates Symbol.signature on 0/10 function|method nodes; TypeScript 25/26; may be partially fixed since pass-9 — re-measure pending), INV-loguk (non-Python analyzers don't populate cyclomatic_complexity; bash/rust LOC-only; only `py.py:1655` defines CC), INV-gilok (Solidity analyzer captures source-text string as the identifier value — `solidity.py:355-369` unconditional `_extract_import_aliases` with no `import_directive` guard).\n\n**Fix shape.** Per-analyzer parity audit: for each declared `Symbol` and `Edge` field, identify the canonical computation (the one Python does correctly) and apply analogous logic in non-Python analyzers. WI-zisov polyglot fixture is the test substrate for verification; add isbefore when L7 work begins.\n\n**Parent:** null (top-level META, peer of INV-luhur)."  # 40ad
    fields:  # 40ad
      statement: "Every Symbol and Edge field declared in the schema must be populated equivalently across all language analyzers; no field may be Python-only or sparse-coverage when the schema presents it as a uniform contract."  # 40ad
      root_cause: "No analyzer-parity audit gate. Python analyzer populates Symbol/Edge fields; non-Python analyzers don't, or do so with wrong-shape values. Manifestations: JavaScript Symbol.signature 0/10 (INV-golap, may be partially fixed since pass-9 — re-measure), non-Python analyzers don't populate cyclomatic_complexity; bash/rust LOC-only (INV-loguk), Solidity analyzer captures source-text-string as identifier value (INV-gilok, solidity.py:355-369). WI-zisov polyglot fixture is the test substrate."  # 40ad
- op: update  # c357
  at: '2026-06-01T23:14:59Z'  # c357
  by: agent  # c357
  actor: jgstern_agent  # c357
  clock: 2  # c357
  nonce: c357  # c357
  set:  # c357
    status: satisfied  # c357
  add:  # c357
    tags: [awaits_bakeoff_validation]  # c357
- op: discuss  # 1f61
  at: '2026-06-01T23:14:59Z'  # 1f61
  by: agent  # 1f61
  actor: jgstern_agent  # 1f61
  clock: 3  # 1f61
  nonce: 1f61  # 1f61
  message: "Closed by Phase 4 PR1-PR5 (commits c17f639177, f4957fb175, c2cd3b9fb5, 9c4541c8c5, eab4e43aed). Populated 7 fields (lines_of_code, is_exported, signature, docstring, qualified_name, cyclomatic_complexity, plus shared symbol_introspection dispatcher) at every Symbol() emit site across the 10 mainstream-language analyzers. Writer-contract validator class self-analysis: 0 violations on the 10×7 grid. Positive evidence: writer_contract at 0."  # 1f61
- op: update  # f9bf
  at: '2026-06-08T18:32:21Z'  # f9bf
  by: agent  # f9bf
  actor: jgstern_agent  # f9bf
  clock: 4  # f9bf
  nonce: f9bf  # f9bf
  set:  # f9bf
    status: violated  # f9bf
- op: discuss  # 6403
  at: '2026-06-08T18:32:21Z'  # 6403
  by: agent  # 6403
  actor: jgstern_agent  # 6403
  clock: 5  # 6403
  nonce: '6403'  # 6403
  message: "Reopen: analyzer-parity is an ongoing invariant; current violations remain (WI-bujot is_exported None 100%; WI-fagab qualified_name misassigned; live run: bash cc 0/2, JS signature 2/5). (tracker-hygiene-sweep 2026-06-08T174504Z)"  # 6403
- op: discuss  # d425
  at: '2026-06-12T01:45:18Z'  # d425
  by: agent  # d425
  actor: jgstern_agent  # d425
  clock: 6  # d425
  nonce: d425  # d425
  message: "emission-parity:F1 / G2 gate landed (PR #4202): tests/test_emission_parity_matrix.py is the standing per-(language, field/edge-type) parity enforcement this META called for — the missing audit gate named in root_cause. It LOCKS current emission with strict-xfail ratchets on the documented holes (python qualified_name=WI-fagab, java imports=INV-gojit, non-python entrypoint concepts=WI-tosul) so parity cannot regress silently; each Wave-3 emitter fix XPASS-trips the gate. STATUS STAYS VIOLATED: reading live emission the gate ratifies the invariant is currently violated in those cells — it operationalizes/ratchets the META, it does not satisfy it. Open children INV-golap/INV-loguk/INV-gilok + WI-fagab/WI-bujot/INV-gojit remain."  # d425
- op: update  # f504
  at: '2026-06-22T01:48:06Z'  # f504
  by: agent  # f504
  actor: jgstern_agent  # f504
  clock: 7  # f504
  nonce: f504  # f504
  set: {}  # f504
  remove:  # f504
    tags: [awaits_bakeoff_validation]  # f504
- op: discuss  # 56bf
  at: '2026-06-22T01:48:06Z'  # 56bf
  by: agent  # 56bf
  actor: jgstern_agent  # 56bf
  clock: 8  # 56bf
  nonce: 56bf  # 56bf
  message: "Drain audit: E-misapplied. META analyzer-parity item; PR #4202 implemented the parity audit GATE (test_emission_parity_matrix.py), not a metric-moving fix. No quantitative bakeoff claim; underlying violations tracked by open children (INV-golap/loguk/gilok etc.) and violated status. Stripping misapplied tag."  # 56bf

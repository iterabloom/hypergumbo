- op: create  # deba
  at: '2026-06-06T10:35:07Z'  # deba
  by: agent  # deba
  actor: jgstern_agent  # deba
  clock: 1  # deba
  nonce: deba  # deba
  data:  # deba
    kind: work_item  # deba
    title: "profile emits dev_frameworks/framework_mode, absent from both spec and schema"  # deba
    status: todo_hard  # deba
    priority: 3  # deba
    parent:  # deba
    tags:  # deba
    - dogfood  # deba
    - dogfood_tranche_06  # deba
    isbefore: []  # deba
    duplicate_of: []  # deba
    not_duplicate_of: []  # deba
    pr_ref:  # deba
    description: "**Defect.** The profile block emits two framework fields (`dev_frameworks`, `framework_mode`) that exist in neither the spec nor the schema.\n\n**Evidence.** The profile producer writes dev_frameworks (e.g. ['pytest']) and framework_mode (e.g. 'auto') into every behavior map, produced by profile.py, but docs/hypergumbo-spec.md's profile example and the auto-generated docs/schema.json both omit them (0 doc grep hits). They are defined only in producer source.\n\n**Contract.** A schema that self-describes as auto-generated from the dataclasses should enumerate every field the producer actually emits, and the spec's profile description should match the emitted shape.\n\n**Consequence.** Schema-driven tooling treats real, always-present profile data as unexpected; a consumer building on the documented profile shape misses framework-mode and dev-framework signals entirely."  # deba
    fields: {}  # deba
- op: discuss  # 8e19
  at: '2026-06-07T06:42:50Z'  # 8e19
  by: agent  # 8e19
  actor: jgstern_agent  # 8e19
  clock: 2  # 8e19
  nonce: '8e19'  # 8e19
  message: "Dogfood enrichment (member F145.A1-a) — generator-staleness facet, reproduces on the frozen substrate. AnalysisRun.failed_files and pass_version are emitted on every analysis_runs entry but are absent from the AnalysisRun schema $def — the same auto-generated-schema-omits-emitted-fields mechanism this row already documents for profile (dev_frameworks/framework_mode). The schema self-describes as auto-generated from the dataclasses yet does not enumerate these AnalysisRun fields, so schema-driven tooling treats real, always-present per-run data as unexpected. Folded here as an additional instance of the same schema-generator staleness; the producer-level root (the dataclass->schema generator emitting contentless/incomplete defs) is filed separately as WI-galug, and the failed_files duplication/placement concern as WI-sizih. Cross-ref WI-galug, WI-sizih."  # 8e19
- op: discuss  # af40
  at: '2026-06-08T10:25:27Z'  # af40
  by: agent  # af40
  actor: jgstern_agent  # af40
  clock: 3  # af40
  nonce: af40  # af40
  message: "Re-confirms profile top-level disagreement: emitted-not-declared dev_frameworks/framework_mode (WI-sirir) + declared-not-emitted total_files/total_lines (WI-sizis). WI-sirir is the emitted-key face. (dogfood; blinded sev 3)"  # af40
- op: update  # 42a4
  at: '2026-07-30T16:48:47Z'  # 42a4
  by: agent  # 42a4
  actor: jgstern_agent  # 42a4
  clock: 4  # 42a4
  nonce: 42a4  # 42a4
  set:  # 42a4
    status: done  # 42a4
- op: discuss  # 4fda
  at: '2026-07-30T16:48:47Z'  # 4fda
  by: agent  # 4fda
  actor: jgstern_agent  # 4fda
  clock: 5  # 4fda
  nonce: 4fda  # 4fda
  message: "DONE — same PR as WI-sizis (the mirror-image half).\n\nprofile emits dev_frameworks, framework_mode and framework_evidence; the schema declared none of them. All three are now declared, with descriptions: dev_frameworks (frameworks seen only in dev/test deps, kept separate from the production 'frameworks' surface), framework_mode (how frameworks was resolved — manifest declaration vs import promotion), framework_evidence (WI-napuj's {framework: [node_ids]} join, omitted-when-empty per INV-virik).\n\nPurely additive documentation of already-emitted fields, so no SCHEMA_VERSION bump. Note this item and WI-sizis are the two directions of ONE disagreement between profile and its schema — declared-but-not-emitted vs emitted-but-not-declared — which is why they were fixed in one edit."  # 4fda

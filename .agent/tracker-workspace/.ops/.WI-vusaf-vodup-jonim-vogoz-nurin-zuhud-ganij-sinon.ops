- op: create  # 84ed
  at: '2026-05-28T16:18:58Z'  # 84ed
  by: agent  # 84ed
  actor: jgstern_agent  # 84ed
  clock: 1  # 84ed
  nonce: 84ed  # 84ed
  data:  # 84ed
    kind: work_item  # 84ed
    title: "compact accepts invalid flag values silently (max<min, coverage>1, negative limits)"  # 84ed
    status: todo_hard  # 84ed
    priority: 2  # 84ed
    parent:  # 84ed
    tags:  # 84ed
    - dogfooding  # 84ed
    - pass5  # 84ed
    - cli_ux  # 84ed
    - compact  # 84ed
    isbefore: []  # 84ed
    duplicate_of: []  # 84ed
    not_duplicate_of: []  # 84ed
    pr_ref:  # 84ed
    description: "Finding X (pass5): `hypergumbo compact` silently accepts numerically invalid flag values for --max-symbols, --min-symbols, --coverage. Inconsistent fallback behavior (max=0 returns 1 node, coverage outside [0,1] silently ignored).\n\nRepro (hypergumbo self-analysis @ dev 77a69627b8, 2026-05-28):\n\n  $ hypergumbo compact --input behavior_map.json --max-symbols 50 --min-symbols 100 --out cmp-mxlt-mn.json\n  # EXIT=0, nodes=50  (min=100 ignored; should error since min > max is incoherent)\n\n  $ hypergumbo compact --input behavior_map.json --max-symbols 100 --coverage 1.5 --out cmp-cov15.json\n  # EXIT=0, nodes=100  (coverage 1.5 silently accepted; range is documented as 0.0-1.0)\n\n  $ hypergumbo compact --input behavior_map.json --max-symbols 100 --coverage -0.5 --out cmp-covneg.json\n  # EXIT=0, nodes=100  (negative coverage silently accepted)\n\n  $ hypergumbo compact --input behavior_map.json --max-symbols 0 --out cmp-mx0.json\n  # EXIT=0, nodes=1   (asked for 0, got 1 — clamped to min without warning)\n\n  $ hypergumbo compact --input behavior_map.json --max-symbols -5 --out cmp-mxneg.json\n  # EXIT=0, nodes=1   (negative max silently clamped to 1, not rejected)\n\nNo warnings, no validation errors. Each silently picks an arbitrary fallback.\n\nSeverity: medium. `compact` ships in the documented LLM-targeting subcommand list. Letting users pass nonsense and silently producing an output that doesn't match their request is worse than failing fast — it produces compact maps that look like the user's request and aren't.\n\nSuggested fix: argparse type validators reject:\n  - --max-symbols < 1\n  - --min-symbols < 0\n  - --max-symbols < --min-symbols (post-parse check)\n  - --coverage outside [0.0, 1.0]"  # 84ed
    fields: {}  # 84ed
- op: discuss  # d591
  at: '2026-05-28T20:32:41Z'  # d591
  by: agent  # d591
  actor: jgstern_agent  # d591
  clock: 2  # d591
  nonce: d591  # d591
  message: "pass12: extended to numeric param validation. compact silently accepts --max-symbols 0 (produces 1 node), --max-symbols -5 (silent), --coverage 2.0 (out of documented range; returns 100=default max), --coverage -1 (negative; returns 100). No clamping warning. Filed as SSS (INV-nonag) since X was scoped to invalid flags only, but the structural pattern is the same — compact has no parameter validation."  # d591
- op: update  # 2ff3
  at: '2026-05-30T05:43:38Z'  # 2ff3
  by: agent  # 2ff3
  actor: jgstern_agent  # 2ff3
  clock: 3  # 2ff3
  nonce: 2ff3  # 2ff3
  set:  # 2ff3
    parent: INV-fabov-kapit-mupit-tigoz-lurol-mulus-jivik-kajun  # 2ff3
    fields:  # 2ff3
      dogfood_anon_id: "ITEM-bcd30e"  # 2ff3
  add:  # 2ff3
    tags: [dogfood_umbrella_member, dogfood_corpus_20260528, dogfood_severity_significant]  # 2ff3
- op: discuss  # bd5e
  at: '2026-05-30T05:43:39Z'  # bd5e
  by: agent  # bd5e
  actor: jgstern_agent  # bd5e
  clock: 4  # bd5e
  nonce: bd5e  # bd5e
  message: "Cluster member of INV-fabov-kapit-mupit-tigoz-lurol-mulus-jivik-kajun (the umbrella META item) 2026-05-29 per the May-28 dogfooding cluster reorganization."  # bd5e
- op: update  # c466
  at: '2026-05-30T09:50:43Z'  # c466
  by: agent  # c466
  actor: jgstern_agent  # c466
  clock: 5  # c466
  nonce: c466  # c466
  set:  # c466
    parent: INV-dunof-zahof-zizon-huhor-vipot-tadul-kunib-sonah  # c466
  add:  # c466
    tags: [dogfood_umbrella_member, fabov_face_argparse_type_range]  # c466
- op: discuss  # 1236
  at: '2026-06-02T08:02:26Z'  # 1236
  by: agent  # 1236
  actor: jgstern_agent  # 1236
  clock: 6  # 1236
  nonce: '1236'  # 1236
  message: "Pass-32 face: post-INV-sugat substrate confirms WI-vusaf's existing repro list + adds `--max-symbols 99999` runaway (F32.10).\n\nWI-vusaf's existing description (2026-05-28 baseline) lists three repros: `--max-symbols 50 --min-symbols 100` (silent), `--coverage 1.5` (silent), `--coverage -0.5` (silent), `--max-symbols 0` (clamp-to-1). Pass-32 re-ran the same flag matrix on the post-INV-sugat-closure substrate (`/tmp/hg-dogfood-s21/bm.json`); all five WI-vusaf cases still reproduce, and the substrate is unchanged from 2026-05-28 in terms of validator behavior:\n\n```\n$ hypergumbo compact --input bm.json --coverage -0.5 --out ... → exit 0\n$ hypergumbo compact --input bm.json --coverage  1.5 --out ... → exit 0\n$ hypergumbo compact --input bm.json --coverage  2.0 --out ... → exit 0\n$ hypergumbo compact --input bm.json --coverage banana --out ... → exit 2 (argparse float-parse)\n$ hypergumbo compact --input bm.json --max-symbols -1 --out ... → exit 0\n$ hypergumbo compact --input bm.json --max-symbols  0 --out ... → exit 0\n$ hypergumbo compact --input bm.json --max-symbols 99999 --out ... → TIMEOUT at 30s (no exit)\n```\n\nThe last row is the new face: **`--max-symbols` accepts values larger than the substrate's node count (34253) and proceeds to run without a clamp-to-available optimization, exceeding 30 seconds**. The default compact run completes in 4 seconds; raising the budget by 1000× yields >30s wall-clock with no progress indication. No upper bound check.\n\n**Why pass-32 is filing this.** Cumulative: the silent acceptance of `--coverage` out-of-range is the spec issue (range is documented as 0.0–1.0); the `--max-symbols 99999` runaway is a new symptom that argues for **range AND upper-bound discipline together** — not just rejecting negatives and >1 ratios, but also clamping or rejecting requests larger than the substrate's actual size. INV-pazur tile already documents the `nodes_summary` post-shrink reconciliation gap; pass-32 supplies the input-validation symmetric face: the budget can be set so high that the shrink loop becomes a no-op but the wall-clock blows out.\n\n**Fix-path candidate.** The argparse-action factory from INV-dunof's \"Fix shape\" should bound `--max-symbols` to `[1, len(bm.nodes)]` when `--input` is provided (the input file is already loaded and counted by the time argparse finishes; the bound is cheap to compute). For `--coverage`, bound to `[0.0, 1.0]` with a clear argparse error message. The compact-50 ⊄ compact-200 monotonicity face (filed against INV-fanur) is a separate concern downstream of the budget-acceptance gate.\n\n**No status change.** WI-vusaf's existing scope already covers the 5 silent-accept faces; pass-32 supplies the post-substrate confirmation and the 99999-runaway extension. Probe artifact: `/tmp/p32_outputD.log` section \"D.10\"."  # 1236
- op: update  # 0734
  at: '2026-06-08T18:30:24Z'  # 0734
  by: agent  # 0734
  actor: jgstern_agent  # 0734
  clock: 7  # 0734
  nonce: '0734'  # 0734
  set: {}  # 0734
  add:  # 0734
    tags: [dogfood]  # 0734
  remove:  # 0734
    tags: [dogfooding]  # 0734

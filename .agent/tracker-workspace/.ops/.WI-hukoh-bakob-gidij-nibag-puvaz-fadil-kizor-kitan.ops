- op: create  # fcbb
  at: '2026-04-07T17:49:04Z'  # fcbb
  by: agent  # fcbb
  actor: jgstern_agent  # fcbb
  clock: 1  # fcbb
  nonce: fcbb  # fcbb
  data:  # fcbb
    kind: work_item  # fcbb
    title: "Forward dataflow slice: decide and implement option (2) or (3)"  # fcbb
    status: todo_soft  # fcbb
    priority: 3  # fcbb
    parent:  # fcbb
    tags:  # fcbb
    - analysis_quality  # fcbb
    before: []  # fcbb
    duplicate_of: []  # fcbb
    not_duplicate_of: []  # fcbb
    pr_ref:  # fcbb
    description: "Longer-term direction for forward dataflow slicing, following the immediate WI-saful option (1) fix (one-hop downstream read admission from writer nodes, implemented in slice.py).\n\nCONTEXT: ADR-0015 §6 describes the intended semantics as 'BFS only follows edges where a write/mutate at the source connects to a read at the destination'. WI-saful (see tracker) shipped option (1) because it was small, contained to slice.py BFS, and cleared the immediate prometheus LOW_DATAFLOW_SLICE_RATIO signal by roughly tripling the dataflow slice. Option (1) has a structural limitation: it only admits read edges that are directly downstream of a writer — it cannot carry a 'this path is in the writer chain' flag further through the graph, so multi-hop dataflow like 'writer → passthrough → reader' misses the reader unless the passthrough is also a writer in its own right.\n\nTWO ALTERNATIVES TO DECIDE BETWEEN:\n\nOption (2) PRINCIPLED: populate edge.meta.dest_access_mode at edge creation time (linker work), then in BFS check both src and dst modes — admit edges where (forward: src in {write,mutate} OR dst in {read,mutate}). This is the 'right' longer-term direction: it makes the access_mode annotation a property of the edge (src->dst transition), not just the source node, and eliminates per-node writer-chain bookkeeping. Cost: touching every linker that creates dataflow-relevant edges (core base.py annotate_dataflow, plus 10+ linkers that call Edge.create with access_mode=...). The dest_access_mode field ALREADY EXISTS in the Edge schema and is referenced by a handful of cgo/ffi/napi/crypto_flow/ipc/ruby_ffi/lua_ffi/yjs_crdt/event_sourcing linkers, but is not populated by the main annotate_dataflow pipeline.\n\nOption (3) STATEFUL: track per-node 'arrived via writer chain?' state in BFS, then admit reads only from writer-chain nodes. Cleaner than option (1) because the 'writer' concept becomes a BFS invariant rather than a precomputed set-membership check. No linker changes needed. Cost: adds an axis to the visited set (visited becomes Set[tuple[node_id, in_writer_chain]]).\n\nDECIDE: which option is correct long-term? The decision must cover:\n  a) Does option (2) actually eliminate the need for option (1)? (Likely yes, if dest_access_mode is populated consistently.)\n  b) Does option (3) handle polyglot linker edges (cgo, ipc, yjs_crdt, event_sourcing) as gracefully as option (2)? These linkers already set dest_access_mode explicitly, suggesting the schema anticipated option (2).\n  c) What is the impact on slice size and accuracy on a real repo (e.g., prometheus)? Re-run the bakeoff after each option and compare slice.dataflow.0 node/edge counts to slice.0.\n  d) What is the migration path from option (1) to the chosen option? Can option (1) be left in place as a fallback, or must it be removed to avoid double-counting?\n\nIMPLEMENTATION: after decision, implement the chosen option, add regression tests (distinct from the WI-saful option (1) tests — the new tests should cover the multi-hop cases that option (1) cannot handle), and validate against the latest deep-bakeoff session. Update ADR-0015 to reflect the final semantics. Reference WI-saful in the PR description for full auditability."  # fcbb
    fields: {}  # fcbb
- op: discuss  # 676b
  at: '2026-04-07T19:24:33Z'  # 676b
  by: human  # 676b
  actor: jgstern  # 676b
  clock: 2  # 676b
  nonce: 676b  # 676b
  message: "(see also INV-sabum for more context)"  # 676b
- op: discuss  # 4605
  at: '2026-04-07T21:29:53Z'  # 4605
  by: agent  # 4605
  actor: jgstern_agent  # 4605
  clock: 3  # 4605
  nonce: '4605'  # 4605
  message: "Acknowledged. INV-sabum was the original 'propagating access_mode to dispatches_to was wrong' invariant — its history confirms that adding access_mode at the wrong layer created false dataflow edges, which is the same direction-of-decision pressure that distinguishes options (2) and (3) here. Specifically: option (2) populates dest_access_mode at edge creation time (the linker layer), which is exactly the same surface that INV-sabum was about; option (3) keeps the decision in BFS state (a layer further up). INV-sabum's resolution argued for 'do the work where the information is actually available,' which leans toward option (2) IF the dest type is reliably resolvable at link time. The Phase 5 cross-linker integration in ADR-0006's new §Return-Type Registry Pre-Pass would directly enable that — once the signature registry exists, dataflow.py can consult it to populate dest_access_mode for chained method calls. So WI-hukoh's option (2) and ADR-0006's INV-dihos work share the same underlying machinery. Will keep this WI in todo_soft pending the INV-dihos Phase 1 validation, then revisit option (2) vs (3) once signature data is available."  # 4605
- op: discuss  # dae1
  at: '2026-04-10T17:17:28Z'  # dae1
  by: human  # dae1
  actor: jgstern  # dae1
  clock: 4  # dae1
  nonce: dae1  # dae1
  message: "has INV-dihos Phase 1 been validated? please check the lab notebook and bakeoff artifacts."  # dae1
- op: discuss  # 5a6b
  at: '2026-04-10T17:38:37Z'  # 5a6b
  by: agent  # 5a6b
  actor: jgstern_agent  # 5a6b
  clock: 5  # 5a6b
  nonce: 5a6b  # 5a6b
  message: "INV-dihos Phase 1 IS validated. Evidence from bakeoff deep-20260409-054615 iter-008 (prometheus):\n\n- typed_receiver_call: 3,475 edges — these are the Phase 1 success cases (variable assignment chaining via return-type registry)\n- chained_return_type_call: 8 edges — inline chained calls resolved by WI-vadin\n- chained_call_unresolved: 698 edges — remaining inline chains that Phase 2 would address (tracked in WI-vadin)\n\nPhase 1 is confirmed working: 3,475 edges resolved through the return-type registry that would have been unresolved or misresolved before (e.g., qry.Close() routing to promql.Query.Close instead of ResourceClient.Close).\n\nFor the WI-hukoh decision itself (forward dataflow slice option 2 vs 3): the return-type registry being validated means option (2) is now technically feasible — the registry provides the type information needed to populate dest_access_mode at edge creation time. However, the INV-dihos Phase 1 coverage is Go+Java only; the registry doesn't yet cover Kotlin, C#, Rust, C++, or Python. Option (3) (BFS-state tracking) remains language-agnostic. The decision hinges on whether to wait for broader Phase 2-4 coverage before committing to option (2)."  # 5a6b
- op: discuss  # b6d3
  at: '2026-04-10T18:47:28Z'  # b6d3
  by: human  # b6d3
  actor: jgstern  # b6d3
  clock: 6  # b6d3
  nonce: b6d3  # b6d3
  message: "Does option (3) handle polyglot linker edges (cgo, ipc, yjs_crdt, event_sourcing) as gracefully as option (2)? These linkers already set dest_access_mode explicitly, suggesting the schema anticipated option (2). Please spend some time carefully thinking and investigating the pros and cons of option 2 versus option 3 from various perspectives."  # b6d3

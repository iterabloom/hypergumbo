- op: create  # e6c3
  at: '2026-06-04T08:27:33Z'  # e6c3
  by: agent  # e6c3
  actor: jgstern_agent  # e6c3
  clock: 1  # e6c3
  nonce: e6c3  # e6c3
  data:  # e6c3
    kind: work_item  # e6c3
    title: "symbols subcommand silently excludes file (901) and variable (1609) kinds; --kind file returns empty; --all excludes 2613 nodes (7.6%)"  # e6c3
    status: todo_hard  # e6c3
    priority: 3  # e6c3
    parent: INV-ripoh-hirom-lazan-konam-lolop-furot-tozuf-ditot  # e6c3
    tags:  # e6c3
    - dogfooding  # e6c3
    - cli_input_boundary  # e6c3
    - fold_audit_2026_06_03  # e6c3
    isbefore: []  # e6c3
    duplicate_of: []  # e6c3
    not_duplicate_of: []  # e6c3
    pr_ref:  # e6c3
    description: "## Origin\n\nMaterialized 2026-06-04 from the 2026-06-03 fold-audit (Family N). Stand-alone finding F30.11 previously folded onto INV-ripoh. Full evidence on INV-ripoh's pass-30 discussion entry.\n\n## Symptom\n\n`symbols` subcommand silently excludes `file` (901 nodes) and `variable` (1609 nodes) kinds. `--kind file` returns empty despite 901 nodes; `--all` excludes 2613 nodes (7.6%).\n\nDocumented behavior in `symbols --help`: `--all  Include all symbols`. Actual: `--all` includes everything EXCEPT file and variable. Two kinds silently filtered with no documentation.\n\nPattern joins INV-ripoh's other silent-kind-filter exhibits (F34.D1: search filters external_symbol; F35.D1 confirmed across 30+ patterns). Three subcommands with silent-kind-filters; each has a different exclusion set; none documented.\n\n## Invariant\n\n`symbols --all` includes ALL symbol kinds without silent exclusion. Either:\n1. `--all` documented as \"all kinds documented at /spec.md#kinds (excludes file, variable)\" — surfacing the filter.\n2. `--all` actually includes file and variable kinds (current default-exclusion happens at default invocation, not under --all).\n3. New flag `--include-meta-kinds` that user must explicitly request to see file/variable kinds.\n\nPath (2) matches the obvious reading of \"all\"; path (3) is most explicit.\n\n## Fix sketch\n\nIn the symbols subcommand, drop the silent file/variable filter when `--all` is set. Add documentation about the filter in the default behavior. Cousin work: WI-search-empty-pattern-doc (Family D — adjacent silent-input edge case).\n\n## Source provenance\n\n- **F30.11** — INV-ripoh discussion entry 2026-06-02 (pass-30 batch).\n- Cousin: F34.D1 + F35.D1 (search filters external_symbol; same pattern, different subcommand).\n\nLab notebook: pass 30.\n\n## Related rows\n\n- Parent: **INV-ripoh**.\n- Sibling: **WI-search-pattern-empty-undocumented** (Family D — adjacent silent-input edge case).\n- Sibling: **WI-test-coverage-production-fns-doc** (Family N — adjacent subcommand-UX gap).\n- Family N siblings."  # e6c3
    fields: {}  # e6c3
- op: discuss  # 467f
  at: '2026-06-05T06:00:53Z'  # 467f
  by: agent  # 467f
  actor: jgstern_agent  # 467f
  clock: 2  # 467f
  nonce: 467f  # 467f
  message: "[+ tranch_03: anon-card-tranch03-037 F55.A2 — symbols --kind variable says 'No symbols found' vs search; same EXCLUDED_KINDS silent filter; variable count matches (folded as duplicate; blind severity 3; new row WI-dahah wont_do)]"  # 467f
- op: update  # adcc
  at: '2026-06-08T18:30:28Z'  # adcc
  by: agent  # adcc
  actor: jgstern_agent  # adcc
  clock: 3  # adcc
  nonce: adcc  # adcc
  set: {}  # adcc
  add:  # adcc
    tags: [dogfood]  # adcc
  remove:  # adcc
    tags: [dogfooding]  # adcc
- op: update  # 3ab5
  at: '2026-07-10T05:22:20Z'  # 3ab5
  by: agent  # 3ab5
  actor: jgstern_agent  # 3ab5
  clock: 4  # 3ab5
  nonce: 3ab5  # 3ab5
  set:  # 3ab5
    status: done  # 3ab5
  add:  # 3ab5
    tags: [wave4]  # 3ab5
- op: discuss  # 6de8
  at: '2026-07-10T05:22:20Z'  # 6de8
  by: agent  # 6de8
  actor: jgstern_agent  # 6de8
  clock: 5  # 6de8
  nonce: 6de8  # 6de8
  message: "Fixed in PR #531 (selfh/dev d09e394a02), cli-output F4 leg A. Root cause: cmd_symbols ran the silent is_excluded_kind pre-filter BEFORE the explicit --kind filter, so --kind file / --kind variable matched the pre-filter and returned empty. Fix (path 2, obvious --kind reading): suppress the exclusion for the exact requested kind — is_excluded_kind(...) and not (args.kind and kind == args.kind). Behavioral evidence (live CLI, this build): a 2-node fixture (function 'run' + file node 'u_mod') → 'symbols --kind file' surfaces u_mod (grep 1); default 'symbols' still hides it (grep 0). Default/--all exclusion regression-fenced by the pre-existing test_cmd_symbols_excludes_excluded_kinds; requesting a non-excluded kind still hides the others. Default-hidden set now disclosed in --kind/--all help + symbols epilog. No flag added (no flag-matrix regen), no schema change. Core package 100% coverage."  # 6de8

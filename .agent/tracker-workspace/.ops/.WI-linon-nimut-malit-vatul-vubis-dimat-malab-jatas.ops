- op: create  # 88bf
  at: '2026-06-07T07:00:57Z'  # 88bf
  by: agent  # 88bf
  actor: jgstern_agent  # 88bf
  clock: 1  # 88bf
  nonce: 88bf  # 88bf
  data:  # 88bf
    kind: work_item  # 88bf
    title: "Python shape_id collapses different-kind symbols (class/method/function) and mis-hashes async def — node kind + AsyncFunctionDef absent from hash inputs"  # 88bf
    status: todo_hard  # 88bf
    priority: 1  # 88bf
    parent:  # 88bf
    tags:  # 88bf
    - dogfood  # 88bf
    isbefore: []  # 88bf
    duplicate_of: []  # 88bf
    not_duplicate_of: []  # 88bf
    pr_ref:  # 88bf
    description: "**Mechanism.** Python shape_id (structural-implementation fingerprint, spec §337/§342, \"changes if control flow changed\") omits the node KIND from its hash inputs, and `_compute_shape_id` (py.py:1649) tests `isinstance(node, ast.FunctionDef | ast.ClassDef)` — but `ast.AsyncFunctionDef` is NOT a subclass of `ast.FunctionDef`, so async defs are hashed via the ClassDef branch. Net effect: a docstring-only class, a sync method, and an async method with byte-identical body structure all get the SAME shape_id.\n\n**Evidence (frozen substrate a32c4a31).** 61 distinct shape_ids span >1 node kind; 689 nodes sit in mixed-kind clusters (59 `('function','method')`, 2 `('class','method')`). Cleanest group `sha256:44283d00f7d29138` (17 nodes) = 16 docstring-only Exception classes + 1 async method (`NullDuressHandler.on_duress_login`, serve_duress.py:82). FRESH-RUN REPRODUCED on /tmp: a docstring-only `class FooError(Exception)` and an `async def on_event` produced the same shape_id, byte-matching the substrate; `def plain_func()` and `def plain_method(self)` also collide.\n\n**Two sub-fixes (a fixer may split):** (1) include node kind in the shape hash inputs (fixes function/method/class conflation); (2) add `ast.AsyncFunctionDef` to the isinstance check (fixes async mis-hashing). \n\n**Consequence.** A consumer (human or LLM agent) using shape_id for clone-detection / \"find structurally-identical implementations\" gets false groupings conflating classes with methods and sync with async defs; an agent could propose dedup/refactor across genuinely different-kind symbols. Blind-judged Significant (sev 4).\n\n**Distinct from:** WI-lutob (shape_id=None on csharp/solidity/wgsl body-bearing), WI-luzut (shape_id=null on Python variables), WI-tisar (stable_id/shape_id share sha256: prefix). This is the only item about cross-KIND value COLLISION. Source: dogfood tranche-08 F157.A1 — recovered post-judging (a Phase-3a CORRECTION-of-a-POSITIVE mis-classification had dropped it from carding; reclassified INDEPENDENT and blind-judged sev 4)."  # 88bf
    fields: {}  # 88bf
- op: discuss  # 23de
  at: '2026-06-08T10:27:31Z'  # 23de
  by: agent  # 23de
  actor: jgstern_agent  # 23de
  clock: 2  # 23de
  nonce: 23de  # 23de
  message: "Re-confirms _compute_shape_id tests ast.FunctionDef so all 2279 async defs hit ClassDef branch -> class shape. WI-linon names sub-fix-2 (add AsyncFunctionDef, py.py:1649) + Exception+async collision. (dogfood; blinded sev 4)"  # 23de
- op: update  # 3613
  at: '2026-07-09T01:51:50Z'  # 3613
  by: agent  # 3613
  actor: jgstern_agent  # 3613
  clock: 3  # 3613
  nonce: '3613'  # 3613
  set: {}  # 3613
  add:  # 3613
    tags: [wave5]  # 3613
- op: discuss  # 30da
  at: '2026-07-09T01:51:50Z'  # 30da
  by: agent  # 30da
  actor: jgstern_agent  # 30da
  clock: 4  # 30da
  nonce: 30da  # 30da
  message: "Wave-5 sweep (2026-07-08): identity:F5 shape_id residual (Python shape_id collapses class/method/function + mis-hashes async def; node-kind + AsyncFunctionDef absent from hash). Empty isbefore, no parent -> terminal validation-drain wave. Batch-2 of the Theme-I identity/provenance sweep; wave5_remaining_plan_07082026.md."  # 30da
- op: update  # 8ce6
  at: '2026-07-15T20:56:53Z'  # 8ce6
  by: agent  # 8ce6
  actor: jgstern_agent  # 8ce6
  clock: 5  # 8ce6
  nonce: 8ce6  # 8ce6
  set: {}  # 8ce6
  add:  # 8ce6
    isbefore: [WI-vogij-jubit-podog-galug-humag-rovuh-jahan-rozug]  # 8ce6
- op: discuss  # 8fa4
  at: '2026-07-15T20:56:53Z'  # 8fa4
  by: agent  # 8fa4
  actor: jgstern_agent  # 8fa4
  clock: 6  # 8fa4
  nonce: 8fa4  # 8fa4
  message: "Cross-link (identity concept-audit 2026-07-15): this shape_id-correctness bug is a PREREQUISITE for WI-vogij (build structural-clone / refactoring-lead detection on shape_id). A clone-detector built on a hash that collapses class/method/function and mis-hashes async def would emit false clones — fix the hash first. Set isbefore WI-vogij."  # 8fa4
- op: update  # 187a
  at: '2026-07-16T02:10:22Z'  # 187a
  by: agent  # 187a
  actor: jgstern_agent  # 187a
  clock: 7  # 187a
  nonce: 187a  # 187a
  set:  # 187a
    status: done  # 187a
- op: discuss  # 9b63
  at: '2026-07-16T02:10:22Z'  # 9b63
  by: agent  # 9b63
  actor: jgstern_agent  # 9b63
  clock: 8  # 9b63
  nonce: 9b63  # 9b63
  message: "FIXED + CLOSED — PR 621ff6f5fd (merged to dev @ 2c02af6fed). Root cause exactly as filed: py.py _compute_shape_id (1) omitted the symbol KIND from its hash inputs (a module-level function and a class method are both ast.FunctionDef with self absent from the body → identical hash) and (2) tested isinstance(node, ast.FunctionDef), which ast.AsyncFunctionDef is NOT a subclass of, so async defs mis-branched into the ClassDef path (a docstring-only async def collided with a docstring-only class — the observed sha256:44283d00f7d29138 cluster). Fix folds the symbol kind (class/method/function) AND type(node).__name__ into the hashed prefix, discriminating kind and sync-vs-async while same-kind structural clones still share a shape_id (shape_id's one non-redundant power over fingerprint). BEHAVIORAL closure evidence (live repro of the item's OWN filed scenario on the production analyze_python path, post-merge): FILED COLLISION 1 (class FooError vs async on_event) RESOLVED (f1439907 vs af48fea4); FILED COLLISION 2 (plain_func vs plain_method) RESOLVED (0c866b2a vs 03e07ff1). 5 new tests in test_py_shape_id_kind.py (2 reproduced the exact documented collisions incl. sha256:44283d00, all green post-fix; 3 within-kind-clone regression pins hold). SHAPE_ID_SCHEME bumped v2->v3 per spec §6 mandate (every Python shape_id value changed); shape_id is not part of stable_id, so identity is unaffected — verified the WI-jafat identity-neutrality golden's stable_id + node.id sets are byte-for-byte UNCHANGED, only its shape_id golden re-baselined. Unblocks WI-vogij (structural-clone / refactoring-lead detection), whose isbefore this satisfied. Full mainstream isolation 100%; CI green."  # 9b63

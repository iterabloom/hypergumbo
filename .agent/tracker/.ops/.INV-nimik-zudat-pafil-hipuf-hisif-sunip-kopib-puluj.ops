- op: create  # 90a1
  at: '2026-02-15T00:51:38Z'  # 90a1
  by: agent  # 90a1
  actor: migration  # 90a1
  clock: 1  # 90a1
  nonce: 90a1  # 90a1
  data:  # 90a1
    kind: invariant  # 90a1
    title: "Route-to-Handler Edge Completeness"  # 90a1
    status: done  # 90a1
    priority: 4  # 90a1
    parent:  # 90a1
    tags: []  # 90a1
    before: []  # 90a1
    duplicate_of: []  # 90a1
    not_duplicate_of: []  # 90a1
    pr_ref:  # 90a1
    justification:  # 90a1
    description: ""  # 90a1
    fields:  # 90a1
      statement: "Routes should have edges to their handler functions when handler info is available"  # 90a1
      root_cause: "Analyzers store handler information in metadata (e.g., `controller_action`, `view_name`) but no edges are created from routes to handlers. Analysis showed only 15.8% of routes across bakeoff artifacts had outgoing edges: Ruby: `analyze/ruby.py:392` sets `symbol_ref=None` with comment \"Route DSL doesn't reference a handler symbol directly\" - but `controller_action` IS stored in metadata Elixir: `analyze/elixir.py:403` sets `symbol_ref=None` with comment \"Router DSL doesn't directly reference symbols\""  # 90a1
      fix: "Created `linkers/route_handler.py` that: 1. Finds route symbols with handler metadata (`controller_action`, `controller`+`action`) 2. Resolves handler references to actual method/function symbols using name matching 3. Creates `routes_to` edges (0.9 confidence) connecting routes to handlers Supported frameworks: Rails: `controller_action = \"users#index\"` → `UsersController#index` Phoenix: `controller` + `action` fields → `Controller.action`"  # 90a1
      regression_tests:  # 90a1
      - tests/test_route_handler_linker.py::TestRouteHandlerLinker  # 90a1
      - tests/test_route_handler_linker.py::TestLinkerEntryPoint  # 90a1
- op: update  # 8a82
  at: '2026-02-23T07:07:22Z'  # 8a82
  by: agent  # 8a82
  actor: jgstern_agent  # 8a82
  clock: 2  # 8a82
  nonce: 8a82  # 8a82
  set:  # 8a82
    status: holding  # 8a82
- op: discuss  # 527f
  at: '2026-03-03T06:15:38Z'  # 527f
  by: agent  # 527f
  actor: jgstern_agent  # 527f
  clock: 3  # 527f
  nonce: 527f  # 527f
  message: "External bakeoff (March 2026) confirms this invariant is violated for Java/JAX-RS. keycloak has 1,302 routes in routes.txt but ZERO kind=route nodes, ZERO routes_to edges in the behavior map. Byte-level scan of 280 MB hg.json confirmed.\n\nThe pipeline splits into three stages:\n1. Route annotation detection (works): @GET/@POST/@Path detected on methods/classes\n2. Route display (works): hypergumbo routes reads metadata, produces 1,302 entries\n3. Route graph materialization (missing): creating kind=route nodes with route_path/http_method fields and routes_to edges\n\nThe materialization gap exists because JAX-RS paths are distributed across class-level @Path + method-level @Path + subresource locator chains, requiring path reconstruction. Rails (centralized config/routes.rb) and Go (explicit r.GET(\"/path\", handler) calls) have this working end-to-end — gitlab produces 5,223 route nodes with 1,275 routes_to edges.\n\nThis INV should be bumped from P4 to at least P2 — Java web apps are a large share of enterprise codebases, and without routes_to edges, slice cannot trace HTTP request → handler → service chains.\n\nSource: /home/jgstern_agent/hypergumbo_lab_notebook/hypergumbo-v2.1.0-bakeoff-gitlab-grafana-bun-keycloak-20260302.md"  # 527f
- op: update  # 8b7b
  at: '2026-03-03T06:15:38Z'  # 8b7b
  by: agent  # 8b7b
  actor: jgstern_agent  # 8b7b
  clock: 4  # 8b7b
  nonce: 8b7b  # 8b7b
  set:  # 8b7b
    status: violated  # 8b7b
    priority: 2  # 8b7b
- op: update  # f751
  at: '2026-03-03T11:09:11Z'  # f751
  by: agent  # f751
  actor: jgstern_agent  # f751
  clock: 5  # f751
  nonce: f751  # f751
  set:  # f751
    status: holding  # f751
- op: discuss  # f700
  at: '2026-03-03T11:09:11Z'  # f700
  by: agent  # f700
  actor: jgstern_agent  # f700
  clock: 6  # f700
  nonce: f700  # f700
  message: "Partially addressed: JAX-RS route materialization (WI-lodik, PR #1684) creates route nodes from enriched concept metadata. Needs bakeoff revalidation to measure improvement."  # f700

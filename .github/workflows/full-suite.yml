name: Full Test Suite

# Full test suite runs as parallel jobs per package (ADR-0010).
# Each package runs in its own job to fit within Codeberg's time limits.
# When any job fails, the stop-the-line protocol blocks PRs until fixed.

on:
  # Triggered after PR merges to dev
  push:
    branches: [ "dev" ]
    paths-ignore:
      - '.agent/tracker/.ops/**'
      - '.agent/tracker-workspace/.ops/**'
  # Manual trigger for debugging
  workflow_dispatch:
  # Nightly validation
  schedule:
    - cron: '0 4 * * *'  # 4 AM UTC daily

# Singleton concurrency - only one full suite runs at a time
# This prevents resource contention and ensures clear pass/fail state
concurrency:
  group: full-suite-singleton
  cancel-in-progress: false

jobs:
  # Detect what files changed to skip expensive jobs for docs-only pushes
  changes:
    runs-on: codeberg-small-lazy
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Detect code changes
        id: filter
        run: |
          # Compare against previous commit
          base="${{ github.event.before }}"
          head="${{ github.sha }}"

          # Handle initial push (no previous commit) or empty/invalid base
          # github.event.before can be:
          # - All zeros: first push to branch
          # - Empty: some merge scenarios
          # - Invalid: ref no longer exists
          if [ -z "$base" ] || [ "$base" = "0000000000000000000000000000000000000000" ]; then
            echo "code=true" >> "$GITHUB_OUTPUT"
            echo "Initial push or empty base - running full suite"
            exit 0
          fi

          # Verify base commit exists (handles invalid/missing refs)
          if ! git cat-file -e "$base" 2>/dev/null; then
            echo "code=true" >> "$GITHUB_OUTPUT"
            echo "Base commit $base not found - running full suite"
            exit 0
          fi

          # Check if any non-doc files changed
          # Docs: *.md, docs/**, LICENSE, .gitignore, etc.
          # Note: YAML files in .agent/tracker(-workspace)/ are tracker data,
          # not code — they should not trigger expensive test jobs.
          CODE_PATTERNS='\.py$|\.json$|\.toml$|pyproject\.toml|scripts/|\.github/workflows/'
          YAML_PATTERN='\.ya?ml$'
          TRACKER_DATA='^\.agent/tracker(-workspace)?/'

          CHANGED=$(git diff --name-only "$base" "$head")
          has_code=false
          echo "$CHANGED" | grep -qE "$CODE_PATTERNS" && has_code=true
          # YAML files count as code UNLESS they're tracker data
          echo "$CHANGED" | grep -E "$YAML_PATTERN" | grep -vqE "$TRACKER_DATA" 2>/dev/null && has_code=true
          echo "code=$has_code" >> "$GITHUB_OUTPUT"

          if [ "$has_code" = "true" ]; then
            echo "Code changes detected - running full suite"
          else
            echo "Docs-only changes - skipping expensive jobs"
          fi

  # Prep job: build grammars AND install pip deps, cache both
  # This saves ~60s per test job by doing expensive installs once
  prep:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: codeberg-small-lazy
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Cache grammar wheels
      - name: Cache grammar wheels
        id: grammar-cache
        uses: actions/cache@v3
        with:
          path: /tmp/grammar-wheels
          key: grammar-wheels-${{ hashFiles('scripts/build-source-grammars') }}
      - name: Build grammar wheels
        if: steps.grammar-cache.outputs.cache-hit != 'true'
        run: |
          pip install --upgrade pip setuptools wheel
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels
      - name: Verify wheels exist
        run: ls -la /tmp/grammar-wheels/*.whl || echo "No wheels (will build in test jobs)"

      # Cache pip packages (site-packages) keyed by pyproject.toml files
      - name: Cache pip packages
        id: pip-cache
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install pip dependencies
        if: steps.pip-cache.outputs.cache-hit != 'true'
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist jsonschema
          pip install -e packages/hypergumbo-core \
                      -e packages/hypergumbo-lang-mainstream \
                      -e packages/hypergumbo-lang-common \
                      -e packages/hypergumbo-lang-extended1 \
                      -e "packages/hypergumbo-tracker[dev]"
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels

  # Run each package's tests in parallel (after prep is done)
  test-core:
    needs: [changes, prep]
    if: needs.changes.outputs.code == 'true'
    runs-on: codeberg-small-lazy
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Restore grammar wheels
      - name: Restore grammar wheels
        uses: actions/cache@v3
        with:
          path: /tmp/grammar-wheels
          key: grammar-wheels-${{ hashFiles('scripts/build-source-grammars') }}

      # Restore pip packages
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}

      # Quick install (mostly no-ops if cache hit, handles editable installs)
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist jsonschema
          pip install -e packages/hypergumbo-core \
                      -e packages/hypergumbo-lang-mainstream \
                      -e packages/hypergumbo-lang-common \
                      -e packages/hypergumbo-lang-extended1 \
                      -e "packages/hypergumbo-tracker[dev]"
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels

      - name: Check for sentence-transformers
        run: |
          if python -c "import sentence_transformers" 2>/dev/null; then
            echo "sentence-transformers available"
            echo "EMBEDDINGS_AVAILABLE=true" >> "$GITHUB_ENV"
          else
            echo "sentence-transformers not installed, using no-embeddings coverage config"
            echo "EMBEDDINGS_AVAILABLE=false" >> "$GITHUB_ENV"
          fi
      - name: Run tests
        id: tests
        run: |
          COV_CONFIG=""
          if [ "$EMBEDDINGS_AVAILABLE" = "false" ]; then
            COV_CONFIG="--cov-config=.coveragerc.no-embeddings"
          fi
          # Detect available CPUs from cgroup limits (Codeberg sets cpu.max
          # to 200000/100000 = 2 cores but nproc reports the host's 32).
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-core/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-core/src $COV_CONFIG --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  test-mainstream:
    needs: [changes, prep]
    if: needs.changes.outputs.code == 'true'
    runs-on: codeberg-small-lazy
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore grammar wheels
        uses: actions/cache@v3
        with:
          path: /tmp/grammar-wheels
          key: grammar-wheels-${{ hashFiles('scripts/build-source-grammars') }}
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist jsonschema
          pip install -e packages/hypergumbo-core \
                      -e packages/hypergumbo-lang-mainstream \
                      -e packages/hypergumbo-lang-common \
                      -e packages/hypergumbo-lang-extended1 \
                      -e "packages/hypergumbo-tracker[dev]"
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels
      - name: Check for sentence-transformers
        run: |
          if python -c "import sentence_transformers" 2>/dev/null; then
            echo "sentence-transformers available"
            echo "EMBEDDINGS_AVAILABLE=true" >> "$GITHUB_ENV"
          else
            echo "sentence-transformers not installed, using no-embeddings coverage config"
            echo "EMBEDDINGS_AVAILABLE=false" >> "$GITHUB_ENV"
          fi
      - name: Run tests
        id: tests
        run: |
          COV_CONFIG=""
          if [ "$EMBEDDINGS_AVAILABLE" = "false" ]; then
            COV_CONFIG="--cov-config=.coveragerc.no-embeddings"
          fi
          # Detect available CPUs from cgroup limits (Codeberg sets cpu.max
          # to 200000/100000 = 2 cores but nproc reports the host's 32).
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-lang-mainstream/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-lang-mainstream/src $COV_CONFIG --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  # Retry jobs for heavy packages that may hit Codeberg's 5-min deadline.
  # These run when the primary job fails OR is cancelled (Codeberg's infra
  # reports timed-out jobs as 'cancelled', not 'failure'). Retries run on
  # the local runner (via the 'self-hosted' label), which has no time limit.
  test-core-retry:
    needs: [changes, prep, test-core]
    if: >-
      needs.changes.outputs.code == 'true' &&
      (needs.test-core.result == 'failure' || needs.test-core.result == 'cancelled')
    runs-on: self-hosted
    timeout-minutes: 15
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore grammar wheels
        uses: actions/cache@v3
        with:
          path: /tmp/grammar-wheels
          key: grammar-wheels-${{ hashFiles('scripts/build-source-grammars') }}
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist jsonschema
          pip install -e packages/hypergumbo-core \
                      -e packages/hypergumbo-lang-mainstream \
                      -e packages/hypergumbo-lang-common \
                      -e packages/hypergumbo-lang-extended1 \
                      -e "packages/hypergumbo-tracker[dev]"
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels
      - name: Check for sentence-transformers
        run: |
          if python -c "import sentence_transformers" 2>/dev/null; then
            echo "sentence-transformers available"
            echo "EMBEDDINGS_AVAILABLE=true" >> "$GITHUB_ENV"
          else
            echo "sentence-transformers not installed, using no-embeddings coverage config"
            echo "EMBEDDINGS_AVAILABLE=false" >> "$GITHUB_ENV"
          fi
      - name: Run tests
        id: tests
        run: |
          COV_CONFIG=""
          if [ "$EMBEDDINGS_AVAILABLE" = "false" ]; then
            COV_CONFIG="--cov-config=.coveragerc.no-embeddings"
          fi
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-core/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-core/src $COV_CONFIG --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  test-mainstream-retry:
    needs: [changes, prep, test-mainstream]
    if: >-
      needs.changes.outputs.code == 'true' &&
      (needs.test-mainstream.result == 'failure' || needs.test-mainstream.result == 'cancelled')
    runs-on: self-hosted
    timeout-minutes: 15
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore grammar wheels
        uses: actions/cache@v3
        with:
          path: /tmp/grammar-wheels
          key: grammar-wheels-${{ hashFiles('scripts/build-source-grammars') }}
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist jsonschema
          pip install -e packages/hypergumbo-core \
                      -e packages/hypergumbo-lang-mainstream \
                      -e packages/hypergumbo-lang-common \
                      -e packages/hypergumbo-lang-extended1 \
                      -e "packages/hypergumbo-tracker[dev]"
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels
      - name: Check for sentence-transformers
        run: |
          if python -c "import sentence_transformers" 2>/dev/null; then
            echo "sentence-transformers available"
            echo "EMBEDDINGS_AVAILABLE=true" >> "$GITHUB_ENV"
          else
            echo "sentence-transformers not installed, using no-embeddings coverage config"
            echo "EMBEDDINGS_AVAILABLE=false" >> "$GITHUB_ENV"
          fi
      - name: Run tests
        id: tests
        run: |
          COV_CONFIG=""
          if [ "$EMBEDDINGS_AVAILABLE" = "false" ]; then
            COV_CONFIG="--cov-config=.coveragerc.no-embeddings"
          fi
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-lang-mainstream/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-lang-mainstream/src $COV_CONFIG --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  test-common:
    needs: [changes, prep]
    if: needs.changes.outputs.code == 'true'
    runs-on: codeberg-small-lazy
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore grammar wheels
        uses: actions/cache@v3
        with:
          path: /tmp/grammar-wheels
          key: grammar-wheels-${{ hashFiles('scripts/build-source-grammars') }}
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist jsonschema
          pip install -e packages/hypergumbo-core \
                      -e packages/hypergumbo-lang-mainstream \
                      -e packages/hypergumbo-lang-common \
                      -e packages/hypergumbo-lang-extended1 \
                      -e "packages/hypergumbo-tracker[dev]"
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels
      - name: Check for sentence-transformers
        run: |
          if python -c "import sentence_transformers" 2>/dev/null; then
            echo "sentence-transformers available"
            echo "EMBEDDINGS_AVAILABLE=true" >> "$GITHUB_ENV"
          else
            echo "sentence-transformers not installed, using no-embeddings coverage config"
            echo "EMBEDDINGS_AVAILABLE=false" >> "$GITHUB_ENV"
          fi
      - name: Run tests
        id: tests
        run: |
          COV_CONFIG=""
          if [ "$EMBEDDINGS_AVAILABLE" = "false" ]; then
            COV_CONFIG="--cov-config=.coveragerc.no-embeddings"
          fi
          # Detect available CPUs from cgroup limits (Codeberg sets cpu.max
          # to 200000/100000 = 2 cores but nproc reports the host's 32).
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-lang-common/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-lang-common/src $COV_CONFIG --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  test-extended:
    needs: [changes, prep]
    if: needs.changes.outputs.code == 'true'
    runs-on: codeberg-small-lazy
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore grammar wheels
        uses: actions/cache@v3
        with:
          path: /tmp/grammar-wheels
          key: grammar-wheels-${{ hashFiles('scripts/build-source-grammars') }}
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist jsonschema
          pip install -e packages/hypergumbo-core \
                      -e packages/hypergumbo-lang-mainstream \
                      -e packages/hypergumbo-lang-common \
                      -e packages/hypergumbo-lang-extended1 \
                      -e "packages/hypergumbo-tracker[dev]"
          ./scripts/build-source-grammars --wheels-dir /tmp/grammar-wheels
      - name: Check for sentence-transformers
        run: |
          if python -c "import sentence_transformers" 2>/dev/null; then
            echo "sentence-transformers available"
            echo "EMBEDDINGS_AVAILABLE=true" >> "$GITHUB_ENV"
          else
            echo "sentence-transformers not installed, using no-embeddings coverage config"
            echo "EMBEDDINGS_AVAILABLE=false" >> "$GITHUB_ENV"
          fi
      - name: Run tests
        id: tests
        run: |
          COV_CONFIG=""
          if [ "$EMBEDDINGS_AVAILABLE" = "false" ]; then
            COV_CONFIG="--cov-config=.coveragerc.no-embeddings"
          fi
          # Detect available CPUs from cgroup limits (Codeberg sets cpu.max
          # to 200000/100000 = 2 cores but nproc reports the host's 32).
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-lang-extended1/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-lang-extended1/src $COV_CONFIG --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  test-tracker:
    needs: [changes, prep]
    if: needs.changes.outputs.code == 'true'
    runs-on: codeberg-small-lazy
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist
          pip install -e "packages/hypergumbo-tracker[dev,tui]"
      - name: Run tests
        id: tests
        run: |
          # Detect available CPUs from cgroup limits
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-tracker/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-tracker/src --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  test-tracker-retry:
    needs: [changes, prep, test-tracker]
    if: >-
      needs.changes.outputs.code == 'true' &&
      (needs.test-tracker.result == 'failure' || needs.test-tracker.result == 'cancelled')
    runs-on: self-hosted
    timeout-minutes: 15
    outputs:
      coverage: ${{ steps.tests.outputs.coverage }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore pip packages
        uses: actions/cache@v3
        with:
          path: ~/.local/lib/python3.11/site-packages
          key: pip-packages-${{ hashFiles('packages/*/pyproject.toml') }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov pytest-xdist
          pip install -e "packages/hypergumbo-tracker[dev,tui]"
      - name: Run tests
        id: tests
        run: |
          if [ -f /sys/fs/cgroup/cpu.max ]; then
            read quota period < /sys/fs/cgroup/cpu.max
            if [ "$quota" != "max" ]; then
              WORKERS=$((quota / period))
            else
              WORKERS=$(nproc)
            fi
          else
            WORKERS=$(nproc)
          fi

          pytest packages/hypergumbo-tracker/tests/ -n "$WORKERS" --tb=short \
            --cov=packages/hypergumbo-tracker/src --cov-report=term | tee coverage-output.txt
          COV=$(grep "^TOTAL" coverage-output.txt | awk '{print $NF}' | tr -d '%')
          echo "coverage=${COV:-0}" >> "$GITHUB_OUTPUT"

  # Aggregate results after all test jobs complete
  aggregate:
    needs: [changes, test-core, test-core-retry, test-mainstream, test-mainstream-retry,
            test-common, test-extended, test-tracker, test-tracker-retry]
    if: always()
    runs-on: codeberg-small-lazy
    steps:
      - uses: actions/checkout@v4

      - name: Handle docs-only push
        if: needs.changes.outputs.code != 'true'
        run: |
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║  DOCS-ONLY PUSH - Test suite skipped                       ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          echo ""
          echo "No code changes detected. Only documentation files were modified."
          echo "Full test suite was skipped to save CI resources."

      - name: Check job results
        if: needs.changes.outputs.code == 'true'
        id: results
        run: |
          echo "Job results:"
          echo "  test-core: ${{ needs.test-core.result }} (coverage: ${{ needs.test-core.outputs.coverage }}%)"
          echo "  test-core-retry: ${{ needs.test-core-retry.result }} (coverage: ${{ needs.test-core-retry.outputs.coverage }}%)"
          echo "  test-mainstream: ${{ needs.test-mainstream.result }} (coverage: ${{ needs.test-mainstream.outputs.coverage }}%)"
          echo "  test-mainstream-retry: ${{ needs.test-mainstream-retry.result }} (coverage: ${{ needs.test-mainstream-retry.outputs.coverage }}%)"
          echo "  test-common: ${{ needs.test-common.result }} (coverage: ${{ needs.test-common.outputs.coverage }}%)"
          echo "  test-extended: ${{ needs.test-extended.result }} (coverage: ${{ needs.test-extended.outputs.coverage }}%)"
          echo "  test-tracker: ${{ needs.test-tracker.result }} (coverage: ${{ needs.test-tracker.outputs.coverage }}%)"
          echo "  test-tracker-retry: ${{ needs.test-tracker-retry.result }} (coverage: ${{ needs.test-tracker-retry.outputs.coverage }}%)"

          # Heavy jobs pass if primary OR retry succeeded
          core_ok=false
          [[ "${{ needs.test-core.result }}" == "success" ]] && core_ok=true
          [[ "${{ needs.test-core-retry.result }}" == "success" ]] && core_ok=true

          mainstream_ok=false
          [[ "${{ needs.test-mainstream.result }}" == "success" ]] && mainstream_ok=true
          [[ "${{ needs.test-mainstream-retry.result }}" == "success" ]] && mainstream_ok=true

          tracker_ok=false
          [[ "${{ needs.test-tracker.result }}" == "success" ]] && tracker_ok=true
          [[ "${{ needs.test-tracker-retry.result }}" == "success" ]] && tracker_ok=true

          if [[ "$core_ok" == "true" ]] && \
             [[ "$mainstream_ok" == "true" ]] && \
             [[ "${{ needs.test-common.result }}" == "success" ]] && \
             [[ "${{ needs.test-extended.result }}" == "success" ]] && \
             [[ "$tracker_ok" == "true" ]]; then
            echo "conclusion=success" >> "$GITHUB_OUTPUT"
            echo "✅ All test jobs passed"
          else
            echo "conclusion=failure" >> "$GITHUB_OUTPUT"
            echo "❌ One or more test jobs failed"
          fi

          # Use retry coverage when retry was the one that succeeded
          COV_CORE="${{ needs.test-core.outputs.coverage }}"
          if [[ -z "$COV_CORE" || "$COV_CORE" == "0" ]] && \
             [[ "${{ needs.test-core-retry.result }}" == "success" ]]; then
            COV_CORE="${{ needs.test-core-retry.outputs.coverage }}"
          fi

          COV_MAINSTREAM="${{ needs.test-mainstream.outputs.coverage }}"
          if [[ -z "$COV_MAINSTREAM" || "$COV_MAINSTREAM" == "0" ]] && \
             [[ "${{ needs.test-mainstream-retry.result }}" == "success" ]]; then
            COV_MAINSTREAM="${{ needs.test-mainstream-retry.outputs.coverage }}"
          fi

          COV_COMMON="${{ needs.test-common.outputs.coverage }}"
          COV_EXTENDED="${{ needs.test-extended.outputs.coverage }}"
          COV_TRACKER="${{ needs.test-tracker.outputs.coverage }}"
          if [[ -z "$COV_TRACKER" || "$COV_TRACKER" == "0" ]] && \
             [[ "${{ needs.test-tracker-retry.result }}" == "success" ]]; then
            COV_TRACKER="${{ needs.test-tracker-retry.outputs.coverage }}"
          fi

          # Default to 0 if empty
          COV_CORE="${COV_CORE:-0}"
          COV_MAINSTREAM="${COV_MAINSTREAM:-0}"
          COV_COMMON="${COV_COMMON:-0}"
          COV_EXTENDED="${COV_EXTENDED:-0}"
          COV_TRACKER="${COV_TRACKER:-0}"

          # Calculate average (integer math)
          AVG=$(( (COV_CORE + COV_MAINSTREAM + COV_COMMON + COV_EXTENDED + COV_TRACKER) / 5 ))
          echo "coverage=$AVG" >> "$GITHUB_OUTPUT"
          echo "Average coverage: $AVG%"

      - name: Record run result
        if: needs.changes.outputs.code == 'true'
        run: |
          # Log run metadata
          echo "{
            \"run_id\": \"${{ github.run_id }}\",
            \"sha\": \"${{ github.sha }}\",
            \"conclusion\": \"${{ steps.results.outputs.conclusion }}\",
            \"coverage\": \"${{ steps.results.outputs.coverage }}%\",
            \"timestamp\": \"$(date -Iseconds)\",
            \"triggered_by\": \"${{ github.event_name }}\",
            \"jobs\": {
              \"core\": \"${{ needs.test-core.result }}\",
              \"core-retry\": \"${{ needs.test-core-retry.result }}\",
              \"mainstream\": \"${{ needs.test-mainstream.result }}\",
              \"mainstream-retry\": \"${{ needs.test-mainstream-retry.result }}\",
              \"common\": \"${{ needs.test-common.result }}\",
              \"extended\": \"${{ needs.test-extended.result }}\",
              \"tracker\": \"${{ needs.test-tracker.result }}\",
              \"tracker-retry\": \"${{ needs.test-tracker-retry.result }}\"
            }
          }"

      - name: Update coverage badge and marker
        if: needs.changes.outputs.code == 'true' && steps.results.outputs.conclusion == 'success'
        run: |
          COV="${{ steps.results.outputs.coverage }}"

          # Use retry coverage when retry was the one that succeeded
          COV_CORE="${{ needs.test-core.outputs.coverage }}"
          if [ -z "$COV_CORE" ] || [ "$COV_CORE" = "0" ]; then
            RETRY_COV="${{ needs.test-core-retry.outputs.coverage }}"
            [ -n "$RETRY_COV" ] && [ "$RETRY_COV" != "0" ] && COV_CORE="$RETRY_COV"
          fi

          COV_MAINSTREAM="${{ needs.test-mainstream.outputs.coverage }}"
          if [ -z "$COV_MAINSTREAM" ] || [ "$COV_MAINSTREAM" = "0" ]; then
            RETRY_COV="${{ needs.test-mainstream-retry.outputs.coverage }}"
            [ -n "$RETRY_COV" ] && [ "$RETRY_COV" != "0" ] && COV_MAINSTREAM="$RETRY_COV"
          fi

          COV_COMMON="${{ needs.test-common.outputs.coverage }}"
          COV_EXTENDED="${{ needs.test-extended.outputs.coverage }}"
          COV_TRACKER="${{ needs.test-tracker.outputs.coverage }}"
          if [ -z "$COV_TRACKER" ] || [ "$COV_TRACKER" = "0" ]; then
            RETRY_COV="${{ needs.test-tracker-retry.outputs.coverage }}"
            [ -n "$RETRY_COV" ] && [ "$RETRY_COV" != "0" ] && COV_TRACKER="$RETRY_COV"
          fi

          # Determine badge color based on coverage
          if [ "$COV" -ge 90 ]; then
            COLOR="brightgreen"
          elif [ "$COV" -ge 70 ]; then
            COLOR="yellow"
          else
            COLOR="red"
          fi

          echo "{\"schemaVersion\":1,\"label\":\"coverage\",\"message\":\"${COV}%\",\"color\":\"${COLOR}\"}" > /tmp/coverage.json

          # Check if all packages have 100% coverage
          ALL_100="false"
          if [ "$COV_CORE" = "100" ] && [ "$COV_MAINSTREAM" = "100" ] && \
             [ "$COV_COMMON" = "100" ] && [ "$COV_EXTENDED" = "100" ] && \
             [ "$COV_TRACKER" = "100" ]; then
            ALL_100="true"
            echo "All packages at 100% coverage - updating last-green-sha marker"
            echo "${{ github.sha }}" > /tmp/last-green-sha.txt
          else
            echo "Not all packages at 100%: core=$COV_CORE, mainstream=$COV_MAINSTREAM, common=$COV_COMMON, extended=$COV_EXTENDED, tracker=$COV_TRACKER"
          fi

          git config user.name "CI Bot"
          git config user.email "ci@iterabloom.com"

          git fetch origin badges:badges 2>/dev/null || true
          git checkout badges 2>/dev/null || git checkout --orphan badges

          git rm -rf . 2>/dev/null || true
          cp /tmp/coverage.json coverage.json
          if [ "$ALL_100" = "true" ]; then
            cp /tmp/last-green-sha.txt last-green-sha.txt
          fi

          git add coverage.json
          if [ "$ALL_100" = "true" ]; then
            git add last-green-sha.txt
          fi
          git commit -m "Update coverage badge: ${COV}%" || echo "No changes to commit"
          git push origin badges --force || echo "Push failed - may need write permissions"

      - name: Fail if any job failed
        if: needs.changes.outputs.code == 'true' && steps.results.outputs.conclusion != 'success'
        run: |
          echo "❌ Full suite FAILED"
          echo ""
          echo "Stop-the-line protocol is now active."
          echo "New PRs will be blocked until this is fixed."
          echo ""
          echo "To submit a fix, use PR title:"
          echo "  fix(job-${{ github.run_id }}): <description>"
          exit 1

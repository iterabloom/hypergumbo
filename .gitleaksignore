# .env contains local development tokens (OpenRouter, Forgejo). The file is
# gitignored but release-check runs `gitleaks --no-git` which walks the
# filesystem, so per-line entries are needed to quiet scans.
.env:generic-api-key:8
.env:generic-api-key:11
.env:generic-api-key:16

# Hypothesis test framework auto-generated constants
.hypothesis/constants/022db5084484a444:generic-api-key:4
.hypothesis/constants/959b519aed8dc0c7:generic-api-key:4
.hypothesis/constants/fe153fcba53d98bb:generic-api-key:4

# Test fixture with fake API key in INI config example
packages/hypergumbo-lang-mainstream/tests/BRANCHES_test_ini.py:generic-api-key:352

# Embeddings module references model name strings that look like API keys
packages/hypergumbo-tracker/src/hypergumbo_tracker/embeddings.py:generic-api-key:12

# Pinned Sourcegraph SCIP proto revision (a commit SHA, not a token). Bumping
# this is a deliberate public-ABI change; see scripts/build-scip-proto header.
scripts/build-scip-proto:sourcegraph-access-token:26

# Go identifier "apiv2.NewAPI" cited in a tracker invariant's op-log trips the
# generic-api-key rule. The .ops file is authoritative tracker state.
.agent/tracker-workspace/.ops/.INV-popup-fafip-dopud-kahoj-vikum-sukar-vubon-giloh.ops:generic-api-key:52

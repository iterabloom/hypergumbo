<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
# Measurement records

This directory holds **measurement records**: the per-item verdict
tables produced when hypergumbo's own output is adjudicated against
ground truth read from source code.

## Why this is a sibling of `docs/audits/`, not part of it

[`docs/audits/README.md`](../audits/README.md) scopes itself
explicitly: its format is "shaped for **axis-conformance audits**" —
per-value verdicts from the CANONICAL / FOLD / DEPRECATE-NO-FOLD
trichotomy — and it says that audits with a different verdict shape
"should propose a sibling format rather than shoehorn into this one."

A precision measurement has a different verdict shape. Its unit is a
reported *finding*, not a vocabulary *value*; its verdicts are TRUE
POSITIVE / FALSE POSITIVE / UNADJUDICABLE; and its output is a rate
with a population attached rather than a migration plan. So it files
here.

## What belongs here

A measurement record, not a lab note and not a status page. Concretely:

- The **question**, stated so that a different answer would change a
  decision.
- The **population**: what was measured, how it was obtained, and
  whether it is a census or a sample. A rate without a named
  denominator is not a measurement.
- The **rubric**, fixed before the labelling and reproduced verbatim,
  together with any revision made after labelling started and the
  reason for it.
- The **verdict table**, one row per adjudicated item.
- The **disagreement rate** between independent passes, reported
  beside the headline number rather than folded into it.
- What the number does **not** support. Precision is not accuracy;
  a census of five repositories is not a random sample of software.

Numbers that describe the state of a tracker item still live on the
tracker item. What lives here is the evidence a reader would need to
disbelieve the number.

## Format

One markdown file per measurement, named `<NNNN>-<topic>.md`, numbered
independently of the ADR and audit-findings series.

## Index

| ID | Title | Instrument | Result |
|----|-------|------------|--------|
| [0001](0001-taint-flow-precision.md) | Taint-flow precision on real repositories | `scripts/measure-taint-precision.py` | ≈41% population-weighted (28 TP of 85 adjudicated across 6 repos); 19 further flows unadjudicable |
| [0002](0002-catalogue-reach-multilanguage.md) | Catalogue reach on Go, JavaScript and Java | `scripts/measure-catalogue-reach.py` | go **84.8% production-faithful** (Go arm corrected — the original 77.1% "all 45 method-kind unattributed" was a fixture artefact and its receiver-typing conclusion is withdrawn), javascript 97.3%, java 90.8% (static form only), python 97.7% |
| [0003](0003-instantiates-taint-delta.md) | What the construction-edge widening did to taint precision | `scripts/measure-taint-precision.py` | **2.9% marginal precision** (1 TP of 35 added flows, 0 removed, census of the delta across the same 6 repos); the change is correct and the trade is bad — 24 of 34 FPs are sink calls with no arguments at all |
| [0004](0004-taint-precision-per-situation.md) | Per-situation taint precision, and what the row denominator was hiding | `verify-claims --format json`, hand-adjudicated | **32.2% per situation vs 11.1% per row** (19 TP of 59 situations / 33 TP of 297 rows, census of 0001's cohort at one commit, two units); false positives concentrate in the largest groups — caddy's `cmdRun` is 76 rows, all false, collapsing to 3. Single-pass non-blind, ties resolved to FP, so both rates are floors |
| [0005](0005-taint-precision-after-vocabulary-split.md) | Taint precision after the fail-open fixes and the `env_read` split | `scripts/measure-taint-precision.py` (extended to report both units) | **21.6% per situation / 10.0% per row** (8 TP of 37 situations / 17 TP of 170 rows, census of 0004's cohort at `ba01153095`). Blind second pass by four independent adjudicators: **0.0% disagreement over all 37** — and the adjudication and adversarial passes that followed it moved the headline **twice, in opposite directions**. The population fell 297 -> 170 rows and **every row of that is one scope decision**: `host_description` ships one claim where `host_secret` has three, and adding the two probe claims restores 0004's count exactly, repo for repo |
| [0006](0006-taint-precision-under-the-ratified-frame.md) | Taint-flow precision under the ratified frame | `verify-claims --format json`, two independent 16-repo adjudication panels | **33.9% per situation / 31.5% per row** (38 TP of 112 situations / 106 TP of 336 rows, 16 repositories, 10 languages, seeded draw). Series position **N=1** — it does not continue 0001–0005 (INV-duvup). Two blind panels agreed **94.6%**, and a subsequent refutation pass killed **7 of 44** panel-agreed true positives, three of them unanimous — agreement is not correctness. Per INV-nular a finding can be true value-flow AND vacuous, so 33.9% is an **upper bound** on useful precision. **Useful precision re-derived under ADR-0046 (WI-gibom): 24.1%** (27/112) — **11** KIND-MISDECLARED, **0** CONFIGURED-ACTION, so the band `≤25% useful` is tripped. The ≤25.0% previously published here survives as a bound and fails as a derivation: its stated basis (five shellcheck TPs) supports 29.5%. The one standing sensitivity — `io_lib:format` declared `logging` — was **resolved against the row, and its deduction STANDS**: useful precision is **21.4%** (24/112). A finding-level A/B showed the three TPs re-rooting onto `io:format`, and a first correction read that as the crossing being represented; a blind panel (two independent passes, **0/7 disagreement**) refuted it — the re-rooted route terminates at an `io:format` whose only argument is locally bound, so the removal replaced a *vacuous* true positive with an outright *false* one. **Presence of a finding after a change is not evidence the finding is true.** Both versions are kept in the record. Two further sensitivities first published here were **withdrawn**: the spec already defines `env_read` as an ambient configuration read *"(environment variables, system properties, argv — values that may carry a credential)"*, so argv and application config are not misdeclarations |
| [0007](0007-the-section-7a-addressable-domain.md) | The ADR-0017 §7a addressable domain | `verify-claims --format json`, census over 0006's cohort | **Zero.** Of 153 `ddg_mixed` evidence rows across 11 repositories, **0** rest on a walk that ran and established no dependence — 90.8% never ran, 9.2% lost track — and `unconfirmed` is 0 in *every* repository. Every removal §7a would authorise is removal-on-ignorance; the +6.3pt price was computed on the wrong field. Blocked walks partition **cross_function 76.3% / source_not_tracked 17.3% / sink_before_source 6.5%** |
| [0008](0008-the-cross-function-composition-ceiling.md) | The §4a cross-function composition ceiling | `scripts/measure-cross-function-reach.py` | **18.3% pooled ceiling** (778 of 4,245 cross-function findings meet the necessary conditions for a single-hop composed walk; per-repo median 22.3%, range 0–100%, beads alone 62.4% of the pool, 94% Go by volume). The payoff splits by direction: **refutation is bounded at zero** (0 of 28 running walks are `unconfirmed`, the only class removal can act on), while **confirmation INFLATES output** — adjudicated flows bypass collapse, so up to 778 new `precise` rows land on a cohort emitting 298 |
| [0009](0009-deferred-crossing-reachability.md) | Reachability of the deferred-crossing family | `measure-catalogue-reach.py` + per-idiom fixtures | **72.6% of the measured family fires in real idiom** (45 of 62; L1 gate 134/135 with a module hint, 37/135 without). The synthetic probe alone says 92.3% and **disagrees with the real idiom on 24% of rows in both directions** — its method spelling is `require('WebSocket')`. `flask.Flask.run`, the filed premise for "maybe it's all inert", fires in both forms a Flask program is written in and misses only the untyped receiver; python is 22/22. Where it IS inert one mechanism covers go and java (`WI-lalot`: a receiver typed from a library RETURN VALUE is not typed at all, killing all 7 go framework launch rows) and a second covers javascript (`INV-misup`, re-derived independently). Register: **135 deferred + 14 misdeclared of 335** own rows, against the census's 131 of 332 — and `ipc_recv` is 67 rows, not the 64 that document totals |
| [0010](0010-deferred-crossing-findings-per-shape.md) | Adjudicated deferred-crossing findings, per shape | `measure-taint-precision.py collect` + per-arm ledgers | **The ruling holds for LAUNCH and fails for HANDLE — and only one of four mechanisms produces anything true.** Arm 1 (measurement 0006's own two-pass labels): the family is **7 situations of 112, 0 TP, 0 useful** against a 33.0% population. Arm 2 (17 repos, this tree, adjudicated TWICE): **50 situations / 411 rows, 14.0% correctness, 14.0% useful**. `DEFERRED` 0 TP of 3, `WIRING` 0 of 8, `WRONG-CHANNEL` **0 of 17**, `READ-IN-SCOPE` **7 of 22** — a stdin handle read in the scope that built it is the only shape that fires. The pre-registered refutation FIRED: `bufio.NewReader(os.Stdin)` read four lines later is a true, useful finding, and Go's *entire* stdin surface is three deferred-crossing rows with no catalogued read to relocate to, so retagging HANDLE is forbidden by ruling 3 on its own terms. LAUNCH is unanimous (0 TP) and **thin: 2 findings against 49 files** with a launch-shaped call. **PASS 1 OVER-CALLED TRUE POSITIVES BY 3.4×** (24→7) by accepting the tool's advertised path instead of reading the sink call's arguments in the TERMINAL function — 0006's own section-F instrument bug, reproduced by hand and then corrected; both ledgers ship. CONFIGURED-ACTION is **still 0** even with caddy in the cohort: clause 3 fails, because the sink writes the whole config blob rather than a declared field. `ets:foldl` corrected from CALLBACK to **transfer** — it returns the accumulator |
| [0011](0011-the-cross-function-composition-price.md) | What a composed walk costs in rows | `scripts/measure-row-inflation.py` | **×1.41 at the ceiling** (3,166 → 4,470 emitted rows, +1,304; ×1.22 at 50% sufficiency, ×1.11 at 25%) over 0007's 11-repo cohort. 0008's relative framing used the wrong denominator — its 298 is the rows *carrying a walk verdict*, not the 3,166 the cohort emits — so the ratio was overstated ~2.5×. The cost is **not one row per confirmation**: 1,549 composable findings yield +1,304 rows because a group whose members are all composable vanishes, and touched groups run 1–104 members. **§4a would invalidate a premise the collapse design states quantitatively**: the `ddg` bypass is justified because `ddg` is "6 of 359 census flows" (1.7%), which reproduces today at **1.43%** and would go to **13.17%, 9×**. 0008's ceiling has also drifted **18.3% → 14.8%** in six days — five repos byte-identical, jaeger's halved — because findings grew 2.5× while collapse absorbed almost all of it. Simulated collapse checked against the production function on four repos, exact each time |
| [0012](0012-taint-precision-after-escape-closure.md) | Taint precision after the escape-closure fixes | `verify-claims --format json`, two independent 12-agent panels + refutation + ADR-0046 vacuity | **34.0% correctness / 30.9% useful** per situation (32 and 29 of 94; rows 30.0% / 28.8%), unweighted per-repo mean useful **28.0%**. **The `≤25% useful` band does NOT trip** — 0006 read 24.1%. Continues 0006's frame on the same 16 repos, and is **not independent of it**: 68 of the 94 labels ARE 0006's, with 26 judged fresh. **The load-bearing check is the stratum split** — carried 30.9% useful against fresh 30.8%, so the survivorship objection to a carry-forward does not bite. Panels agreed **96.2%**; the refutation pass killed **0 of 11** against 0006's 7 of 44. n=94 rather than 112 because five repos can no longer supply M=7 — *because the defects they exhibited were fixed* — so F2's equal allocation is unreachable and pooled ≠ unweighted mean, reported throughout. All three vacuous TPs are KIND-MISDECLARED (`hPutStrLn stderr` as `fs_write`, `io.WriteString` to a subprocess pipe as `fs_write`, `sendmsg` on an AF_UNIX socket as `net_send`); CONFIGURED-ACTION stays structurally at **0** |
| [0013](0013-django-queryset-chain-delta.md) | What typing the Django QuerySet chain did to taint precision (INV-mumov, Phase 6 PR 1) | `measure-taint-precision.py collect`, two asserted arms, every new situation read at source | **87.5% correct, 17.5% useful** on the 40 situations the change added to pretix (kserve control: 0 moved, 0 lost): 7 real `select_for_update(...).get()` → write flows, 5 false, and 28 on a lazy QuerySet combinator that reads nothing — ADR-0049's deferred row, now removable under ruling 3 |
| [0014](0014-lazy-queryset-disclosure-delta.md) | What moving Django's lazy QuerySet combinators to a disclosure boundary did to taint precision (WI-fasap, Phase 6 PR 2) | `measure-taint-precision.py collect`, one asserted subject arm against 0013's baseline ledger, every vanished situation read at source | **44 situations removed on pretix, 33 vacuous / 11 deferred / 0 real reads deleted** (kserve control: 0 moved; no verdict moved): the 28 vacuous situations 0013 found are gone, the 7 useful `select_for_update(...).get()` flows remain, and all 18 in-scope iteration reads survive on the new `django.db.models.__iter__` call site. The cost is 11 QuerySets handed out of the composing scope, disclosed by `db_compose`'s shadow rather than minted. |
| [0015](0015-django-related-manager-delta.md) | What typing Django's reverse-relation managers did to taint precision (WI-gulaz, Phase 6 PR 3) | `measure-taint-precision.py collect`, one asserted subject arm against 0014's baseline ledger, every new situation read at source, a refutation check on the production survey | **27 situations added on pretix, 0 lost, 66.7% correct, 66.7% useful** (kserve control: 0 moved; no verdict moved; 0 wrong-type): 18 real fetched-row → write pairs (`gc.transactions.create`, `inv.team.members.add`, `order.refunds.create`, `Checkin.all` rows into `LogEntry.objects.bulk_create`), 9 false — 4 a fetched row used only as a check argument two hops away, 5 a structural pairing whose real carrier is an untyped instance `.save()` / `.delete()` on a loop variable. |
| [0016](0016-django-instance-write-delta.md) | What typing the Django ORM instance write through the model lineage did to taint precision (WI-gamas, Phase 6 PR 4) | `measure-taint-precision.py collect`, TWO asserted arms run fresh, every new situation read at source, a refutation check on both production surveys | **11 situations added on pretix, 0 lost, 72.7% correct, 72.7% useful** (kserve control: 0 moved; no verdict moved; 0 wrong-type of 75 marker edges): the typed `save`/`delete` surface goes from **2 sites to 75** in a repository with ~2,900 of them. A third rule — `super().save()` inside a model override, where the write actually is — was built, measured and WITHHELD: it is correct at the site (61 sites, 0 wrong-type) but the ADR-0017 walk crosses the django-orm-dispatch `dispatches_to` edge to reach an ORM method the function never calls, so it bought 3 true findings against 20 false ones. |
| [0017](0017-java-lang-implicit-import-delta.md) | What resolving `java.lang` statics without a wildcard import did to taint precision (INV-suril) | TWO asserted arms (by java.py BLOB, since both trees were dirty), three java repositories, every moved row read against source, RC1/RC2 refutation cells scanned | **21 situations added, 0 lost, 9.5% correct (14.3% counting one plausible), 0 wrong-type of 21** — and the low figure is PRICED, not excused: java has no def/use extractor (`WI-gotun`), so every java flow is `structural`/`walk=unavailable`, every figure here is a FLOOR, and the BASELINE's own java findings are 3 of 3 in the same regime, so the change scales java's existing precision rather than introducing a worse one. Pre-fix sizing: cassandra loses **459 edges** to this against **446** that classify in the whole repository. Independent of any walk, **8 blindness caveats retire** on jenkins. cassandra moved 0 situations while gaining 273 findings — they land outside the seven questions the claim set asks. |
| [0018](0018-java-callee-name-slot-delta.md) | What taking the receiver out of java's callee name slot did to the disclosures — and what it exposed (WI-nakut, java half) | THREE asserted arms by blob (baseline / name-slot fix / fix + linker guard), two java repositories plus a shipped-CLI fixture, every moved edge read against source | **No verdict moved in 34 claim evaluations** — the change produces no findings, so no precision figure is computed and this does not enter the 0006 series. The boundary-scoped `untyped_receiver` caveat could never fire for java (**match-as-written 0** on all three sizing repos) and now attributes **974 edges / 43 names / 9 boundaries** on jenkins over 650 already matching, **4,708 / 53 / 10** on cassandra over 4,953; the unscoped caveat's distinct-method COUNT was inflated **29% / 102% / 176%** — cassandra printed 22,584 where there are 8,191, `get` alone as **427** "methods". The A/B then found what the plan did not predict: java's calls reached the `method-call-recovery` linker for the first time and it resolved them wrongly (**9 of 9** on sherpa-onnx: `audio.getSampleRate()` → `OfflineTts`), with the refuting evidence — `receiver_type_hint` — already stamped on every edge. The guard added in arm S2 removes those **plus 262 PRE-EXISTING false recoveries** on jenkins; the pre-registered "may only remove what arm S introduced" clause was refuted and was the wrong test — **12 of 12 sampled removals are false resolutions**. |
| [0023](0023-c-include-module-hint.md) | What stamping C's `#include` set on ambiguous bare calls recovers, and what it costs in verdict labels (WI-lajus) | Two arms one FILE apart, the `c.py` blob asserted at BEGIN and END of each arm with the arm exiting 3 on a mismatch; census of every primitive in every boundary of three repos; TWO UNITS reported separately because they move in opposite directions | **13 new primitives, ZERO lost** across passt / torsocks / libvfio-user — `send` and `recv`, the population the item filed, reach for the first time (7/7 on passt, 4 on libvfio-user); io_edges passt 75 → 119. **And 15 of 21 claim verdicts move `confirmed_with_caveats` → `inconclusive`, ZERO toward `confirmed`.** Findings are unchanged in every repo, so this is NOT a precision result and none is claimed. **The obvious explanation is wrong and torsocks refutes it**: it has no `net_recv`/`ipc_recv` in EITHER arm and still moved six claims, and `command_launch` is unchanged in all three. The mechanism is **INV-zimud** — the coverage gate expands a disjunctive slot with ALL, not ANY, and its own comment records "the three C repos 0.0%" — plus `c.yaml` declaring no `module_completeness` for any header to vouch for the newly-stamped calls. Direction is entirely toward WITHHOLDING, and the retracted confirmations were on repos whose socket I/O was invisible (passt is a user-mode TCP/IP stack confirming "no untrusted input reaches a subprocess" with zero send/recv classified). **A regression caught before shipping**: stamping every unresolved call, rather than only ambiguous ones, moved `stdio.fclose` 5→2 and `fopen` 1→0 on qemu-dtc — partial evidence is worse than none. Residuals filed as WI-hasul and WI-bulub. |
| [0022](0022-django-setup-fixture-receiver-typing.md) | What Django test-fixture receiver typing buys, on the only arm that can see it (WI-zamud) | Two arms differing ONLY in the rule, markers asserted at BEGIN and END; **two SOURCE MODES reported separately and never pooled**, because the whole population is under `src/tests/` where WI-bifob excludes it by default; population existence checked BEFORE the arms and reconciled over three rounds when the pre-registered stop condition fired; census of all 224 new situations by a classifier hand-verified 6/6 and run in two variants; the arm's OWN ambient precision measured for comparison | **The pre-registered ≥50% bar FAILED: 60 TP / 135 FP = 30.8% correct, 95% CI 24.7–37.6%, entirely below the bar**; 29 vacuous, 0 wrong-type. **pretix `prod` 149 → 149 (NEW 0) exactly as predicted** — the default arm is structurally blind to this rule — and `all` 613 → 837 (NEW 224, GONE 0); kserve unmoved in both modes. **What the number measures: all 224 are `structural`/`walk_verdict=unavailable`** (no dataflow walk ran) and 152 are hops=0, and the arm's own pre-existing population scores **24.2% [16.7–33.7] by the same classifier — statistically indistinguishable (p=0.25)**, so the 50% bar was carried over from DEFAULT-arm precedents and mis-specified. **Refutation: 0 violations over 692 independently verified sites**, after three defects in the refutation SCAN were found and fixed rather than reported as analyzer violations (8→1→0). Edge reach **+1,447 ORM call sites (10,422 → 11,869), 0 gone** — CORRECTED after publication: the first version reported +756 `(src,dst)` pairs and "110 apparent removals", which was this record's own instrument keying on `edge.line` and never reading `meta["call_lines"]`, the documented field in which `ir.deduplicate_edges` preserves every collapsed site. Ships with the criterion failure declared, on the grounds that the default arm is unmoved and the rule is refutation-clean. |
| [0021](0021-django-super-write-remeasured.md) | What the Django `super().save()` write chokepoint costs and buys on a walk that is no longer lying (WI-sihoh) | Two arms differing ONLY in the rule, both carrying INV-putug's fix; markers asserted at BEGIN and END; class-hop canary replayed against 0016 round 1 at exactly 33 BEFORE use; PC1b added to the pre-registration to distinguish a working fix from an empty population; full-function readback of all 16 new situations | **pretix 133 → 149 situations: 16 NEW, 0 GONE; 9 TP / 7 FP = 56.3% correct (8/8 = 50.0% under the strict reading of situation 9), 0 vacuous, 0 wrong-type**; kserve control unmoved. **PC2: 61 `super()` sites typed, 0 violations across all 136 marker edges**, the same 61 three independent routes agree on. **PC1: class-node hops 0, where round 1 read 33 with this same rule present** — and PC1b shows that zero is evidence rather than an empty population (896 refusable edges across 440 classes, 67 of the new sinks inside one), making this the first live-population observation of INV-putug's fix. The 23 pair-level removals are RE-KEYS, not losses (0 situations vanished). No suppression: all four substituted D2 observables moved up or stayed flat, disclosed because `collect` records no `sanitized` count. **The pre-registered prediction (≈3 TP / 0 FP) was WRONG** — seven of round 1's eight "ordinary" FPs were this rule's all along. Ships AT the 50% bar, not above it; the case rests on the false-NEGATIVE direction, not the rate. |
| [0020](0020-dispatch-edge-value-carrying.md) | What refusing a class-sourced `dispatches_to` edge removes — and why the first design measured an empty population (INV-putug) | Four arms, markers asserted at BEGIN and END; two on the shipped tree, two pinning `py.py` to `359880a105` so WI-sihoh's WITHHELD rule supplies the population (harness common-mode, not shipped); class-hop canary validated against 0016 round 1 at exactly 33; complete census of every removal | **On the shipped tree the fix is INERT — 0 moved, 0 class hops — because the population cannot exist while WI-sihoh is withheld, which the pre-registration should have caught and did not.** On the harness arms: pretix 183 → 163, **22 removed / 2 added, net −20**, matching 0016's independent attribution of 20 situations to this shape; class-node hops **33 → 0**; kserve control unmoved. **All 22 removals crossed a class node (zero collateral), and 0 of 22 are correct** — no source function's body calls the named sink at all, and the two sites read at source are a no-argument `InvoiceAddress()` never saved and a `SubEventItem.delete` never called. **11 class-hop flows were KEPT** via honest routes (`generate_invoice → build_invoice → Invoice.save`, annotated receiver), so the fix deletes the dishonest path to a finding, not the finding. R1–R4 all fail to fire; INV-zuhig preserved field-for-field. Two instrument faults recorded, including a `flow_id` key embedding the arm letter that reported "removed 148, added 148". |
| [0019](0019-dns-lookup-marginal-precision.md) | What rowing DNS lookups as `net_recv` added — and why the number measures the WALK, not the rows (WI-dozul) | Two arms by blob with a pre-registered row canary per arm, 11 repositories chosen by MEASURED call-site count over 273 (364 MB, 35 DNS sites), census read-back of every moved situation | **6 situations added, 0 removed, 0 verdicts moved, and 0 of 6 CORRECT** — every one a spurious source/sink pairing (redis `_anetTcpServer` passes `perm=0` while the `chmod` sits behind `AF_LOCAL && perm`; vault's `os/exec` calls run BEFORE the `LookupNS` and consume other data). **The result is attributed, and the attribution is the finding:** 5 of 6 false positives are in c, and `registered_def_use_languages()` returns `go, javascript, python, rust, typescript` ONLY — c/cpp/java/erlang have NO def/use extractor, so every c flow is structural and pairs any source with any sink in one function. Python: 12 call sites → **0 flows, a TRUE negative**, asserted not inferred (a fixture reports `socket.gethostbyname → subprocess.run` at confidence **precise**). The item's pre-registered re-open trigger's CONDITION fires but its PREMISE does not — it anticipated correct-but-not-useful findings, and these are not correct for a reason unrelated to forward-vs-reverse — so it is **reported, not executed**: filing a boundary audit on this number would blame the ROW for the WALK's defect. |
| [0024](0024-scala-receiver-type-module-slot.md) | What routing Scala's already-inferred receiver type into the module slot recovers, and what it discloses (WI-sigog) | Two arms, the `scala.py` blob asserted at BEGIN and END with the arm exiting 3 on a mismatch; the B arm DERIVED from an existing survey because `io-boundaries` / `verify-claims` are interpreters; census of every moved row | **0 → 595 of 11,032 typed method edges on sbt (5.39%) and 0 → 317 on lila (1.06%); 0 → 13 catalogue rows reached on sbt, 1 → 2 on lila, 14/14 correct on census**; two controls byte-identical, `sanitized_flows` 0 → 0 (the sanitizer hazard did not fire). Verdicts stayed flat and the whole effect was in the **caveats** — the first differ compared four verdict fields and reported "no claim-level movement", which was wrong. **The ceiling is the finding:** only **10.1% / 4.8%** of scala's external method edges carry a `receiver_type_hint` at all → `WI-pokam` |
| [0025](0025-objc-field-expression-receiver.md) | What fixing Objective-C's message-receiver parse corrects, and why it cannot move recall (WI-garar, stage 1) | Two arms, `objc.py` blob asserted at BEGIN and END; per-site name map keyed on `(src, line, name)` TRIPLES after the `(src, line)` pair silently dropped every sibling send on a nested line | **787 selectors corrected (380 of them previously emitting NOTHING) and 20 false module slots removed, at ZERO change in recall** — the F3 gate refuses an untyped method call however correct its NAME (INV-tapat / INV-maluk). A negative result, and a deliverable: three copies of "which child is the receiver" had desynchronised three ways, and reading the name from `meta["callee_name"]` rather than re-deriving it from the id is what ADR-0036 ruling 1 exists for (an ObjC selector ends in a colon) |
| [0026](0026-objc-property-receiver-typing.md) | What typing an Objective-C `self.<property>` receiver reaches, and the parse failure that hides it (WI-garar, stage 2) | Two arms one stage apart, blob-asserted; the property map GLOBAL because properties are declared in `.h` and used in `.m`; falsifier named in the pre-registration | **CocoaLumberjack +3 catalogue rows end to end; AFNetworking's untypable receiver share 52.4% → 46.3%**, below its pre-stage-1 47.5%. **The headline prediction was REFUTED and honoured**: `net_recv` stayed 0 because tree-sitter fails to parse **72 of 387 objc files (18.6%)**, including the header declaring `session` → `WI-lafom`. A refuted prediction can indict the INPUT rather than the fix — but only if you go and find out which. The change's own first cut put the `IBOutlet` MACRO in the module slot by taking the FIRST `type_identifier` |
| [0027](0027-objc-macro-parse-recovery.md) | Which macros break the Objective-C parse, and what recovering them reaches (WI-lafom) | Each suspected construct parsed IN ISOLATION rather than read at the first ERROR node's line; each rewrite rule priced cumulatively; two arms by blob, asserted at BEGIN and END; whole-artifact equality before any field is picked; every moved site read back | **Two triggers, neither of them what the item named: `NS_ASSUME_NONNULL_BEGIN` only in the WRAPPING position, and `NS_ENUM`/`NS_OPTIONS`, which the item did not name at all.** ERROR files **74 → 39 of 387 (19.1% → 10.1%)**, `property_declaration` **436 → 486**, `class_interface` **322 → 346**, no count falling anywhere; symbols +109 / +59 / +160, fmdb +252 edges. **The grammar-bump option is CLOSED, not skipped** — 3.0.2 is the newest published, upstream's last substantive commit is 2024-12-16, and its NS_OPTIONS bug has been open since 2025-02-25. Two further rules were measured and REJECTED: Apple's availability macros recover zero files, and a generic `SHOUTY(...)` rule sheds 17 real properties. **The one count that fell is a precision gain**: AFNetworking's typed slots 1189 → 1148, all 41 re-keys of `AFURLSessionManager → external` — a first-party class leaving the slot at the count the item predicted, because `_project_classes` contains it for the first time. 85 of 85 newly typed fmdb slots correct; Mantle byte-identical, as pre-registered. Residual: **10.1% still fail**, and 577 of 3,205 typed slots (18.0%) name a first-party class through an unguarded branch — pre-existing, and visible only now |

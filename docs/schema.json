{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/iterabloom/hypergumbo/docs/schema.json",
  "title": "Hypergumbo Behavior Map",
  "description": "Output schema for hypergumbo analysis. Auto-generated from Python dataclasses.",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "string",
      "description": "Schema version (semver)",
      "const": "0.20.6"
    },
    "confidence_model": {
      "type": "string",
      "description": "Identifier for confidence scoring algorithm"
    },
    "stable_id_scheme": {
      "type": "string",
      "description": "Identifier for stable_id generation algorithm"
    },
    "shape_id_scheme": {
      "type": "string",
      "description": "Identifier for shape_id generation algorithm"
    },
    "repo_fingerprint_scheme": {
      "type": "string",
      "description": "Identifier for repo fingerprinting algorithm"
    },
    "symbol_fingerprint_scheme": {
      "type": "string",
      "description": "Identifier for the Symbol.fingerprint structural hashing scheme"
    },
    "view": {
      "type": "string",
      "enum": [
        "behavior_map",
        "compact",
        "tiered"
      ],
      "description": "Output view type: 'behavior_map' for the base analysis, 'compact'/'tiered' for the budget-limited projections"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp when analysis was generated"
    },
    "analysis_incomplete": {
      "type": "boolean",
      "description": "True if analysis was truncated or limited"
    },
    "analysis_runs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/AnalysisRun"
      },
      "description": "Provenance tracking for each analysis pass"
    },
    "profile": {
      "type": "object",
      "description": "Repository profile (languages, frameworks, etc.)",
      "properties": {
        "languages": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "files": {
                "type": "integer"
              },
              "loc": {
                "type": "integer",
                "description": "Source-lines-of-code (SLOC) for this language \u2014 the file-level LOC convention. Distinct from the per-symbol line_span (physical end-start+1 span). WI-bozid: the schema previously declared phantom `lines`/`percentage` keys the producer (profile.LanguageStats.to_dict) never emits; the real emitted shape is {files, loc}."
              }
            }
          }
        },
        "frameworks": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "dev_frameworks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Frameworks detected only in development/test dependencies, kept separate from `frameworks` (production surface) so a consumer can tell a test harness from an application framework."
        },
        "framework_mode": {
          "type": "string",
          "description": "How `frameworks` was resolved (e.g. manifest declaration vs import-based promotion)."
        },
        "framework_evidence": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "WI-napuj: {framework: [node_ids]} \u2014 where each declared framework is actually imported, so a framework claim traces to evidence. Omitted when empty (INV-virik present-when-populated)."
        }
      }
    },
    "nodes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Symbol"
      },
      "description": "Code symbols (functions, classes, etc.)"
    },
    "edges": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Edge"
      },
      "description": "Relationships between symbols"
    },
    "features": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Feature"
      },
      "description": "Feature slice index entries (handler slices etc.); full slice content lives in per-slice side files"
    },
    "metrics": {
      "type": "object",
      "description": "Aggregate analysis metrics",
      "properties": {
        "total_nodes": {
          "type": "integer",
          "description": "Number of emitted Symbols"
        },
        "total_edges": {
          "type": "integer",
          "description": "Number of emitted Edges"
        },
        "total_files": {
          "type": "integer",
          "description": "Node-distinct path count \u2014 number of distinct node.path values that survive analysis (INV-mozaf)"
        },
        "avg_confidence": {
          "type": "number",
          "description": "Mean edge confidence (3 decimal places)"
        },
        "languages": {
          "type": "object",
          "description": "Per-language node/edge/file counts",
          "additionalProperties": {
            "type": "object"
          }
        },
        "by_supply_chain_tier": {
          "type": "object",
          "description": "Per-tier node/edge counts",
          "additionalProperties": {
            "type": "object"
          }
        },
        "debug": {
          "type": "object",
          "description": "Introspection counters (not a stable API)",
          "properties": {
            "unique_paths_in_analysis": {
              "type": "integer"
            },
            "analyzed_file_symbols": {
              "type": "integer"
            },
            "profile_files_sum": {
              "type": "integer",
              "description": "Legacy profile-language file-count sum, kept for introspection (over-counts vs node-distinct total_files)"
            }
          }
        }
      }
    },
    "limits": {
      "$ref": "#/$defs/Limits",
      "description": "Known gaps and limitations"
    },
    "supply_chain_summary": {
      "type": "object",
      "description": "Supply-chain classification summary, keyed by tier name (first_party / internal_dep / external_dep) plus derived_skipped. Each tier carries {files, symbols}; external_dep also `ecosystem` and `directness` sub-buckets and derived_skipped a {files, paths} shape. WI-vafid: the producer emits these at the top level, not under a by_tier wrapper.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "files": {
            "type": "integer"
          },
          "symbols": {
            "type": "integer"
          },
          "ecosystem": {
            "type": "object",
            "additionalProperties": {
              "type": "integer"
            }
          },
          "directness": {
            "type": "object",
            "additionalProperties": {
              "type": "integer"
            }
          },
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "entrypoints": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Entrypoint"
      },
      "description": "Detected program entrypoints"
    },
    "usage_contexts": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/UsageContext"
      },
      "description": "Concept-tagged call/usage sites (per ADR-3ccc usage_context_patterns)"
    },
    "reproducibility_context": {
      "type": "object",
      "description": "L2 reproducibility metadata (INV-morag option B): direct dependencies + runtime identity, with an explicit not_captured disclosure of L3-L5 factors",
      "properties": {
        "level": {
          "type": "string",
          "const": "L2",
          "description": "Reproducibility capture level"
        },
        "captured": {
          "type": "object",
          "description": "Recorded reproducibility inputs. Sub-keys tree_sitter_version / grammars appear only when tree-sitter is importable",
          "properties": {
            "hypergumbo_version": {
              "type": "string"
            },
            "python_version": {
              "type": "string"
            },
            "python_implementation": {
              "type": "string"
            },
            "tree_sitter_version": {
              "type": "string"
            },
            "grammars": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          }
        },
        "not_captured": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Factors that may affect reproducibility but are not recorded (transitive deps, OS, hardware)"
        },
        "implications": {
          "type": "string",
          "description": "What diff-attribution level a consumer can expect from the captured fields alone"
        }
      }
    },
    "validation_report": {
      "type": "object",
      "description": "Spec-vs-data validator output (ADR-0033): the triage surface for contract violations; never a build break",
      "properties": {
        "schema_version": {
          "type": "string",
          "description": "validation_report schema version"
        },
        "violations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ValidationViolation"
          },
          "description": "Structured violations, all classes"
        },
        "violations_by_class": {
          "type": "object",
          "additionalProperties": {
            "type": "integer"
          },
          "description": "Per-validator-class counts"
        },
        "wired_checks": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "check": {
                "type": "string"
              },
              "validator_class": {
                "type": "string"
              },
              "description": {
                "type": "string"
              }
            },
            "additionalProperties": false
          },
          "description": "validator:F2 (WI-moriz) \u2014 the predicates actually wired into validate_ir, each mapped to the validator_class it contributes to. Makes a 0 count interpretable ('0 of these named checks', not '0 defects of any kind'); a defect class absent here is, by absence, not yet validated."
        },
        "stable_id_stats": {
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "total_symbols": {
                  "type": "integer"
                },
                "non_null": {
                  "type": "integer"
                },
                "none_cohort": {
                  "type": "integer"
                },
                "none_cohort_pct": {
                  "type": "number"
                },
                "collision_groups": {
                  "type": "integer"
                },
                "collided_symbols": {
                  "type": "integer"
                },
                "collision_rate_pct": {
                  "type": "number"
                },
                "per_file_collision_groups": {
                  "type": "integer"
                }
              },
              "additionalProperties": false
            },
            {
              "type": "null"
            }
          ],
          "description": "ADR-0035 \u00a75 corpus stable_id population/collision/None-cohort disclosure over an all-Symbols denominator; always present so a clean non-null rate can never hide a None-cohort. Null on bare build_validation_report calls (unit tests)."
        }
      }
    },
    "sketch_precomputed": {
      "type": "object",
      "x-internal": true,
      "description": "INTERNAL cache artifact (NOT a stability contract; consumers should not depend on its presence or shape \u2014 spec.md). Pre-computed sketch payload (additional-file centrality, readme description, config info) reused by the `sketch` step. Optional; omitted when --no-sketch-precomputed. Each sub-field is nullable because small repos may not surface a README, license, or any centrality signal.",
      "properties": {
        "additional_file_centrality_scores": {
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "number"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Non-source-file-path \u2192 symbol-mention density score, for ranking the sketch's Additional Files section. The score is \u03a3(in-degree of the graph symbols whose names appear in the file) \u00f7 file length in characters \u2014 an in-degree-weighted mention density, NOT a [0,1]-normalized centrality and NOT a probability: it is unbounded \u2265 0 (a short file naming high-in-degree symbols routinely exceeds 1.0), is meaningful only as a relative ranking key within one run, and is not comparable across repos (WI-sigof). Keys are config/doc files only (NOT whole-graph symbol centrality \u2014 that is the `symbols` command). file-anchor:F1 guarantees every key is also a kind=file node path (WI-rajod)."
        },
        "config_info": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Aggregated license / config string"
        },
        "readme_description": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Extracted README description"
        }
      }
    }
  },
  "required": [
    "schema_version",
    "view",
    "generated_at",
    "nodes",
    "edges"
  ],
  "$defs": {
    "Span": {
      "type": "object",
      "description": "Source code location",
      "properties": {
        "start_line": {
          "type": "integer",
          "minimum": 0,
          "description": "Starting line number (1-indexed); 0 reserved for synthetic boundary symbols (kind=external_symbol) which have no source location"
        },
        "end_line": {
          "type": "integer",
          "minimum": 0,
          "description": "Ending line number (1-indexed); 0 reserved for synthetic boundary symbols (kind=external_symbol) which have no source location"
        },
        "start_col": {
          "type": "integer",
          "minimum": 0,
          "description": "Starting column (0-indexed)"
        },
        "end_col": {
          "type": "integer",
          "minimum": 0,
          "description": "Ending column (0-indexed)"
        }
      },
      "required": [
        "start_line",
        "end_line",
        "start_col",
        "end_col"
      ]
    },
    "Symbol": {
      "type": "object",
      "description": "A code symbol (function, class, method, etc.)",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique, location-addressed node identifier (ADR-0036 grammar: lang:path:span:name:kind). Because it encodes location it CHURNS on file move / rename / signature change; for an edits-surviving identity use stable_id, and for cross-run rename tracking use fingerprint."
        },
        "name": {
          "type": "string",
          "description": "Symbol name"
        },
        "kind": {
          "type": "string",
          "description": "Symbol type. Per ADR-0027, the canonical axiom is 'Symbol.kind names the source-language syntactic construct that the symbol represents.' The x-axis-of-values map below documents the canonical registry from packages/hypergumbo-core/src/hypergumbo_core/symbol_kinds.py::SYMBOL_KINDS. The schema enum is intentionally open (type: string without enum constraint) until ADR-0027 Phase 4b producer migrations land per-cluster \u2014 current production includes dynamic kind=f\"ipc_{...}\" emits at ipc.py:498 and phoenix_ipc.py:276 that produce values not in the static registry. The L3 producer-coherence linter (scripts/check-producer-axis-coherence) gates new literal additions against the registry; the open enum honestly reflects the producer-side leak the ADR is fixing.",
          "x-axis-of-values": {
            "function": "language_construct",
            "method": "language_construct",
            "class": "language_construct",
            "interface": "language_construct",
            "contract": "language_construct",
            "modifier": "language_construct",
            "struct": "language_construct",
            "enum": "language_construct",
            "union": "language_construct",
            "error_set": "language_construct",
            "list": "language_construct",
            "map": "language_construct",
            "trait": "language_construct",
            "module": "language_construct",
            "namespace": "language_construct",
            "variable": "language_construct",
            "constant": "language_construct",
            "const": "language_construct",
            "property": "language_construct",
            "attribute": "language_construct",
            "field": "language_construct",
            "constructor": "language_construct",
            "getter": "language_construct",
            "setter": "language_construct",
            "type": "language_construct",
            "type_alias": "language_construct",
            "typedef": "language_construct",
            "alias": "language_construct",
            "simple_type": "language_construct",
            "defined_type": "language_construct",
            "macro": "language_construct",
            "generic": "language_construct",
            "mixin": "language_construct",
            "extension": "language_construct",
            "record": "language_construct",
            "abstract": "language_construct",
            "instance": "language_construct",
            "subroutine": "language_construct",
            "procedure": "language_construct",
            "filter": "language_construct",
            "workflow": "language_construct",
            "call_site": "language_construct",
            "arrow_function": "language_construct",
            "object": "language_construct",
            "prop": "language_construct",
            "slot": "language_construct",
            "template": "language_construct",
            "directive": "language_construct",
            "declaration": "language_construct",
            "keyword": "language_construct",
            "export": "language_construct",
            "import": "language_construct",
            "reference": "language_construct",
            "component": "language_construct",
            "view": "language_construct",
            "file": "language_construct",
            "library": "language_construct",
            "package": "language_construct",
            "executable": "language_construct",
            "program": "language_construct",
            "project": "language_construct",
            "bin": "pending_classification",
            "test": "language_construct",
            "target": "language_construct",
            "special_target": "language_construct",
            "recipe": "language_construct",
            "env_var": "language_construct",
            "build_arg": "language_construct",
            "exposed_port": "language_construct",
            "stage": "language_construct",
            "requirement": "language_construct",
            "setting": "language_construct",
            "derivation": "language_construct",
            "dependency": "language_construct",
            "addtask": "language_construct",
            "task": "language_construct",
            "trigger": "language_construct",
            "section": "language_construct",
            "paragraph": "language_construct",
            "code_block": "language_construct",
            "diagram": "language_construct",
            "plot": "language_construct",
            "yield": "language_construct",
            "for_loop": "language_construct",
            "conditional": "language_construct",
            "block": "language_construct",
            "prefix": "language_construct",
            "base": "language_construct",
            "query": "language_construct",
            "mutation": "language_construct",
            "subscription": "language_construct",
            "scalar": "language_construct",
            "entry": "language_construct",
            "entity": "language_construct",
            "architecture": "language_construct",
            "participant": "language_construct",
            "state": "language_construct",
            "fragment": "language_construct",
            "partial": "language_construct",
            "provider": "language_construct",
            "local": "language_construct",
            "style_block": "language_construct",
            "permission": "language_construct",
            "keyframes": "language_construct",
            "media": "language_construct",
            "font_face": "language_construct",
            "class_selector": "language_construct",
            "id_selector": "language_construct",
            "rule_set": "language_construct",
            "subdirectory": "language_construct",
            "table": "language_construct",
            "table_array": "language_construct",
            "link": "language_construct",
            "label": "language_construct",
            "command": "language_construct",
            "environment": "language_construct",
            "binding": "language_construct",
            "operator": "language_construct",
            "assumption": "language_construct",
            "id": "language_construct",
            "source": "language_construct",
            "port": "language_construct",
            "output": "language_construct",
            "input": "language_construct",
            "value": "language_construct",
            "pattern": "language_construct",
            "subscript": "language_construct",
            "signal": "language_construct",
            "message": "language_construct",
            "data": "language_construct",
            "resource": "language_construct",
            "event": "language_construct",
            "protocol": "language_construct",
            "index": "language_construct",
            "node": "language_construct",
            "inductive": "language_construct",
            "theorem": "language_construct",
            "playbook": "language_construct",
            "external_symbol": "language_construct",
            "binary": "pending_classification",
            "benchmark": "pending_classification",
            "example": "pending_classification",
            "workspace": "pending_classification",
            "handler": "pending_classification",
            "pattern_rule": "pending_classification",
            "helper": "pending_classification",
            "uniform": "pending_classification",
            "varying": "pending_classification",
            "private": "pending_classification",
            "storage": "pending_classification",
            "workgroup": "pending_classification",
            "operation": "pending_classification"
          },
          "x-deprecated": []
        },
        "language": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Programming language of the host source file. Null for Class B synthetic stand-ins emitted by protocol linkers (ADR-0031) \u2014 those carry discovery_language + protocol_origin instead."
        },
        "path": {
          "type": "string",
          "description": "File path"
        },
        "span": {
          "oneOf": [
            {
              "$ref": "#/$defs/Span"
            },
            {
              "type": "null"
            }
          ],
          "description": "Source location"
        },
        "origin": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Pass IDs that contributed to this symbol (INV-jidat)"
        },
        "origin_run_id": {
          "type": "string",
          "description": "Unique execution ID"
        },
        "stable_id": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Structural identity hash within a (qualified_name, module_path) scope (ADR-0014 as amended by Phase 6 / INV-bazij): survives body edits, NOT rename or move. Serialized as sha256:<16hex> and shares this exact surface with shape_id; the two are discriminated by field name (and the top-level stable_id_scheme / shape_id_scheme descriptors), NOT by an in-value prefix, and their value-spaces are disjoint. Do not join on bare hash values across the two axes (WI-tisar)."
        },
        "shape_id": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Structural *skeleton* hash \u2014 the parse subtree with identifiers, literals, comments, and punctuation STRIPPED (ADR-0014 \u00a71), so same-shape / different-name symbols collide. Within-language only. Contrast fingerprint, which KEEPS identifiers/literals; shape_id is a strict coarsening of it. Serialized as sha256:<16hex>, the same surface as stable_id (see WI-tisar). (Currently a serialized output-boundary field with no internal consumer.)"
        },
        "fingerprint": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Structural content hash of the symbol's parse subtree (shape + identifiers + literals; whitespace/comment-invariant), prefixed with the scheme tag declared in symbol_fingerprint_scheme. Null when the span has no parseable content or no grammar is available."
        },
        "quality": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "description": "Node-level quality assessment ({score, reason}). Has no producer (INV-nuzal): unlike edge.quality it is not derived from confidence, so it is omitted when null (INV-virik omit-when-empty) and appears only if a future pass populates it."
        },
        "meta": {
          "type": "object",
          "description": "Symbol metadata",
          "properties": {
            "abstract": {
              "description": "True on abstract class / method Symbols. Distinguishes interface-like classes from concrete classes in languages with explicit ``abstract`` keywords."
            },
            "annotations": {
              "description": "Java / Kotlin / Python decorator-equivalent annotations on a Symbol. Distinct from ``decorators`` (Python-style) where the syntax carries different semantics."
            },
            "base_classes": {
              "description": "List of base-class names on class / interface Symbols. Used by framework_patterns to identify Django Model, SQLAlchemy Base, etc."
            },
            "block_type": {
              "description": "Block subtype for generic ``block`` Symbols where the construct admits sub-classification (e.g. 'datasource', 'generator' in Prisma). Fold residue per WI-runod Wave 6 PR 4."
            },
            "component_framework": {
              "description": "Framework on single-file-component symbols (e.g. 'vue', 'svelte', 'astro'). Fold residue for the ``component_file`` \u2192 ``file`` migration per audit-findings 0011."
            },
            "concepts": {
              "description": "Framework/language concepts attached to a symbol, as a list of ``{concept, framework, ...}`` dicts. (A literal example is deliberately NOT spelled out here: generate-concepts scrapes source for that dict shape, and an example in prose is indistinguishable from a declaration \u2014 it listed this module as a web framework beside django and rails.) Written BOTH by analyzers at construction (bash's ``shell_script``, python's ``main_guard``, html's ``html_entry``) and by ``framework_patterns.enrich_symbols`` at consumer time."
            },
            "config_format": {
              "description": "Configuration format on config-shape Symbols (e.g. 'tsconfig'). Fold residue per audit-findings 0005."
            },
            "constructed_from": {
              "description": "The callee whose result a variable is bound to \u2014 the ``FastAPI`` in ``app = FastAPI()``, the ``sqlalchemy.orm.declarative_base`` in ``Base = declarative_base()``. Recorded verbatim, keeping any attribute qualification, so a framework YAML can distinguish a namespaced callee from a same-named local one. WI-nopod: a whole class of frameworks is configured by CONSTRUCTING an object rather than decorating or subclassing one, and nothing recorded that binding \u2014 ``instantiates`` is anchored at the file (\"this file instantiated FastAPI somewhere\"), never at the variable. Sibling of ``base_classes``: both record what type a symbol relates to, as metadata a linker can later promote to edges. Static analysis cannot distinguish a constructor from a factory and the key does not try to \u2014 it names the callee, which is what the AST supports."
            },
            "decorators": {
              "description": "List of decorator names on function / method / class Symbols. Used by framework_patterns to identify Flask routes, FastAPI handlers, etc."
            },
            "dependency_scope": {
              "description": "Dependency scope on dependency Symbols (e.g. 'dev'). Fold residue for the ``devDependency`` \u2192 ``dependency`` migration."
            },
            "directness": {
              "description": "Declaration relationship between the project's manifests and an external dependency (ADR-0041 \u00a72): 'direct' (declared in a project manifest), 'transitive' (in the manifest but not declared direct), or 'undeclared' (imported but declared nowhere \u2014 a phantom dependency, and the bucket stdlib falls into). Stamped once at classification time on boundary/dependency Symbols by ``ir.create_boundary_nodes`` via ``DependencyManifest.classify_directness``. A SEPARATE axis from supply-chain ``tier`` (distance) and from the ``ecosystem`` provenance axis (stdlib vs third_party, ADR-0041 \u00a73)."
            },
            "display_name": {
              "description": "Human-readable display name overriding the default. Set by analyzers where the source name is mechanical (e.g. ``__call__`` displayed as the containing class)."
            },
            "documentation": {
              "description": "Docstring / leading-comment text on documented Symbols. Extracted by analyzers that parse docstrings."
            },
            "ecosystem": {
              "description": "Provenance class of an external dependency's distribution channel (ADR-0041 \u00a73): 'stdlib' (shipped with the language runtime) vs 'third_party' (fetched from a package registry). Stamped on tier-3 boundary Symbols by ``ir.create_boundary_nodes`` from the single-source language stdlib catalog (``io_boundary.IoBoundaryCatalog.is_stdlib_module``); absent when the language has no enumerated stdlib. A SEPARATE axis from supply-chain ``tier`` (distance), ``directness`` (declaration relationship), and \u2014 crucially \u2014 from ``package_ecosystem`` (which names the package-MANAGER registry, e.g. 'npm'/'composer', of package-shaped Symbols; this key names provenance CLASS, a different question)."
            },
            "entry_role": {
              "description": "Entry-point role on file-shape Symbols (e.g. 'main', 'script'). Fold residue for the ``main_entry`` / ``script`` \u2192 ``file`` migration."
            },
            "export_scope": {
              "description": "Export visibility scope (e.g. 'module', 'package', 'public'). Finer-grained than the boolean ``exported`` for languages with multi-level visibility."
            },
            "export_source": {
              "description": "Originating source for re-exported symbols (e.g. ES re-export chains). Resolves ``export { foo } from './bar'`` to the underlying definition site."
            },
            "exported": {
              "description": "True if the Symbol is exported from its file (e.g. ES module ``export``, Go capitalized name). Read by reachability / dead-code passes."
            },
            "external_boundary": {
              "description": "True on synthetic boundary Symbols emitted by ``ir._canonical_external_id`` for unresolved-but-referenced names. Read by ``is_external_boundary`` helper."
            },
            "fields": {
              "description": "Declared fields of a class-like symbol, as a mapping of field name to type information. Written by the java and python analyzers on their own class symbols."
            },
            "framework_role": {
              "description": "Framework-specific role of a symbol whose canonical ``Symbol.kind`` is a generic language construct (e.g. 'event_publisher', 'route', 'graphql_resolver'). Fold residue per audit-findings 0013 / WI-habut Wave 5."
            },
            "http_method": {
              "description": "HTTP method a route marker handles (e.g. 'GET', 'POST', 'ANY'). Older producers also smuggle transport sentinels ('WS', 'LIVE', 'RPC') into this key \u2014 a mechanism-vs-category leak ``routes.route_of`` lifts 'WS' out into ``route_protocol`` and INV-tibap folds LIVE/RPC out of."
            },
            "import_path": {
              "description": "Source-language import path on Symbols whose primary identifier doesn't match (e.g. ``from foo.bar import baz`` makes ``baz``'s import_path ``foo.bar.baz``)."
            },
            "inferred_return_type": {
              "description": "Inferred return type for Symbols whose declared type is missing. Set by the limited type-inference passes (Python ``typing.get_type_hints``, etc.)."
            },
            "install_mode": {
              "description": "Install mode on requirement Symbols (e.g. 'editable'). Fold residue per audit-findings 0006 build-config cluster."
            },
            "install_source": {
              "description": "Install source on requirement Symbols (e.g. 'url'). Fold residue per audit-findings 0006."
            },
            "is_class_based_view": {
              "description": "Django class-based-view marker on class Symbols. Set by the Django framework-dispatch linker."
            },
            "is_local": {
              "description": "True on locally-scoped Symbols (e.g. nested function definitions). Read by containment / slice filters."
            },
            "is_native": {
              "description": "True on native-bridge declarations (e.g. JNI method, N-API binding). Set by FFI analyzers."
            },
            "is_recursive": {
              "description": "True on recursive function / method Symbols. Detected by call-graph self-loop analysis."
            },
            "module_system": {
              "description": "Module system on synthetic file-shape Symbols (e.g. 'esm', 'commonjs'). Fold residue for the ``module_file`` \u2192 ``file`` migration per audit-findings 0005."
            },
            "override": {
              "description": "True if the method overrides a superclass / interface method. Set by analyzers that parse ``override`` / ``@Override`` keywords."
            },
            "package_ecosystem": {
              "description": "Package-manager ecosystem on package-shaped Symbols (e.g. 'npm', 'composer'). Fold residue for the ``npm_package`` / ``composer_package`` \u2192 ``package`` migration per audit-findings 0005."
            },
            "parameters": {
              "description": "Structured parameter list on function / method Symbols (name + type + default). Used by signature-shape matching."
            },
            "params": {
              "description": "Short-form parameter-name list on function / method Symbols. Lighter-weight alternative to ``parameters`` for analyzers that don't extract types."
            },
            "parent_base_classes": {
              "description": "Transitive ancestor base classes on class Symbols. Populated by the type_hierarchy linker."
            },
            "reference_syntax": {
              "description": "Use-site reference syntax of an external boundary Symbol (e.g. 'unresolved', 'attribute', 'module', 'namespace') whose canonical ``Symbol.kind`` is 'external_symbol'. Per ADR-0036 Ruling 2 the id kind-slot is a pure copy of ``Symbol.kind``, so the reference syntax that used to live there moves to this registered home. WI-pubiv."
            },
            "return_type": {
              "description": "Declared return type on function / method Symbols. Populated by typed-language analyzers."
            },
            "route_framework": {
              "description": "Framework name uniform across a route marker (e.g. 'flask', 'rails'). The canonical additive home for the route marker's framework: stamped by the framework-pattern route enrichment (first-wins, never clobbering an existing value) and read by ``routes.route_of``, which unions it with the legacy ``concept == 'route'`` dict and Starlette's ``meta['framework']`` (INV-vokak)."
            },
            "route_path": {
              "description": "URL path a ``framework_role == 'route'`` marker handler is registered at (e.g. '/users', '/api/v1/orders'). Analyzer-level (fires with no manifest); normalized by ``routes.route_of``."
            },
            "route_protocol": {
              "description": "Transport protocol of a route endpoint ('http' | 'websocket' | \u2026), split out of the ``http_method`` verb field. Producers persist it by spreading ``routes.transport_meta(method)`` into the route marker's meta (the py/elixir analyzers, the grpc linker, and framework-pattern enrichment); a persisted value wins in ``routes.route_of`` over the 'WS'-sentinel derivation (INV-tibap)."
            },
            "scip_kind": {
              "description": "SCIP-native kind label on Symbols sourced from SCIP indexes. Kept alongside the canonical ``Symbol.kind`` for index round-trip fidelity."
            },
            "scope": {
              "description": "Lexical scope qualifier (e.g. 'workgroup' in WGSL, 'thread_local' in C++). Distinct from ``visibility`` (which is access control) and ``static`` (which is binding-time)."
            },
            "static": {
              "description": "True on static method / field Symbols. Affects method-call resolution (no implicit ``self`` receiver)."
            },
            "symbol_roles": {
              "description": "SCIP-style symbol-role bitset for Symbols sourced from SCIP indexes (definition / reference / import / etc.)."
            },
            "tags": {
              "description": "Free-form tag list for analyzer-specific annotations not (yet) elevated to a named meta key. Use sparingly \u2014 sustained use of a tag is an ADR-0024 promotion signal."
            },
            "task_implementation": {
              "description": "Task implementation language on task Symbols (e.g. 'python'). Fold residue for the ``python_task`` \u2192 ``task`` migration."
            },
            "test_dialect": {
              "description": "Test framework dialect on test Symbols (e.g. 'robot'). Fold residue per audit-findings 0006."
            },
            "virtual": {
              "description": "True on virtual / abstract method Symbols. Distinct from ``override`` (a virtual method may or may not also override)."
            },
            "visibility_signal": {
              "description": "Which signal determined the canonical ``Symbol.visibility`` level (INV-jusot) \u2014 one of 'language_modifier' (an explicit ``modifiers`` term or the legacy Apex/Clojure ``meta['visibility']``), 'name_convention' (Python leading-underscore), or 'default' (public). Provenance for the computed visibility field; set by the finalize visibility pass. Replaces the retired ``meta['visibility']`` key, whose language-syntactic value is now folded into the typed ``Symbol.visibility`` field (the meta key collided with the field under the cross-axis hygiene test)."
            }
          }
        },
        "supply_chain": {
          "type": "object",
          "description": "Supply chain classification",
          "properties": {
            "tier": {
              "type": "integer",
              "enum": [
                1,
                2,
                3,
                4
              ],
              "description": "1=first_party, 2=internal_dep, 3=external_dep, 4=derived"
            },
            "tier_name": {
              "type": "string",
              "enum": [
                "first_party",
                "internal_dep",
                "external_dep",
                "derived"
              ]
            },
            "reason": {
              "type": "string",
              "description": "Why this tier was assigned"
            },
            "is_test_file": {
              "type": "boolean",
              "description": "True if the file holds test code (independent of tier)"
            },
            "is_example_file": {
              "type": "boolean",
              "description": "True if the file is example/demo/sample/tutorial code"
            },
            "is_config_file": {
              "type": "boolean",
              "description": "True if the file is a dependency/build manifest"
            },
            "is_generated_file": {
              "type": "boolean",
              "description": "True if the file is generated code"
            },
            "is_exported": {
              "type": "boolean",
              "description": "True if the symbol is part of the package's public API"
            }
          },
          "required": [
            "tier",
            "tier_name",
            "reason"
          ]
        },
        "cyclomatic_complexity": {
          "oneOf": [
            {
              "type": "integer",
              "minimum": 1
            },
            {
              "type": "null"
            }
          ],
          "description": "McCabe cyclomatic complexity (decision points + 1)"
        },
        "line_span": {
          "oneOf": [
            {
              "type": "integer",
              "minimum": 1
            },
            {
              "type": "null"
            }
          ],
          "description": "Physical line span of the symbol body (end_line - start_line + 1, including blank/comment lines). NOT source-lines-of-code; file-level SLOC is profile.languages[*].loc. Renamed from lines_of_code (WI-bozid)."
        },
        "signature": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Function/method signature string, e.g., '(x: int, y: str) -> bool'"
        },
        "docstring": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "First-line summary of doc comment (truncated to 80 chars)"
        },
        "modifiers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Semantic modifiers (e.g., 'native', 'public', 'static')"
        },
        "discovery_language": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "ADR-0031: host source language where a linker discovered the pattern that produced this Class B synthetic stand-in. Null for real-source declarations (language already carries that information)."
        },
        "protocol_origin": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "ADR-0031: protocol or framework family (kafka, websocket, grpc, database_query, ...) for Class B synthetic stand-ins. Catalog at protocol_origins.py. Null for real-source declarations."
        },
        "display_label": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "ADR-0032: human-readable display string for synthetic linker stand-ins (e.g. \"invoke('save_data')\"). Consumers display it but never branch on it. Null for real-source declarations."
        },
        "qualified_name": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "ADR-0032: fully-qualified name including ancestor containers, joined with the per-language separator declared in qualified_name_axis.py. Null when name already encodes the qualified form or no separator policy is declared."
        },
        "visibility": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "INV-jusot: the single canonical visibility level of the symbol \u2014 one of public / private / protected / internal / package (vocabulary in visibility.py). Computed once in finalize from the highest-priority signal (a language modifier, else the legacy Apex/Clojure meta['visibility'], else the Python leading-underscore name convention, else the public default); the deciding signal is recorded in meta['visibility_signal']. Null only on symbols that predate the finalize visibility pass. Supersedes the retired per-language meta['visibility'] key; is_exported is reconciled to visibility=='public' in a follow-up."
        }
      },
      "required": [
        "id",
        "name",
        "kind",
        "path"
      ]
    },
    "Edge": {
      "type": "object",
      "description": "A relationship between two symbols",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique edge identifier"
        },
        "src": {
          "type": "string",
          "description": "Source symbol ID"
        },
        "dst": {
          "type": "string",
          "description": "Destination symbol ID"
        },
        "type": {
          "type": "string",
          "description": "Relationship type",
          "enum": [
            "calls",
            "imports",
            "instantiates",
            "extends",
            "implements",
            "contains",
            "uses",
            "references",
            "depends_on",
            "depends_on_manifest",
            "sources",
            "subprocess_calls",
            "links",
            "wraps",
            "module_attr_ref",
            "dispatches_to",
            "event_publishes",
            "inherits",
            "decorated_by",
            "includes",
            "constrains",
            "defines_target",
            "data_flows_to",
            "module_exports",
            "overrides"
          ],
          "x-axis-of-values": {
            "calls": "relationship",
            "imports": "relationship",
            "instantiates": "relationship",
            "extends": "relationship",
            "implements": "relationship",
            "contains": "relationship",
            "uses": "relationship",
            "references": "relationship",
            "depends_on": "relationship",
            "depends_on_manifest": "relationship",
            "sources": "relationship",
            "subprocess_calls": "relationship",
            "links": "relationship",
            "wraps": "relationship",
            "module_attr_ref": "relationship",
            "dispatches_to": "relationship",
            "event_publishes": "relationship",
            "inherits": "relationship",
            "decorated_by": "relationship",
            "includes": "relationship",
            "constrains": "relationship",
            "defines_target": "relationship",
            "data_flows_to": "relationship",
            "module_exports": "relationship",
            "overrides": "relationship"
          },
          "x-deprecated": []
        },
        "line": {
          "type": "integer",
          "minimum": 1,
          "description": "Line number where relationship occurs"
        },
        "edge_key": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Canonical key for deduplication"
        },
        "confidence": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Detection reliability (0.0-1.0) \u2014 the producer's evidence-derived estimate that the relationship exists (ADR-0039 ruling 1). NOT a ranking value; ranking prominence lives in rank_score."
        },
        "origin": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Pass IDs that contributed to this edge (INV-jidat)"
        },
        "origin_run_id": {
          "type": "string",
          "description": "Unique execution ID"
        },
        "is_resolved": {
          "type": "boolean",
          "default": true,
          "description": "Whether the dst symbol was resolved at analysis time. Per ADR-0028, this sibling field captures what the Cluster B `*_unresolved` evidence_type values previously smuggled into the inference label. Default True (the ~90% case); Phase 3 producers explicitly set False for the Cluster B fold targets."
        },
        "dst_ref": {
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "lang": {
                  "type": "string"
                },
                "module_path": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                }
              },
              "required": [
                "lang",
                "module_path",
                "name"
              ],
              "additionalProperties": false
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Structured identity for the dst endpoint when it points at an external symbol (stdlib, dependency, unresolved external). Per WI-tihup, this is the canonical source of truth for external-target identity \u2014 the legacy `dst` string field is built from the same ExternalRef and stays populated alongside for back-compat with consumers that haven't migrated. None when dst points at an in-repo Symbol (whose `dst` field is a real Symbol ID)."
        },
        "derived_from": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Symbol (or Edge) IDs the producer consumed to construct this Edge (INV-rukor). Populated by linkers; null for analyzer-originated edges."
        },
        "confidence_source": {
          "type": "string",
          "enum": [
            "evidence_derived",
            "emitter_constant",
            "composite"
          ],
          "description": "Provenance of the confidence value (ADR-0039 ruling 2): evidence_derived (from the evidence_type registry base), emitter_constant (a declared hardcoded producer value), or composite (still fuses a ranking adjustment ruling 3 relocates to rank_score)."
        },
        "rank_score": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Ranking prominence (0.0-1.0), ADR-0039 ruling 3. Initializes from confidence and accumulates the ranking adjustments relocated off confidence; equal to confidence until a producer relocates its adjustment."
        },
        "meta": {
          "type": "object",
          "description": "Edge metadata",
          "properties": {
            "access_mode": {
              "description": "The effect the edge's source has on its destination, from the four-cell vocabulary 'read' / 'write' / 'mutate' / 'delete' (ADR-0015 + ADR-0038 ruling 1) \u2014 derived per-edge from AST role at emission (read-evidence \u2192 read; store position \u2192 write; augmented-assignment / receiver-mutating call \u2192 mutate; delete target \u2192 delete), NOT from the line's statement kind. Applicability is per-edge-type (ADR-0038 ruling 2 / INV-tibob): populated on calls / references / module_attr_ref / event_publishes; Declared-N/A on the structural edge types AND on instantiates (a constructor call is not an access). See ``access_mode_applicable_edge_types`` / ``access_mode_na_edge_types``. Set by the ``dataflow`` annotate passes."
            },
            "bridge_kind": {
              "description": "FFI / native-bridge mechanism on ``calls`` edges that absorb the bridge family (e.g. 'cgo', 'napi', 'wasm', 'ffi'). Value vocabulary tracked at ``edge_types.BRIDGE_KINDS``."
            },
            "call_arg_shape": {
              "description": "What the arguments at this call site CAN carry. One value today: 'literal_only' -- every positional and keyword argument is a literal constant, or there are no arguments at all. INV-fubag, and it exists to be a PROOF rather than a heuristic: taint models a flow as the tainted value being an argument to the sink call or its receiver, so a call passing only literals has nothing that could be the tainted value. Measured cost of not having it (docs/measurements/0003): 24 of 34 adjudicated false positives were sink calls with no arguments at all -- tempfile.TemporaryDirectory(), TemporaryFile(), NamedTemporaryFile(delete=True). ABSENCE IS THE CONSERVATIVE READING and is load-bearing: producers stamp this ONLY when they can prove the argument list is all-literal, so every un-stamped call -- including every edge in every behavior map written before this key existed -- keeps flowing. A default that silenced findings would make this a false-negative generator on a security analysis. Sparse by convention for the same reason: stamping 'dynamic' on every call edge would say nothing absence does not already say, at the cost of a key on every edge of every map. Scoped to the CALL FAMILY -- the question does not arise for an edge that is not an invocation."
            },
            "call_arg_shape_values": {
              "description": "Every distinct ``call_arg_shape`` among the call sites that collapsed into this edge, sorted, capped at ``ir._CALL_LINES_CAP``. Written by ``ir._absorb_call_site`` ONLY when the sites disagreed, in which case the singular ``call_arg_shape`` is removed \u2014 its presence would state one site's value of the whole relationship (INV-vukiv). Absence of this key therefore means every collapsed site agreed, exactly as absence of ``call_lines`` means there was one site."
            },
            "call_construct": {
              "description": "Source-language call construct collapsed under ``ast_call`` apex (e.g. 'method', 'function', 'pipe', 'application'). Fold residue per audit-findings 0012 / WI-nibis Wave 4. Scoped to the CALL FAMILY -- ``calls`` and ``instantiates`` -- declared on ``applicable_edge_types`` below rather than asserted in this sentence, because a scope that lives only in prose cannot be checked and this one drifted (WI-toruz). ``instantiates`` is in scope deliberately: ``edge_type`` is NOT a function of the construct, since ruby emits ``calls`` for the same source construct that dart/csharp emit ``instantiates`` for (INV-kahig), so 'constructor' is the only cross-language invariant for object creation and is NOT redundant with ``edge_type``."
            },
            "call_kind": {
              "description": "Specialized call type for Cluster E sub-case (a) reclassifications (e.g. 'abi', 'subprocess', 'db_query'). Fold residue per audit-findings 0010."
            },
            "call_lines": {
              "description": "Sorted union of every call-site line that collapsed into this edge, its own ``line`` included \u2014 the sibling of ``referring_paths``, minted at the same edge-dedup merge in ir.py. Present only when two or more sites collapsed, so its absence means the single site is ``line``. The call graph keeps one edge per (src, dst, type); this retains the sites that model discards, which ADR-0017 \u00a74 needs to map a dataflow use at line U onto the callee invoked at line U. Capped at ``ir._CALL_LINES_CAP`` (lowest N retained)."
            },
            "call_locality": {
              "description": "File-locality of a call edge \u2014 whether caller and callee live in the same source file ('same_file') or different files ('cross_file'). A SEPARATE axis from ``call_construct`` (which names the syntactic call shape): INV-vakaf split this out after bash/objc were found smuggling 'cross_file' through ``call_construct``. Sparse by convention: set to 'cross_file' on boundary-crossing calls; ABSENT means same-file (the common case)."
            },
            "callee_name": {
              "description": "The callee's name at FULL FIDELITY, as the producer saw it at the call site -- the lossless home ADR-0036 Ruling 1 designates. The id's name slot is deliberately LOSSY (names containing ':' fold to '.') because the id is a location-addressed key, not a fidelity surface, and Ruling 1 says in as many words that consumers needing the exact name MUST read it from elsewhere and never re-derive it from the id. Until this key existed there was no elsewhere for an unresolved external: an Objective-C selector (writeToFile:atomically:) ENDS in a colon, so the id's second-to-last token was the EMPTY STRING, the boundary node synthesised from that id had name='', and the selector existed nowhere in the output (INV-divuf / WI-nakut). Stamped on EVERY edge make_unresolved_edge builds, including the cell where WI-huzuv correctly withholds dst_ref because the module is unknown -- carrying the name in dst_ref sometimes and here otherwise would give one fact two homes."
            },
            "channel": {
              "description": "Logical dataflow channel / conduit the data flows through on a dataflow-family edge \u2014 a message topic (``event_publishes``), IPC channel, CRDT document, crypto primitive, etc. The CONDUIT referent ONLY (WI-pozom): a WebSocket route path uses ``url_path`` and a dispatch target is the edge ``dst`` \u2014 never ``channel``. The transport family is on the co-occurring ``channel_kind`` key."
            },
            "channel_kind": {
              "description": "Channel shape on event / message edges (e.g. 'websocket', 'message_queue', 'crdt')."
            },
            "client_framework": {
              "description": "Client-side framework of a WebSocket edge, from the websocket linker's pattern\u2192framework map (e.g. 'socketio', 'ws', 'native_websocket', 'django_channels', 'fastapi', 'starlette')."
            },
            "data_direction": {
              "description": "Dataflow DIRECTION across a cross-boundary edge \u2014 the way data moves between src and dst, from the closed vocabulary 'src_to_dst' / 'dst_to_src' / 'bidirectional' (``ir.VALID_DATA_DIRECTIONS``). ADR-0038 ruling 3: the post-eviction home of the FFI-bridge / protocol-linker direction semantic that ``access_mode='write'`` + ``dest_access_mode='read'`` used to smuggle (the cgo docstring said it outright \u2014 'Go caller passes data to C'). DIRECTION, not ACCESS \u2014 a SEPARATE axis from ``access_mode`` (ADR-0038 ruling 1). ``dest_access_mode`` is removed entirely (it was observed zero-entropy \u2014 'read' on every populated record). ``event_publishes`` edges do NOT carry this key: publishing IS a genuine write, so they keep ``access_mode='write'`` (ADR-0038 \u00a7Neutral/acknowledged)."
            },
            "detection_pattern": {
              "description": "Pattern-shape inference heuristic that produced this edge (e.g. 'abi_name_match', 'jni_naming', 'implicit_convention') \u2014 names HOW the edge was string/name/URL/regex-matched. Distinct from 'framework_dispatch' (which dispatch convention produced the edge, not how it was matched). Fold residue per audit-findings 0014 \u00a7'detection_pattern partition'."
            },
            "disambiguation_fallback": {
              "description": "True when the edge's destination was resolved by simple-name fallback (no fully-qualified module / namespace / kind disambiguator available). Carries with it the contract that ``confidence <= 0.5`` on the same edge \u2014 see INV-zuhub. Consumers filter the fallback population from the precision-resolved one by checking this flag. Currently set by ``linkers/inheritance.py``; per-linker conformance for the four remaining edge-type families (calls / dispatches_to / references / module_exports) tracked under INV-zuhub items 1-3."
            },
            "dispatch_kind": {
              "description": "Dispatch shape on ``dispatches_to`` edges (e.g. 'route', 'di_register', 'di_resolve', 'annotated'). Fold residue from ADR-0023 Phase 4b dispatch / publish family."
            },
            "env_var": {
              "description": "The variable name behind a synthesized environment read \u2014 the `API_KEY` in `$API_KEY`. Carried for the READER rather than for matching: bash's env_read catalogue is ONE row on the os.environ precedent, so the name never participates in the lookup. PER CALL SITE (INV-vukiv): `$HOME` then `$API_KEY` in one function collapsed to `env_var='HOME'`, which named the harmless read and dropped the secret one."
            },
            "env_var_values": {
              "description": "Every distinct ``env_var`` among the call sites that collapsed into this edge, sorted, capped at ``ir._CALL_LINES_CAP``. Written by ``ir._absorb_call_site`` ONLY when the sites disagreed, in which case the singular ``env_var`` is removed \u2014 its presence would state one site's value of the whole relationship (INV-vukiv). Absence of this key therefore means every collapsed site agreed, exactly as absence of ``call_lines`` means there was one site."
            },
            "ffi_mechanism": {
              "description": "FFI mechanism for cross-language bridges where ``bridge_kind`` is too coarse (e.g. specific JNI / WASM-bindgen / luajit-ffi variants)."
            },
            "framework_dispatch": {
              "description": "The framework-or-generic dispatch convention that produced this edge \u2014 ONE coherent axis ('which dispatch convention recovered this edge'), per ADR-0028 Cluster 28C ('framework-dispatch convention') and spec \u00a79. Values are convention identifiers: framework-specific compounds ('django_orm', 'kafka_streams', 'vue_component', 'tauri_invoke') AND framework-agnostic dispatch mechanisms ('registry_dispatch', 'middleware_chain', 'npm_package'). NOT a bare-framework-name axis \u2014 that reading misreads audit-findings 0014's '<framework_name>' shorthand (INV-junid). Distinct from 'detection_pattern' (pattern-shape match heuristics \u2014 HOW an edge was string/name/URL-matched, not which convention dispatches it). Pure provenance: zero production consumers read this key; promote to a typed Edge.framework field ONLY if a consumer materializes (ADR-0028 OQ2, declined 2026-07-05; ADR-0024 rule 3 threshold = \u22653 distinct values OR \u22652 producers). Fold residue per audit-findings 0014 / WI-kagik Wave 5."
            },
            "handler_name": {
              "description": "Handler function name a route/command edge dispatches to (route_handler and go_cobra linkers) \u2014 the callable servicing the route or CLI subcommand."
            },
            "io_boundaries": {
              "description": "The FULL set of boundaries a single call crosses, for primitives that cross more than one simultaneously (``simultaneous: true`` in the io_primitives YAML \u2014 e.g. scala's ``Process.run``, which spawns AND pipes). The scalar ``io_boundary`` names one of them; this names all of them. Written by ``io_boundary.tag_io_boundaries``."
            },
            "io_boundary": {
              "description": "Boundary classification on edges that cross an IO primitive (e.g. 'net_send', 'fs_read', 'process_spawn'). Two-source field per the WI-fakuv / WI-puvun io-boundary REFRAME: the full value-space is CONSUMER-TIME-derived by ``io_boundary.compute_boundary_map`` from the ``io_primitives`` YAML catalogs and is NOT persisted (it is stamped in memory only by the ``io-boundaries`` / ``verify-claims`` / ``slice --io-boundary`` paths). What survives into a persisted behavior map is only the analyzer producer-hints \u2014 currently just bash's ``command_launch`` (``bash.py``; bash has no ``io_primitives`` catalog so the analyzer stamps it directly), which is why the persisted field reads as ``command_launch``-only. Consumers that need the full classification must run ``compute_boundary_map`` rather than read the persisted meta key."
            },
            "io_mode": {
              "description": "Read/write disambiguation for a mode-parameterised IO primitive \u2014 the ``'w'`` in ``fopen(path, 'w')`` \u2014 resolving one catalogue row to a directional boundary. PER CALL SITE (INV-vukiv): one function may open the same path 'r' at one line and 'w' at another, and those two calls collapse to ONE edge. Measured before the flag existed: adding a preceding open(p,'r') deleted a real truncating open(p,'w') from the boundary map outright, because the survivor carried the first site's mode and the gate eliminated the fs_write row."
            },
            "io_mode_values": {
              "description": "Every distinct ``io_mode`` among the call sites that collapsed into this edge, sorted, capped at ``ir._CALL_LINES_CAP``. Written by ``ir._absorb_call_site`` ONLY when the sites disagreed, in which case the singular ``io_mode`` is removed \u2014 its presence would state one site's value of the whole relationship (INV-vukiv). Absence of this key therefore means every collapsed site agreed, exactly as absence of ``call_lines`` means there was one site."
            },
            "io_primitive": {
              "description": "Fully-qualified IO primitive matched at the boundary (e.g. 'os.open', 'socket.socket'). Set on io_boundary edges by ``io_boundary.compute_boundary_map``."
            },
            "io_target_kind": {
              "description": "What KIND of thing a call site's I/O target is, where the catalogue row cannot tell: 'host_path' (a place in a filesystem), 'null_device' (a kernel sink that discards), 'std_stream' (/dev/stdout, /dev/stderr, /dev/fd/N \u2014 a real crossing, but a logging one rather than a filesystem one), 'in_memory' (WI-lipis: the bytes never left the process \u2014 a `bufio.NewScanner(strings.NewReader(s))` wraps a buffer, not a channel), or 'unresolved' (a variable target; a real write to a place the analyzer cannot name). INV-nular: `redirect.>` is ONE catalogue row whose boundary depends on the target at the call site, exactly as `builtins.open`'s depends on the mode, so the discriminator is stamped by the analyzer and read by `io_boundary.classify_call`. Measured: without it, `echo \"$API_KEY\" > /dev/null` returned `violated` against a fs_write must-not-exist claim. PER CALL SITE for the same reason `redirect_target` is."
            },
            "io_target_kind_values": {
              "description": "Every distinct ``io_target_kind`` among the call sites that collapsed into this edge, sorted, capped at ``ir._CALL_LINES_CAP``. Written by ``ir._absorb_call_site`` ONLY when the sites disagreed, in which case the singular ``io_target_kind`` is removed \u2014 its presence would state one site's value of the whole relationship (INV-vukiv). Absence of this key therefore means every collapsed site agreed, exactly as absence of ``call_lines`` means there was one site."
            },
            "mechanism": {
              "description": "Dispatch/call mechanism on a ``dispatches_to`` or ``calls`` edge \u2014 names HOW the dispatch/call is wired when the canonical ``edge_type`` alone is too coarse. Current value space: 'otp_call' / 'otp_cast' (Elixir/Erlang GenServer synchronous call vs asynchronous cast, folded from the former otp_call/otp_cast edge_types per WI-rorul); 'callback' (Elixir/Erlang behaviour + Rails callbacks, folded from ``invokes_callback`` per audit-findings 0017); 'kernel_launch' (CUDA kernel launch, folded from ``kernel_launch``); 'template' (Vue template\u2192method call, folded from ``template_calls``). Distinct from ``framework_dispatch`` (framework name, e.g. 'otp_genserver') and ``call_construct`` (call-edge syntactic shape)."
            },
            "protocol": {
              "description": "Wire-level protocol on ``calls`` edges that absorb the IPC family (e.g. 'abi', 'ipc', 'ipc_event'), and on ``implements`` edges for protocol-contract implementations folded from the resolver/OpenAPI/RPC family (audit-findings 0016, WI-sumik): a GraphQL resolver satisfying a schema field carries 'graphql'; a Go method satisfying a proto RPC carries 'grpc'. Value vocabulary tracked at ``edge_types.PROTOCOL_KINDS``; per ADR-0023 Phase 4b precedent."
            },
            "query_type": {
              "description": "SQL operation of a database-query edge: 'SELECT', 'INSERT', 'UPDATE', or 'DELETE' (database_query linker)."
            },
            "queue_type": {
              "description": "Message-queue family/pattern that produced a publish/subscribe edge, from the message_queue linker's queue classification."
            },
            "receiver": {
              "description": "Call-site receiver classification: a per-language fold-residue label (audit-findings 0012) emitted only by analyzers whose call syntax carries receiver flavor (Ruby / Go / C# / Rust / C++), hence ABSENT on corpora that lack those languages (e.g. a pure-Python tree). Complete current producer vocabulary (no consumer branches on it yet): 'bare' (implicit / self receiver), 'external' / 'constant_external' / 'stdlib' (receiver resolves outside the module), 'typed_field' / 'typed_var' (receiver has a known type), 'field_chain' (chained field-access receiver), 'generic' (receiver present but unclassified). NOTE: this key mixes a resolution CLASS (bare / external / typed / generic) with an expression SHAPE (field_chain); kept as one key because no consumer distinguishes them \u2014 re-evaluate (split into receiver_class + receiver_shape) if one ever needs to query them independently. Disambiguates ``ast_call`` + ``call_construct=method`` edges."
            },
            "receiver_type_hint": {
              "description": "Inferred type of a call edge's receiver, stamped by the analyzer receiver-type inference to aid cross-file method resolution (e.g. an external constructor's return type)."
            },
            "redirect_target": {
              "description": "The path (or `<unresolved>`) a shell redirection writes to or reads from \u2014 the `/etc/cron.d/pwned` in `echo x > /etc/cron.d/pwned`. Written by bash.py's file_redirect branch. PER CALL SITE, and the measurement that filed INV-vukiv: one function redirecting to /dev/null on one line and to /etc/cron.d/pwned on the next collapsed to a single edge reading '/dev/null', so the cron-dropper was reported as a write to the bit bucket."
            },
            "redirect_target_resolved": {
              "description": "Whether ``redirect_target`` names a literal path rather than an unexpanded variable. Companion boolean so a consumer can tell 'wrote /tmp/x' from 'wrote somewhere I cannot name' without re-parsing the target. PER CALL SITE for the same reason ``redirect_target`` is."
            },
            "redirect_target_resolved_values": {
              "description": "Every distinct ``redirect_target_resolved`` among the call sites that collapsed into this edge, sorted, capped at ``ir._CALL_LINES_CAP``. Written by ``ir._absorb_call_site`` ONLY when the sites disagreed, in which case the singular ``redirect_target_resolved`` is removed \u2014 its presence would state one site's value of the whole relationship (INV-vukiv). Absence of this key therefore means every collapsed site agreed, exactly as absence of ``call_lines`` means there was one site."
            },
            "redirect_target_values": {
              "description": "Every distinct ``redirect_target`` among the call sites that collapsed into this edge, sorted, capped at ``ir._CALL_LINES_CAP``. Written by ``ir._absorb_call_site`` ONLY when the sites disagreed, in which case the singular ``redirect_target`` is removed \u2014 its presence would state one site's value of the whole relationship (INV-vukiv). Absence of this key therefore means every collapsed site agreed, exactly as absence of ``call_lines`` means there was one site."
            },
            "ref_construct": {
              "description": "Source-language construct on a reference-FAMILY (non-call) edge (``references`` / ``includes`` / ``extends`` / ``depends_on`` / ``contains`` / ``data_flows_to``) where the canonical ``edge_type`` alone is too coarse. Value space: 'jsx' (JSX render), 'script_src' (HTML ``<script src=...>``), 'websocket_endpoint' (WebSocket endpoint connectivity), 'event_emit' (Solidity event emission), 'dispatch_table' (C/C++ dispatch-table reference); and \u2014 from the ADR-0023 endpoint_shape fold-tail (audit-findings 0017, WI-pumav) \u2014 'markdown_link', 'rdf_vocabulary', 'association' (Ruby ActiveRecord), 'build_tag_alternative' (Go build tags), 'view_render' (controller\u2192template), 'template' (Twig/Blade extends/includes), 'puppet_class', 'sass_mixin', 'dockerfile_stage' (Docker FROM..AS), 'puppet_require', 'puppet_notify', 'crypto' (crypto write\u2192read on ``data_flows_to``); and \u2014 from the resolver/OpenAPI/RPC family fold (audit-findings 0016, WI-sumik) \u2014 'graphql_resolver_type' (a GraphQL ``@Resolver(() => Type)`` declaration-time association) and 'openapi_operation' (an OpenAPI spec operation referencing its realizing route handler, direction-preserving); and -- per WI-diruk -- 'method_group' (a C# method group, ``Action h = Handle;`` or ``items.ForEach(Process)``, which REFERENCES a method without invoking it; it was emitted on ``call_construct`` until the concept audit found it on ``references`` edges, where a call construct cannot belong). Renamed from ``construct`` per INV-lajov to disambiguate from the sibling ``call_construct`` (which names the call SHAPE on the CALL family -- ``calls`` and ``instantiates``, see that key's ``applicable_edge_types``): distinct vocabularies, distinct edge families. INV-lajov originally wrote 'zero overlap' here on the strength of a corpus measurement in which every observed ``call_construct`` sat on a ``calls`` edge; that described the sample rather than the field, and was already false by five ``constructor``-on-``instantiates`` sites (WI-toruz)."
            },
            "referring_paths": {
              "description": "The list of source file paths that referred to a deduplicated edge \u2014 the only list-of-paths meta value, minted at the edge-dedup merge in ir.py so one canonical edge retains every referring site."
            },
            "refresh": {
              "description": "Boolean flag on a ``depends_on`` edge marking an ordering dependency that ALSO triggers a refresh-on-change of the dst (Puppet ``notify``, folded from the former ``notifies_resource`` edge_type per audit-findings 0017). Present-and-true only on the refresh subset; a plain ordering ``depends_on`` (e.g. Puppet ``require``, ``ref_construct='puppet_require'``) omits it. Makes the require-vs-notify distinction queryable without a distinct edge type; no consumer branches on it yet."
            },
            "resolution_quality": {
              "description": "Pathway-quality label on ``ast_call`` edges (e.g. 'recovery', 'ambiguous'), ORTHOGONAL to ``Edge.is_resolved``. It names uncertainty in the resolution *pathway* (heuristic line-proximity vs. direct binding), NOT target locality \u2014 in-repo-ness lives on ``is_resolved`` (ADR-0037 ruling 1/3), and resolution certainty proper lives in ``confidence`` / ``evidence_type`` (ADR-0028). A recovery edge to a real in-repo method is correctly ``is_resolved=True`` with ``resolution_quality='recovery'``. INV-tadup added 'chained_return_type' (Go: the receiver's type came from a chained call's return type) \u2014 it named a resolution MECHANISM and was being smuggled through ``call_construct``, which is the syntactic-construct axis."
            },
            "server_framework": {
              "description": "Server-side framework of a WebSocket edge \u2014 same vocabulary as ``client_framework``, naming the endpoint that accepts the connection."
            },
            "table_name": {
              "description": "Database table a database-query edge reads or writes, extracted from the query by the database_query linker."
            },
            "topic": {
              "description": "Message-queue / pub-sub topic (or channel) an event_publishes edge with meta['channel_kind']='queue' targets \u2014 the literal topic string, or the variable's identifier name when the topic is dynamic (see ``topic_type``)."
            },
            "topic_type": {
              "description": "Whether the message-queue ``topic`` was a string 'literal' or a 'variable' reference resolved to its identifier name."
            },
            "url_path": {
              "description": "URL / route path on a protocol-boundary edge (e.g. a WebSocket endpoint path, or a cross-language WS client\u2192server route). Distinct from ``channel`` (the dataflow conduit/topic) per WI-pozom."
            },
            "evidence_type": {
              "type": "string",
              "description": "Inference pathway by which the analyzer concluded this edge exists. Per ADR-0028, the canonical axiom is 'Edge.evidence_type names the inference pathway by which the analyzer concluded this edge exists.' The x-axis-of-values map documents the canonical registry from packages/hypergumbo-core/src/hypergumbo_core/evidence_types.py::EVIDENCE_TYPES. The schema enum is intentionally open (type: string without enum constraint) until Phase 4b producer migrations land per-cluster \u2014 current production includes dynamic f-string emits (websocket.py, di_resolution.py, inheritance.py) that produce values outside the static registry. The L3 producer-coherence linter gates literal additions; the open enum honestly reflects the producer-side leak the ADR is fixing.",
              "x-axis-of-values": {
                "ast_annotation": "inference_pathway",
                "ast_attribute": "inference_pathway",
                "ast_call": "inference_pathway",
                "ast_call_direct": "inference_pathway",
                "ast_call_extension": "inference_pathway",
                "ast_call_inherited": "inference_pathway",
                "ast_call_inherited_field": "inference_pathway",
                "ast_call_inherited_method": "inference_pathway",
                "ast_call_static": "inference_pathway",
                "ast_call_this": "inference_pathway",
                "ast_call_this_property": "inference_pathway",
                "ast_call_type_inferred": "inference_pathway",
                "ast_call_ufcs": "inference_pathway",
                "ast_cite": "inference_pathway",
                "ast_decorator": "inference_pathway",
                "ast_extends": "inference_pathway",
                "ast_implements": "inference_pathway",
                "ast_import": "inference_pathway",
                "ast_include": "inference_pathway",
                "ast_includes": "inference_pathway",
                "ast_method_inferred": "inference_pathway",
                "ast_method_this": "inference_pathway",
                "ast_method_this_property": "inference_pathway",
                "ast_method_type_inferred": "inference_pathway",
                "ast_name_read": "inference_pathway",
                "ast_new": "inference_pathway",
                "ast_package": "inference_pathway",
                "ast_perform": "inference_pathway",
                "ast_ref": "inference_pathway",
                "ast_static_call": "inference_pathway",
                "ast_type_ref": "inference_pathway",
                "async_spawn": "inference_pathway",
                "behaviour": "inference_pathway",
                "behaviour_callback": "inference_pathway",
                "bridging_header_import": "inference_pathway",
                "build_dependency": "inference_pathway",
                "build_target_main": "inference_pathway",
                "callable_reference": "inference_pathway",
                "callback_argument_reference": "inference_pathway",
                "canonical_name": "inference_pathway",
                "cgo_call": "inference_pathway",
                "closure_wrapper": "inference_pathway",
                "cmake_target_link": "inference_pathway",
                "constructor_reference": "inference_pathway",
                "designated_init_fptr": "inference_pathway",
                "dispatch_pattern": "inference_pathway",
                "dispatch_table_initializer": "inference_pathway",
                "dispatch_table_reference": "inference_pathway",
                "dockerfile_copy_from": "inference_pathway",
                "dockerfile_from": "inference_pathway",
                "enclosing_scope": "inference_pathway",
                "eta_expansion": "inference_pathway",
                "macro_expansion": "inference_pathway",
                "extends": "inference_pathway",
                "function_pointer": "inference_pathway",
                "function_pointer_arg": "inference_pathway",
                "function_reference": "inference_pathway",
                "function_reference_arg": "inference_pathway",
                "grpc_stub_resolution": "inference_pathway",
                "hash_field_reference": "inference_pathway",
                "hg_annotation": "inference_pathway",
                "import": "inference_pathway",
                "import_declaration": "inference_pathway",
                "import_directive": "inference_pathway",
                "import_statement": "inference_pathway",
                "import_static": "inference_pathway",
                "import_to_manifest": "inference_pathway",
                "include": "inference_pathway",
                "include_directive": "inference_pathway",
                "instance": "inference_pathway",
                "interface_dispatch": "inference_pathway",
                "jsx_element": "inference_pathway",
                "link": "inference_pathway",
                "luajit_ffi_lookup": "inference_pathway",
                "make_prerequisite": "inference_pathway",
                "message_send": "inference_pathway",
                "method_reference": "inference_pathway",
                "module_attribute_reference": "inference_pathway",
                "module_export_heuristic": "inference_pathway",
                "module_identifier_reference": "inference_pathway",
                "module_source": "inference_pathway",
                "naming_convention": "inference_pathway",
                "notify": "inference_pathway",
                "object_field_reference": "inference_pathway",
                "open": "inference_pathway",
                "open_import": "inference_pathway",
                "recipe_dependency": "inference_pathway",
                "reference": "inference_pathway",
                "require": "inference_pathway",
                "require_alias_call": "inference_pathway",
                "require_dynamic": "inference_pathway",
                "require_statement": "inference_pathway",
                "require_static": "inference_pathway",
                "schema_relation": "inference_pathway",
                "scip_occurrence_ref": "inference_pathway",
                "scip_relationship": "inference_pathway",
                "signal_constraint": "inference_pathway",
                "source_statement": "inference_pathway",
                "span_overlap": "inference_pathway",
                "sql_foreign_key": "inference_pathway",
                "stack_construction": "inference_pathway",
                "static": "inference_pathway",
                "struct_field_reference": "inference_pathway",
                "subdir_include": "inference_pathway",
                "trait_impl": "inference_pathway",
                "tree_sitter": "inference_pathway",
                "type_hierarchy": "inference_pathway",
                "typeclass_instance": "inference_pathway",
                "use": "inference_pathway",
                "use_declaration": "inference_pathway",
                "use_directive": "inference_pathway",
                "use-package": "inference_pathway",
                "using_directive": "inference_pathway",
                "variable_match": "inference_pathway",
                "verilog_instantiation": "inference_pathway",
                "vhdl_architecture": "inference_pathway",
                "cffi_call": "pending_classification",
                "ctypes_call": "pending_classification",
                "alias_resolution": "pending_classification",
                "import_resolution": "pending_classification",
                "ipc_channel_match": "pending_classification",
                "topic_match": "pending_classification",
                "ctypes_stdlib_call": "pending_classification",
                "cffi_stdlib_call": "pending_classification",
                "qualified_call": "pending_classification",
                "ast_call_namespace": "pending_classification"
              },
              "x-deprecated": []
            },
            "evidence_lang": {
              "type": "string"
            }
          }
        }
      },
      "required": [
        "id",
        "src",
        "dst",
        "type",
        "line",
        "confidence"
      ]
    },
    "AnalysisRun": {
      "type": "object",
      "description": "Provenance tracking for an analysis pass",
      "properties": {
        "execution_id": {
          "type": "string",
          "description": "Unique run identifier (uuid)"
        },
        "pass": {
          "type": "string",
          "description": "Analysis pass identifier"
        },
        "version": {
          "type": "string",
          "description": "Hypergumbo version"
        },
        "run_signature": {
          "type": "string",
          "description": "Deterministic config hash"
        },
        "repo_fingerprint": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Git state hash for cache invalidation"
        },
        "toolchain": {
          "type": "object",
          "description": "Runtime that produced the analysis (python + tree-sitter/grammar versions when applicable)",
          "properties": {
            "name": {
              "type": "string"
            },
            "version": {
              "type": "string"
            }
          }
        },
        "config_fingerprint": {
          "type": "string",
          "description": "sha256 fingerprint of the effective per-pass config"
        },
        "files_analyzed": {
          "type": "integer",
          "description": "Number of files analyzed by this pass"
        },
        "files_skipped": {
          "type": "integer",
          "description": "Number of files skipped by this pass"
        },
        "skipped_passes": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Legacy per-run mirror of limits.skipped_passes. Pass-level skips (a pass that did not run: no files matched, missing grammar, or crashed) have no analysis_runs[] entry and appear only in top-level limits.skipped_passes; this per-run field has no current producer and is omitted when empty (INV-virik / INV-nihug). Read limits.skipped_passes for skip provenance."
        },
        "failed_files": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Per-file failures recorded by this pass ({path, reason}; drained into limits.failed_files)"
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Warnings emitted during the pass"
        },
        "started_at": {
          "type": "string",
          "format": "date-time",
          "description": "UTC timestamp when the pass started"
        },
        "duration_ms": {
          "type": "integer",
          "description": "Wall-clock duration in milliseconds"
        },
        "pass_version": {
          "type": "string",
          "description": "Code-hash of the pass module (compute_pass_version); distinct from `version` (the package version)"
        },
        "nodes_emitted": {
          "type": "integer",
          "description": "Number of Symbols this pass contributed (INV-gizik / INV-pitab); distinct from files_analyzed (a file count)"
        },
        "edges_emitted": {
          "type": "integer",
          "description": "Number of Edges this pass contributed (INV-gizik / INV-pitab); a pass with edges_emitted>0 must carry duration_ms>0"
        }
      },
      "required": [
        "execution_id",
        "pass",
        "version"
      ]
    },
    "FailedFile": {
      "type": "object",
      "description": "A file that failed during analysis",
      "properties": {
        "path": {
          "type": "string",
          "description": "Repo-relative path of the failed file"
        },
        "reason": {
          "type": "string",
          "description": "Why analysis failed (exception summary)"
        },
        "analyzer": {
          "type": "string",
          "description": "Pass ID of the analyzer that failed"
        }
      },
      "required": [
        "path",
        "reason",
        "analyzer"
      ]
    },
    "ClassificationFailure": {
      "type": "object",
      "description": "A file that failed supply chain classification",
      "properties": {
        "path": {
          "type": "string",
          "description": "Path that could not be classified"
        },
        "reason": {
          "type": "string",
          "description": "Why classification fell through"
        }
      },
      "required": [
        "path",
        "reason"
      ]
    },
    "AmbiguousPath": {
      "type": "object",
      "description": "A file with ambiguous supply chain classification",
      "properties": {
        "path": {
          "type": "string",
          "description": "Path with ambiguous classification"
        },
        "assigned": {
          "type": "integer",
          "description": "Tier that was assigned despite ambiguity"
        },
        "note": {
          "type": "string",
          "description": "Why the classification is ambiguous"
        }
      },
      "required": [
        "path",
        "assigned",
        "note"
      ]
    },
    "SupplyChainLimits": {
      "type": "object",
      "description": "Supply chain classification issues",
      "properties": {
        "classification_failures": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ClassificationFailure"
          },
          "description": "Files that failed supply chain classification"
        },
        "ambiguous_paths": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/AmbiguousPath"
          },
          "description": "Files whose classification was ambiguous"
        }
      }
    },
    "Limits": {
      "type": "object",
      "description": "Known gaps and limitations of this analysis \u2014 what was NOT captured, enabling honest downstream reporting",
      "properties": {
        "failed_files": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FailedFile"
          },
          "description": "Files that failed analysis ({path, reason, analyzer})"
        },
        "skipped_languages": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Languages detected but not analyzed"
        },
        "skipped_passes": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Passes skipped at dispatch time or crashed mid-run, with reasons (a 'crashed: ' prefix marks a contained pass crash)"
        },
        "truncated_files": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Files truncated or skipped due to size"
        },
        "partial_results_reason": {
          "type": "string",
          "description": "Why results are partial, when they are"
        },
        "max_tier_applied": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "description": "Max supply-chain tier filter applied, when any"
        },
        "tier_filtered_files": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Files whose symbols/edges the supply-chain tier filter dropped (e.g. DERIVED tier-4 excluded by default) \u2014 the 'what' behind max_tier_applied's 'why' (WI-tulit)"
        },
        "max_files_per_analyzer": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-analyzer file cap applied, when any"
        },
        "test_files_excluded": {
          "type": "boolean",
          "description": "Whether test files were excluded from analysis \u2014 always emitted so the state is observable (true when excluded, false otherwise)"
        },
        "supply_chain": {
          "$ref": "#/$defs/SupplyChainLimits",
          "description": "Supply chain classification issues"
        },
        "not_captured": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Universal static disclaimer: the fixed categories of constructs static analysis never captures anywhere (dynamic imports, eval, etc.). Identical across all analyses \u2014 NOT a per-repo measurement of constructs this repo contains-but-skipped."
        },
        "analyzer_version": {
          "type": "string",
          "description": "hypergumbo version string that produced this analysis"
        }
      }
    },
    "SliceQuery": {
      "type": "object",
      "description": "The query that produced a feature slice (subset of SliceQuery fields that affect the stable feature id)",
      "properties": {
        "entrypoint": {
          "type": "string",
          "description": "Symbol name, file path, or node ID the slice starts from"
        },
        "hops": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "description": "Max traversal depth (null = unlimited)"
        },
        "max_files": {
          "type": "integer",
          "description": "Max files included in the slice"
        },
        "exclude_tests": {
          "type": "boolean",
          "description": "Whether test files were excluded"
        },
        "exclude_utility": {
          "type": "boolean",
          "description": "Whether utility files were excluded"
        },
        "method": {
          "type": "string",
          "description": "Traversal method (bfs)"
        },
        "reverse": {
          "type": "boolean",
          "description": "True = callers (backward traversal)"
        },
        "max_tier": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "description": "Max supply chain tier included, when set"
        },
        "language": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Entry-point language filter, when set"
        },
        "hub_threshold": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "description": "Hub-degree pruning threshold, when set"
        },
        "exclude_imports": {
          "type": "boolean",
          "description": "Present (true) when import edges were excluded"
        },
        "pass_through_kinds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Node kinds traversed but excluded from output"
        },
        "dataflow": {
          "type": "boolean",
          "description": "Present (true) for dataflow-constrained slices"
        }
      },
      "required": [
        "entrypoint"
      ]
    },
    "Feature": {
      "type": "object",
      "description": "A feature slice index entry (spec \u00a79 features[]): IDs + query + summary so consumers can discover slices via the behavior map alone and diff across commits via the stable id",
      "properties": {
        "entry_nodes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of the entry point nodes"
        },
        "node_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Sorted IDs of all nodes in the slice"
        },
        "edge_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Sorted IDs of all edges in the slice"
        },
        "query": {
          "$ref": "#/$defs/SliceQuery",
          "description": "Query that produced this slice"
        },
        "limits_hit": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Limits reached during traversal (e.g. hop_limit)"
        },
        "node_depths": {
          "type": "object",
          "description": "Node ID \u2192 BFS depth, when recorded"
        },
        "node_tiers": {
          "type": "object",
          "description": "Node ID \u2192 supply chain tier, when recorded"
        },
        "admission_stats": {
          "type": "object",
          "description": "Per-rule dataflow edge-admission counters, when dataflow=True"
        },
        "id": {
          "type": "string",
          "description": "Stable feature ID (sha256 of the canonical query JSON)"
        },
        "name": {
          "type": "string",
          "description": "Human-readable feature name (the query entrypoint)"
        }
      },
      "required": [
        "entry_nodes",
        "node_ids",
        "edge_ids",
        "query"
      ]
    },
    "ValidationViolation": {
      "type": "object",
      "description": "One structured spec-vs-data mismatch from the end-of-pipeline validator stage (ADR-0033)",
      "properties": {
        "severity": {
          "type": "string",
          "enum": [
            "error",
            "warning",
            "info"
          ],
          "description": "Violation severity"
        },
        "validator_class": {
          "type": "string",
          "description": "Which validator class emitted this (axis_conformance, writer_contract, cross_field, verdict_enum, id_format)"
        },
        "message": {
          "type": "string",
          "description": "Human-readable description for review"
        },
        "axis": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Axis name (axis_conformance only)"
        },
        "field_name": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Dataclass field name when applicable"
        },
        "record_id": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Symbol.id / Edge.id / AnalysisRun.execution_id"
        },
        "observed": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Offending value (stringified)"
        },
        "expected": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Short description of what was expected"
        }
      },
      "required": [
        "severity",
        "validator_class",
        "message"
      ]
    },
    "Entrypoint": {
      "type": "object",
      "description": "A detected program entrypoint (main function, CLI verb, HTTP route, etc.). Produced by `detect_entrypoints()`.",
      "properties": {
        "symbol_id": {
          "type": "string",
          "description": "ID of the Symbol that is the entrypoint"
        },
        "kind": {
          "type": "string",
          "description": "Entrypoint category (what the entrypoint IS \u2014 distinct from meta.evidence_type, which names HOW it was detected). One of the EntrypointKind catalog (WI-pupiz, single source hypergumbo_core.entrypoints.all_known_entrypoint_kinds): aiohttp_view, android_activity, android_application, aspnet_controller, background_task, cli_command, cli_main, connectivity_based, controller, django_view, electron_main, electron_preload, electron_renderer, error_handler, event_handler, express_route, fastify_route, form, go_handler, grape_api, graphql_server, hapi_route, html_entry, http_route, koa_route, ktor_route, laravel_controller, library_export, main_function, main_guard, micronaut_controller, middleware_handler, nestjs_controller, phoenix_controller, plug_route, rails_controller, rust_handler, scheduled_task, script_module, serializer, shell_script, sinatra_route, slim_route, spa_bootstrap, spring_controller, test_function, tornado_handler, vapor_route, websocket_handler"
        },
        "label": {
          "type": "string",
          "description": "Human-readable label for the entrypoint"
        },
        "confidence": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Detection reliability (0.0-1.0) \u2014 how sure the detector is that this symbol IS an entrypoint. NOT prominence; ADR-0039 ruling 3 relocates ranking to rank_score."
        },
        "rank_score": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Ranking prominence (0.0-1.0), ADR-0039 ruling 3. Initializes from confidence and accumulates the entrypoint ranking adjustments; entrypoint ordering and the MIN_ENTRYPOINT_CONFIDENCE filter key on this."
        },
        "meta": {
          "type": "object",
          "description": "Provenance dict mirroring Edge.meta (WI-rukam). Keys: 'id' (entrypoint:sha256:<16hex> content hash of kind+symbol_id+label, always present), 'source' (producer pass: concept_detector / connectivity_fallback / script_module_detector), 'evidence_type' (inference pathway: manifest_declared / framework_pattern / structural / language_convention / naming_heuristic / connectivity_heuristic). Registered on the entrypoint_meta axis in axis_meta_keys.py.",
          "properties": {
            "id": {
              "type": "string"
            },
            "source": {
              "type": "string"
            },
            "evidence_type": {
              "type": "string"
            }
          }
        }
      },
      "required": [
        "symbol_id",
        "kind"
      ]
    },
    "UsageContext": {
      "type": "object",
      "description": "Concept-tagged usage site (per ADR-3ccc usage_context_patterns) \u2014 e.g. a Flask `@app.route` call or a Django ORM filter.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable hash ID for this usage site"
        },
        "context_name": {
          "type": "string",
          "description": "Name of the function/decorator/sentinel at this site (e.g. 'Route', 'session.query')"
        },
        "kind": {
          "type": "string",
          "description": "Usage kind \u2014 call, data_value, export, macro"
        },
        "path": {
          "type": "string",
          "description": "Source file path (relative to repo root)"
        },
        "position": {
          "type": "string",
          "description": "Position descriptor \u2014 e.g. args[0], view_func, default (per ADR-3ccc positional vocabulary)"
        },
        "span": {
          "$ref": "#/$defs/Span"
        },
        "symbol_ref": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Resolved Symbol ID if available, otherwise null"
        },
        "metadata": {
          "type": "object",
          "description": "Per-pattern metadata payload (e.g. {route_path, methods, view_name} for a Flask route)"
        }
      },
      "required": [
        "id",
        "kind"
      ]
    },
    "UsagePatternSpec": {
      "type": "object",
      "description": "Specification for matching usage contexts (call-based frameworks)",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Regex pattern to match context kind (call, data_value, export, macro)"
        },
        "name": {
          "type": "string",
          "description": "Regex pattern to match context_name (function called, var defined, etc.)"
        },
        "position": {
          "type": "string",
          "description": "Regex pattern to match position (args[1], :get, default, etc.)"
        }
      },
      "additionalProperties": false
    },
    "Pattern": {
      "type": "object",
      "description": "A pattern to match against symbol metadata or usage contexts",
      "properties": {
        "concept": {
          "type": "string",
          "description": "The concept type this pattern identifies (route, model, task, etc.)"
        },
        "decorator": {
          "type": "string",
          "description": "Regex pattern to match against decorator names"
        },
        "base_class": {
          "type": "string",
          "description": "Regex pattern to match against base class names"
        },
        "parent_base_class": {
          "type": "string",
          "description": "Regex pattern to match parent class's base classes (for methods)"
        },
        "method_name": {
          "type": "string",
          "description": "Regex pattern to match method name (last part of qualified name)"
        },
        "symbol_name": {
          "type": "string",
          "description": "Regex pattern to match symbol name directly"
        },
        "language": {
          "type": "string",
          "description": "Regex pattern to filter by symbol's language"
        },
        "annotation": {
          "type": "string",
          "description": "Regex pattern to match against Java annotations"
        },
        "parameter_type": {
          "type": "string",
          "description": "Regex pattern to match against parameter types"
        },
        "symbol_kind": {
          "type": "string",
          "description": "Regex pattern to match against symbol kind field"
        },
        "extract_path": {
          "type": "string",
          "description": "JSONPath-like expression to extract route path from metadata"
        },
        "extract_method": {
          "type": "string",
          "description": "How to derive HTTP method (decorator_suffix, kwargs.methods, etc.)"
        },
        "prefix_from_parent": {
          "type": "string",
          "description": "Concept name to look up on parent class for path prefix (v1.3.x)"
        },
        "usage": {
          "$ref": "#/$defs/UsagePatternSpec",
          "description": "UsagePatternSpec for matching against UsageContext records"
        },
        "extract": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Dict of extraction expressions for usage-based patterns"
        }
      },
      "required": [
        "concept"
      ],
      "additionalProperties": false
    },
    "FrameworkPatternDef": {
      "type": "object",
      "description": "Framework pattern definition (YAML file structure)",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique framework identifier (e.g., 'fastapi', 'spring')"
        },
        "language": {
          "type": "string",
          "description": "Primary language for this framework"
        },
        "patterns": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Pattern"
          },
          "description": "List of patterns to match"
        },
        "linkers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Linkers that should be activated when this framework is detected"
        }
      },
      "required": [
        "id",
        "language",
        "patterns"
      ],
      "additionalProperties": false
    }
  }
}

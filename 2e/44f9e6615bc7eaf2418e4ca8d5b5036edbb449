Original commit body from 8ae881eff187a552e8e82b4845357fd791b6fdbf:

call_construct was derived from the receiver's NAME, and the name is None for
every receiver EXPRESSION -- new File(x).mkdirs(), get().mkdirs(),
((File) o).mkdirs(), f.getParentFile().mkdirs(). One variable was answering two
questions: "what identifier can I look this receiver's type up under" and "is
there a receiver at all" (LIVE.md rule 7). Measured at tip with a control in
the same run: java, scala and swift stamp the bare-identifier shape and leave
every expression shape unstamped; objc, rust and go were already correct.

THIS IS THE FAIL-OPEN DIRECTION. taint._register_sanitizer_callers refuses an
unresolved bare-name sanitizer match ONLY on call_construct == "method"; with
the key absent the edge reaches the permit branch and registers a PHANTOM
BARRIER, and since PR #214 a barrier earns `sanitized`, which drops the flow
from the claim's violation set. A missing stamp deletes findings.

The parity gate grew the arm that would have caught it. test_disclosure_parity
shipped with plain receiver.method() fixtures and recorded a cast-receiver
residual as "real but separate and much narrower". Re-measured it is neither:
it is EVERY non-identifier receiver, in THREE languages. COMPLEX_RECEIVER_FIXTURES
is registry-driven with the same measured-exemption discipline as the first arm;
python is exempt because it emits no edge at all for a call-result receiver --
an emission gap already filed as WI-makij, cited rather than re-filed.

In scala and swift the same conflated predicate also gated the INV-fahub magnet
guard, so a complex-receiver call fell through to the bare short-name binds that
guard exists to prevent. Both now route through the honest-unresolved branch.

A/B ON SIX REAL REPOSITORIES (82,001 call edges; clover, http4s, gatling,
Kingfisher, VernissageServer, confluent-examples), arms differing in one hunk,
arm proved by module path AND behaviour:

  edge count      IDENTICAL in every repo -- nothing gained or lost
  +15,896 edges   gained the stamp (19.4% of all call edges lacked it)
   2,314 edges    changed dst (scala/swift routing; java 0)

Those 2,314 are removals of confidently-wrong bindings, and the shape says so
without sampling: 1,200 (51.9%) pointed at a field, variable or property --
not callable at all -- and 75% crossed a file boundary on nothing but a short
name (.map() -> an unrelated package.map, .tearDown() -> a different test
class's, .field -> a data field).

`this` stays excluded, narrowly: its receiver type IS known (the enclosing
class), so there is no want of receiver evidence to record. The residual --
this.m() unresolved because m is inherited from an external supertype -- is
filed separately, because stamping it also refuses function-kind catalogue hits
in gate_named_entry and that is a recall change needing its own measurement.

8533 passed, 100% coverage on changed sources, mypy strict ratchet clean at 669.

Racial-Capitalism-Is-Bad: a raccoon building a treehouse shaped like kiddush cup made of bagel chips in a bioluminescent lagoon
Signed-off-by: jgstern-agent <josh-agent@iterabloom.com>

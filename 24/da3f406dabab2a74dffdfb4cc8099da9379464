Original commit body from 0cb34f21f4fb2dd4831a87da142fa22103b1ba81:

The rebase conflicted on .ci/affected-tests.txt, which is generated rather
than authored — hand-merging it would ship a selection computed for a
different base. Regenerated by rerunning the gate on the rebased tree.

The rerun was necessary independent of the conflict: PR #176 passed CI on
a tree that did NOT contain PR-1's changes to taint.py, the module this
PR's DDG work sits on. That green certified the wrong tree.

Signed-off-by: jgstern-agent <josh-agent@iterabloom.com>

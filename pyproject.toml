[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "hypergumbo"
version = "0.5.0"
description = "Local-first repo behavior map generator (MVP)"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "AGPL-3.0-or-later" }
authors = [{ name = "Hypergumbo contributors" }]
keywords = ["static-analysis", "code-graph", "cli", "local-first"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-cov>=5",
  "openai>=1.0",  # For testing LLM-assisted plan generation
  # All language analyzers needed for full test suite
  "tree-sitter>=0.21",
  "tree-sitter-javascript>=0.21",
  "tree-sitter-typescript>=0.21",
  "tree-sitter-php>=0.23",
  "tree-sitter-c>=0.21",
  "tree-sitter-java>=0.21",
  "tree-sitter-rust>=0.21",
  "tree-sitter-language-pack>=0.13",  # Elixir
]
javascript = [
  "tree-sitter>=0.21",
  "tree-sitter-javascript>=0.21",
  "tree-sitter-typescript>=0.21",
]
php = [
  "tree-sitter>=0.21",
  "tree-sitter-php>=0.23",
]
c = [
  "tree-sitter>=0.21",
  "tree-sitter-c>=0.21",
]
java = [
  "tree-sitter>=0.21",
  "tree-sitter-java>=0.21",
]
elixir = [
  "tree-sitter-language-pack>=0.13",
]
rust = [
  "tree-sitter>=0.21",
  "tree-sitter-rust>=0.21",
]
llm-assist = [
  "openai>=1.0",
]
llm-local = [
  "llm>=0.19",
]

[project.scripts]
hypergumbo = "hypergumbo.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/hypergumbo"]
